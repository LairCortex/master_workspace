# image-storage Specification

## Purpose

Файловое хранилище изображений игр: оригиналы и превью лежат на диске в каталоге игры, в базе — только метаданные и sha256; обеспечены дедупликация, управление жизненным циклом файлов и миграция наследственных base64-данных.

## Requirements

### Requirement: Игра — каталог с БД и деревом изображений

Каждая игра SHALL храниться как каталог `games/<имя>/`, содержащий файл базы `game.db` и каталог `images/` с файлами изображений. Файлы изображений игры SHALL находиться исключительно внутри её каталога.

#### Scenario: Создание новой игры

- **WHEN** пользователь создаёт новую игру
- **THEN** создаётся каталог `games/<имя>/` с инициализированной `game.db` и пустым `images/`

#### Scenario: Выбор из списка

- **WHEN** приложение строит список доступных игр
- **THEN** в списке отображаются каталоги, содержащие `game.db`, по имени каталога

### Requirement: Контентная адресация файлов по sha256

Имя файла изображения SHALL быть определено sha256-хэшем его содержимого; путь файла SHALL выводиться детерминированно из хэша и каталога игры. Расширение файла SHALL определяться по реальному формату содержимого, а не по имени исходного файла. Пути файлов SHALL отсутствовать в данных базы: из базы не должен быть возможен путь, не совпадающий с выводимым из хэша.

#### Scenario: Именование файла

- **WHEN** сохранено изображение с хэшем `abcdef…` и форматом PNG
- **THEN** на диске оно лежит в `images/ab/abcdef….png` (подкаталог по первым символам хэша)

#### Scenario: Дедупликация идентичных изображений

- **WHEN** одно и то же изображение (равный sha256) привязывается ко второй сущности
- **THEN** на диске не создаётся второй файл: обе сущности ссылаются на одну и ту же запись `images` и один физический файл

### Requirement: Оригиналы сохраняются в исходном размере

Оригинал изображения SHALL сохраняться по размеру файла, переданного пользователем, без ограничения размера (действовавшее ограничение 1000px снято). Уменьшенная копия (preview) создаётся дополнительно и отдельно от оригинала.

#### Scenario: Импорт изображения больших размеров

- **WHEN** пользователь выбирает изображение 4000×3000
- **THEN** файл-оригинал на диске сохраняет размеры 4000×3000, а preview создаётся по правилу preview-требования

### Requirement: Таблица метаданных images

База игры SHALL содержать таблицу `images` с полями: sha256 (уникальный), расширение, фактические ширина/высота, размер файла в байтах, время создания. Сущности организации, персонажа и локации SHALL ссылаться на изображение ссылкой на запись `images` (с обнулением ссылки при удалении сущности), а не хранить пиксели. Наследственная колонка base64 после миграции SHALL хранить только NULL и не использоваться.

#### Scenario: Запись метаданных при сохранении

- **WHEN** сохранено новое изображение
- **THEN** в `images` появляется одна строка с sha256, определённым расширением, фактическими размерами и размером файла; сущность ссылается на эту строку

#### Scenario: Общее изображение у двух сущностей

- **WHEN** две сущности привязаны к одному файлу изображения (одинаковый sha256)
- **THEN** в `images` одна строка, обе сущности ссылаются на неё; удаление одной из сущностей не удаляет файл

#### Scenario: Очистка наследственной колонки

- **WHEN** legacy-игра смигрирована
- **THEN** базовые колонки base64 содержат NULL, изображения доступны через `images`

### Requirement: Единый конвейер сохранения изображений

Сохранение изображений из любого источника (выбор файла в карточке сущности, поле «image» xlsx-импорта) SHALL проходить через единый конвейер: проверка читаемости → вычисление sha256 → при существующей записи с этим sha256 файловая запись не повторяется → иначе атомарная запись оригинала и preview (частичные файлы по итоговому имени не допускаются) → запись в `images`.

#### Scenario: Нечитаемый файл из диалога

- **WHEN** пользователь выбирает файл, который не может быть декодирован как изображение
- **THEN** пользователю показывается сообщение об ошибке, изображение к сущности не привязывается, приложение работает

#### Scenario: Нечитаемый файл в xlsx-импорте

- **WHEN** в xlsx-файле поле изображения указывает на нечитаемый файл
- **THEN** импорт завершается, сущность создаётся без изображения, в отчёте импорта есть предупреждение по этой строке

#### Scenario: Повторное сохранение того же изображения

- **WHEN** сохраняется изображение, уже существующее в хранилище (sha256 совпадает, файлы на месте)
- **THEN** запись в хранилище не дублируется, сущность получает ссылку на существующую запись

#### Scenario: Атомарность записи

- **WHEN** запись новых файлов прерывается (сбой, ошибка диска)
- **THEN** по финальным именам файлов отсутствуют (частичные файлы не допускаются), приложение остаётся в согласованном состоянии после startup-сканирования

### Requirement: Preview-копия фиксированного формата

Для каждого сохранённого изображения SHALL создаваться preview-копия: максимальная сторона 512px, формат WebP (сжатие с потерями), параметры генерации фиксированы константами, чтобы preview детерминированно пересоздавался из оригинала. Preview генерируется при сохранении и при восстановлении по startup-сканированию; отдельной кнопки/действия для пересоздания preview не требуется.

#### Scenario: Создание preview при сохранении

- **WHEN** сохранено изображение
- **THEN** рядом с оригиналом появляется preview-файл (512px, WebP) по выведенному имени

#### Scenario: Пересоздание утраченного preview

- **WHEN** при открытии игры обнаруживается, что у записи `images` есть оригинал, но отсутствует preview
- **THEN** preview пересоздаётся из оригинала фиксированной процедурой, запись `images` сохраняется

### Requirement: Порядок операций при замене, снятии и удалении

При замене изображения, снятии изображения («Убрать») и удалении сущности SHALL сначала коммититься изменения ссылок в базе, и только затем выполняться удаление файлов (best-effort). Физическое удаление файла-изображения допускается только если на него больше не ссылается ни одна сущность. Ошибка физического удаления SHALL не отменять и не отменяться: пользовательская операция фиксируется, ошибка фиксируется в логе.

#### Scenario: Замена изображения

- **WHEN** у сущности заменено изображение и сохранено
- **THEN** сущность ссылается на новое изображение; файл старого изображения удалён с диска, если не используется другой сущностью

#### Scenario: Замена изображения общим изображением

- **WHEN** изображение сущности заменено на другое, при этом старое изображение используется ещё одной сущностью
- **THEN** файл старого изображения на диске сохраняется

#### Scenario: Снятие изображения

- **WHEN** пользователь снимает изображение с сущности
- **THEN** после сохранения ссылки на изображение у сущности нет; файл удалён, если не используется другими сущностями

#### Scenario: Сбой удаления файла

- **WHEN** после фиксации изменения ссылок операции удаления файла не удалась
- **THEN** изменения сущности сохранены, пользователю операция засчитана, ошибка удаления записана в лог

### Requirement: Startup-сканирование и инвариант целостности

При открытии игры SHALL выполняться сканирование, после которого гарантируется: у каждой записи `images` существует файл оригинала; preview существует или может быть детерминированно пересоздан; каждый файл в хранилище принадлежит ровно одной записи `images`. Правила сканирования: файл без записи — удалить; запись без оригинала — удалить запись и обнулить ссылающиеся ссылки; запись с оригиналом, но без preview — пересоздать preview; незаиспользуемая (без ссылок) запись — удалить запись и файлы.

#### Scenario: Удаление сиротского файла

- **WHEN** в `images/` найдётся файл, для которого нет записи в `images` (остаток прерванной операции)
- **THEN** после открытия игры файл удалён

#### Scenario: Записи без оригинала

- **WHEN** в `images` есть запись, а файл оригинала отсутствует
- **THEN** после открытия игры запись удалена, ссылающиеся сущности очищены, изображения этих сущностей отображаются как отсутствующие

#### Scenario: Повторный запуск

- **WHEN** игра открывается повторно после успешного сканирования
- **THEN** состояние хранилища не изменяется (сканирование идемпотентно)

### Requirement: Миграция наследственных base64-изображений

При открытии игры в базе, содержащей legacy-значения base64 в наследственных колонках, SHALL автоматически выполняться идемпотентная миграция: каждое legacy-значение декодируется, сохраняется через единый конвейер (оригинал + preview + запись `images`), сущность перепривязывается на новую запись, legacy-колонка обнуляется. Если миграция перенесла хотя бы одно изображение, база SHALL быть упакована (VACUUM) в том же запуске до открытия пользовательского интерфейса.

#### Scenario: Первое открытие legacy-игры

- **WHEN** открыта игра, у которой изображения хранятся в base64
- **THEN** все изображения доступны как файлы (оригинал + preview), сущности ссылаются на них, legacy-колонки пусты, база упакована

#### Scenario: Повторное открытие после миграции

- **WHEN** открыта уже смигрированная игра
- **THEN** миграция не выполняется повторно, данные не изменяются

#### Scenario: Мигрированный «оригинал»

- **WHEN** legacy-значение перенесено в файлы
- **THEN** файл-оригинал соответствует тому изображению, которое было в base64 (без возможности восстановить больший размер, который никогда не сохранялся)
