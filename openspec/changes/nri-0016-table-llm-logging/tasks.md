# Tasks: nri-0016-table-llm-logging

Окна П1 (не-модален + реестр) и русские стандартные кнопки уже влиты. Пин до правки.

## 1. «Стол»: lifecycle панели (TB1, TB2, TB5, TB7)

- [ ] 1.1 `panel.py`: `closeEvent` при работающем столе — подтверждение «Остановить стол?», Да → существующий `stop_requested`, по сигналу остановленности — `accept+close` с флагом остановки; Нет — остаются; программное закрытие (смена игры/shutdown) подтверждения не спрашивает. Проверить: `tests/ui/test_table_host_panel_close.py` (fake-host) — close+Да → stop-сигнал и закрытие; Нет → диалог закрыт, панель живёт, host не тронут; закрытие без стола — без вопросов.
- [ ] 1.2 Показ реквизитов только по факту: блоки URL/QR/PIN скрыты до `running`; `refresh_urls` привязан к запуску (`host.port`), `port_spin.valueChanged` сбрасывает показ; кнопки панели без autoDefault (Enter не стартует). Проверить: тест — до старта `isVisible(pin/qr)==False`, после старта адреса/QR содержат фактический порт; смена порта до старта не оставляет старых адресов; `QTest.keyClick return` в порту не эмитит `start_requested`.
- [ ] 1.3 Мелочи панели: подпись «Порт:», плейсхолдер пустого списка игроков, PIN `TextSelectableByMouse`, QR = адрес-хост показанного списка (первый неплейковой IPv4/loopback-only). Проверить: точечные оффскрин-ассерты надписей/флагов; существующие grab-тесты вне «Стола» не затронуты.

## 2. PIN шесть цифр (TB6)

- [ ] 2.1 `table_host_service.py:78-82` — `:06d` `randbelow(1_000_000)`; `web/index.html:12` `maxlength="6"`; `web/app.js:49-50` `!== 6`. Проверить: сервисный тест (уникальность/смена стартов, проверка join с верным/неверным пином — длина 6), веб-константа — греп-пин в тесте панели (`maxlength`==проверочная длина), e2e стола (существующие) зелёные, литерал `:107` существующего теста не врёт длине.

## 3. Посадки и «Выгнать» (TB3-ремонт, TB4)

- [ ] 3.1 Строки посадки: `setItemWidget` с настоящим `QCheckBox`+имя (вместо `ItemIsUserCheckable`); `toggled` ведёт старые вызовы seat()/drop_seat() под тем же `_seating_loading`-гардом; клик по строке — текущая строка. Проверить: оффскрин — чекбокс-виджет в строке, toggle вызывает ожидаемый seat/drop (мок-хост), загрузка чекнутой не даёт рекурсии; поведение мыши как раньше.
- [ ] 3.2 Гейт «Выгнать»: enabled только при `currentRow()>=0` (и running); поведение `kick_selected` без изменений. Проверить: тест — пустой список/без выделения → `isEnabled()==False`; с выбором → включена, клик дёргает kick ровно один раз.

## 4. LLM-окно (LS1–LS4)

- [ ] 4.1 `LlmSetupRoot.qml:174-203`: два `Repeater` → один с делегатом-парой (подпись+поле рядом). Проверить: оффскрин-развёртка детей страницы — чередование label/field и имя поля==имени подписи для всех страниц `FIELD_CONFIG`-полей; при одном поле и при пяти — порядок корректен.
- [ ] 4.2 Окно и футер: показ через реестр П1 (NonModal, title «Настройка LLM…»), постоянные «N из M» и «Закрыть» (reject без подтверждения) на всех страницах. Проверить: окно-формат тест; «Закрыть» не меняет `llm_config.json` (mtime/содержимое в tmp-доме); счётчик = текущая/всего.
- [ ] 4.3 Терминология + LS4: единый корень «промпт» (VM-строки, `llm_setup_view_model.py:91` и греп-обход всех строк `app/` на «промт»), слот имени поля «Ключ API» стабилен (плейсхолдер не в имени). Проверить: тест имени/заголовков; греп-пин: ноль «промт» без «п» в UI-строках; accessibility-ассерт Name==«Ключ API».

## 5. Логи и слушатели (AB5, AB6, D1)

- [ ] 5.1 `main_window.py:28-31,200-218`: handler — `RotatingFileHandler(NRI_MANAGER_DIR/"nri_manager.log", 2_000_000, 3, encoding utf-8)` (каталог создаётся), `_app_root()`-путь удалён целиком, отдельный `setLevel` handler'а снят. Проверить: monkeypatch `NRI_MANAGER_DIR`→tmp_path — он-пишет в tmp-дом, off закрывает поток, повторное включение дописывает; ротация на уменьшенном maxBytes даёт `.1` и не больше трёх архивов; AST-пин: `_app_root`/«рядом с exe» в `main.py`/`main_window.py` отсутствуют; grep логов: секретов нет (регрессия аудита).
- [ ] 5.2 `runtime.py`: `add_listener` возвращает handle, новый `remove_listener`; лаунчер снимает подписку на `finished`; `main.py`-создатели временных окон с подпиской — тоже (инвентаризация грепом `add_listener`). Проверить: тест — закрытый лаунчер не получает/не роняет ничего при `set_theme` (caplog пустой), handle-отписка идемпотентна.

## 6. Версия, тема-семантика, экспорт (AB7, D2, A4)

- [ ] 6.1 `app/__init__.py::__version__` + неактивный пункт «Версия X.Y.Z» в меню «О приложении» (`main_window.py:123-137`). Проверить: `tests/test_version_consistency.py` — pyproject==`__version__` (tomllib); пункт неактивен и показывает ту же версию; релизные места (spec/CHANGELOG) — строка AGENTS обновлена в таске 7.2.
- [ ] 6.2 `LauncherRoot.qml:151` — чекбокс темы фиксированной подписью «Светлая тема» + checked из темы (переключение = существующий поток set_theme без реверса текста). Проверить: тест — текст не зависит от темы, checked == (theme==light), клик переключает и ui.json пишет прежний; пункт меню сохраняет то же оформление (обновление существующего теста).
- [ ] 6.3 `main.py:_on_export_game` — стартовый каталог = каталог базы текущей игры (`games/...` из менеджера сессий), имя как сейчас; не-нативная опция панели из П1 не потеряна. Проверить: тест (tmp home/игры) — `QFileDialog` сконфигурирован начальной директорией `games/` (проверяется через перехват/инъекцию, без реального диалога).

## 7. Гейт, документация, живой контур

- [ ] 7.1 `QT_QPA_PLATFORM=offscreen python -m pytest`, гейт 100 % покрыт (новые модули/ветki — closeEvent, реестр-пути, remove_listener).
- [ ] 7.2 Документация: AGENTS.md (версия — четыре места синхронизации; лог-файл в `~/.nri_manager`; PIN 6 цифр), `docs/functional-checklist.md` (стол закрывается остановкой, версия в меню, лог дома), `docs/CHANGELOG.md`; статус П3 в карте. Проверить: расхождения со спеками нет.
- [ ] 7.3 Живой ре-аудит (computer-use; порт стола — тестовый; выход в сеть не нужен, AI не трогать): закрытие панели с подтверждением и свободный порт после (`lsof`), до/после старта реквизиты, Enter-проверка реальной клавиатурой, LLM-окно: 8 страниц с парами подписей (скриншоты-доказательства), «Закрыть» без записи, «N из M»; версия в меню; лог в `~/.nri_manager` (и в замусоленном dev-дерева не появляется новый nri_manager.log в репо — старый удалить вручную и записать в отчёт); смена темы с закрытым лаунчером; дописка в QA-отчёты серий AB/TB/LS/D. Проверить: TB1–TB7, LS1–LS4, AB5–AB7, D1, D2, A4 закрыты.
- [ ] 7.4 Финал: каждая spec-строка↔тест; `openspec validate nri-0016-table-llm-logging --strict`; commit по запросу пользователя.
