# Tasks: add-game-calendar-core

Открытые вопросы design.md проверены: оба (порядок колонок предпросмотра, TEXT-кодирование) не влияют на то, что строится в C0, — оставлены отложенными.

## 1. Скелет модуля и модели данных

- [x] 1.1 Создать `app/domain/game_calendar.py`: frozen-dataclass'ы `MonthSpec(name, length)`, `IntercalarySpec(name, after_month)`, `CalendarSpec(months, week_names, intercalary)`, координаты `MonthDay(year, month, day)` / `IntercalaryDay(year, index)` (union `GameCoord`), ошибка `InvalidGameDateError(ValueError)`; константы `MIN_YEAR=1`, `MAX_YEAR=9999`; файл никем не импортируется — проверить, что `python -m pytest tests/domain -q` проходит (пустой пока набор не ломается), модуль импортируется `python -c "import app.domain.game_calendar"` без ошибок
- [x] 1.2 Объявить `typing.Protocol` `GameCalendar` (`to_key`, `from_key`, `weekday`, `month_length`, `is_valid`) и заглушки-исключения `NotImplementedError` для будущих реализаций — проверить импортом протокола и `isinstance`-совместимостью заглушки

## 2. Валидация спеки

- [x] 2.1 В тестовом файле `tests/domain/test_game_calendar.py` покрыть табличную отбраковку (пустое имя; повтор имени внутри месяца/недели/вставных; длина месяца < 1; `W < 2`; `W ≠ len(week_names)`; 0 месяцев; отсылка к несуществующему месяцу) и успех «гибких» спек (500×10, месяц 99999, несколько вставных после одного месяца) — тесты красные
- [x] 2.2 Реализовать `validate(spec) -> list[SpecProblem]` с полным списком причин и int64-guard'ом `L ≤ (2⁶³−1)//19999` — зелёные тесты 2.1 + отдельный тест: спека с годовой длиной за guard отклоняется, у границы — принимается
- [x] 2.3 Конструктор `CustomCalendar(spec)` бросает `ValueError` с полным перечнем причин на невалидной спеке — покрыть тестом (создание с двумя дефектами поднимает error, в тексте обе причины)

## 3. Арифметика кастомного календаря (н.э.)

- [x] 3.1 Подготовить префиксные таблицы календаря (длины месяцев с резервом под вставные слоты, стартовые офсеты вставных дней, длина года `L`) и `to_key`/`from_key` для эры н.э. по формуле D4 с бинарным поиском — покрыть круговым обходом малой спеки по всем дням года и краями годов 1/9999; проверить, что `to_key(MonthDay(1,1,1)) == 1`
- [x] 3.2 Вставные дни: слоты после месяца-хозяина по порядку списка; round-trip и монотонность для каждого вставного дня, включая несколько подряд после одного месяца — зелёные тесты 3.1 + новые кейсы
- [x] 3.3 `is_valid`/отказ `to_key` на координатах вне спеки (31-е в 30-дневном, месяц вне числа месяцев, год вне 1…9999, `IntercalaryDay` с вне-списочным индексом) — покрыть тестами; молчаливой нормализации нет (тест «ошиблись днём — имён не меняем»)
- [x] 3.4 `from_key` корректен для спек-гигантов (месяц 99999 дней; 500 месяцев): выборочный круговой обход краёв каждого месяца/вставного дня — зелёные тесты

## 4. Недельный цикл

- [x] 4.1 `weekday(coord)`: счётчик только месячных дней по D5, якорь `MonthDay(1,1,1) → 0`, `IntercalaryDay → None`; `weekday` не зависит от эры-аргумента — покрыть тестами непрерывности: 11-имённая неделя без пропусков через границы месяцев и лет; день недели первого дня месяца не сдвигается вставным днём в предыдущем месяце
- [x] 4.2 Свойство «якорь не в ключе»: два календаря, отличающихся только циклическим сдвигом списка имён недели, дают равные ключи и разные индексы дней — покрыть тестом (будущий сдвиг цикла не меняет порядок)

## 5. StandardCalendar (пресет «Стандартный»)

- [x] 5.1 Реализовать `StandardCalendar` делегированием действующим `era_key`/`date.fromordinal`/`date.weekday` (D1), `month_length`/`is_valid` по настоящему григорианскому календарю, эра-параметр — покрыть круговым обходом високосного года
- [x] 5.2 Золото: совпадение ключей с `app.domain.date_era.era_key` на границах (29 февраля високосного/невисокосного, 1 и 9999 обеих эр, соседи границ месяцев) и случайной выборке обеих эр (seed фиксирован) — тест зелёный
- [x] 5.3 День недели стандартного календаря: григорианский `weekday()` для BC-даты по дате-зеркалу с тем же номером года; вставных дней у пресета нет (`IntercalaryDay` → `is_valid == False`, `to_key` отказывает) — покрыть тестами

## 6. BC-зеркало кастомного календаря

- [x] 6.1 Реализовать зеркало `to_key(н.э. координаты) − 2L·year` в `CustomCalendar` (D4) — покрыть монотонностью по всем дням двух соседних годов BC и «все BC-ключи < 0 < все AD-ключи»
- [x] 6.2 Эра не меняет строение: валидность/длины/позиции идентичны в обеих эрах, round-trip зеркальных координат — покрыть тестами

## 7. Текстовый предпросмотр года

- [x] 7.1 `render_calendar_year(calendar, year) -> str` по D8 (заголовок недели, сетки месяцев по W, плашка `— имя —` после месяца-хозяина) — покрыть снапшот-тестом маленькой спеки с 2 вставными днями
- [x] 7.2 Сверка предпросмотра с арифметикой: колонка каждого дня в снапшоте равна `weekday`, пустые ячейки ровно там, где `weekday` больше не совпал, плашки не участвуют в сетке недель — покрыть параметризованным тестом (неделя 7 и неделя 11)

## 8. Приёмка и финальная проверка

- [x] 8.1 Fuzz-свойства на случайных валидных спеках (seed фиксирован): круговой обход, монотонность ключа, непрерывность недели, неизменность соседей вставного дня — зелёный прогон `python -m pytest tests/domain/test_game_calendar.py -q`
- [x] 8.2 Полный прогон `QT_QPA_PLATFORM=offscreen python -m pytest -q` (ничего в приложении не сломано; новых импортов `game_calendar` в app-код нет — проверить `grep -rn "game_calendar" app/ tests/` вне own-файлов пусто)
- [x] 8.3 Ревью соответствия delta-спека: каждый сценарий `specs/game-calendar-core/spec.md` имеет зелёный тест (сверить таблицей «сценарий → тест» в описании PR/коммита); статус куска C0 в `docs/custom-calendar-roadmap.md` — по закрытию
