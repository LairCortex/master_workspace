# Tasks: nri-0014-window-contract-and-docs

Пин каждого пункта — сначала падающий тест, потом правка (AGENTS «refactor only in green slices»/правило 6). Номера дефектов — сверка с `docs/design-review-roadmap.md`.

## 1. Реестр открытых окон (норма qml-shell, AB4)

- [x] 1.1 Новый `app/presentation/window_registry.py`: `MenuWindowRegistry.open(key, factory)` — живое окно по ключу → `raise_()+activateWindow()` и возврат существующего, иначе `show()` из factory с авто-`forget` по `finished`. Проверить: новый `tests/presentation/test_window_registry.py` — второй `open` не создаёт объект (счетчик factory), возвращает то же окно после `close` создаётся новое; окно с `WA_DeleteOnClose` не оставляется в реестре.
- [x] 1.2 Подключение реестра в composition root/wiring там, где меню создаёт диалоги (только пункты контракта этого изменения — docs-окна и «Сменить игру…»). Проверить: тесты этих входов используют реестр (второй клик пункта → один экземпляр).

## 2. DocViewer: скролл, заголовок, не-модален, экземпляр (AB1–AB4)

- [x] 2.1 `qml/DocViewerRoot.qml`: `ThemeTextArea` оборачивается в штатный `ScrollView` (образец `SheetPresetRoot.qml`), `mono/readOnly` сохранены. Проверить: `tests/ui/test_doc_viewer_scroll.py` — у корня есть `QQuickScrollView`-содержимое, `contentHeight > viewport`, программный `contentY=…` сдвигает содержимое; оффскрин-пин структуры (сейчас ни одного теста скролла острова нет).
- [x] 2.2 `doc_viewer_dialog.py` + `main_window.py:235-245`: не-модальное окно (`NonModal`, `show()` через реестр, ключи `docs_readme`/`docs_changelog`), `setWindowTitle("Документация"/"Changelog")` — окно с шапкой вместо `dlg.open()`-листа. Проверить: тест формата — `windowModality()==NonModal`, `isVisible` не блокирует (нет modality), `windowTitle` соответствует пункту; второй вызов пункта поднимает существующее, README+Changelog → два разных окна.

## 3. «Сменить игру…» — не-модальное окно (A1–A3)

- [x] 3.1 `main.py:433-437`: реестр-показ (`launcher_switch`), `NonModal+show()+raise_()`, комментарий модалености приводится в соответствие; заголовок лаунчера (`game_launcher_dialog.py:58`) остаётся. Проверить: тест — «Сменить игру…» даёт видимое NonModal-окно с непустым заголовком; повторный вход не дублирует; закрытие возвращает в ту же игру (`e2e` о смене игры зелёные без изменений).
- [x] 3.2 `main_window.py:71`: пункт переименован «Сменить игру…». Проверить: AST/текст-тест меню — пункты с гарантированным диалогом заканчиваются «…» (существующие «Экспорт игры…», «Календарь…» не задеты).

## 4. Заголовок листа и глубина стека (E1, CR5; компонент qml-components)

- [x] 4.1 `nri/components/ThemeSheetHeader.qml` (+`qmldir`): `string title` + кнопка «Закрыть» (role/name/Press → `closeRequested()`), токены темы. Проверить: `tests/presentation/test_sheet_header_accessibility.py` — role Button/name «Закрыть», `doAction("Press")` = один сигнал; off-skin не падает.
- [x] 4.2 `EventDialogRoot.qml`, `EntityCardRoot.qml`: шапка из 4.1 сверху корня, title = существующий текст windowTitle (пробросить value из Python в корень), закрытие = существующий reject/Esc-путь без нового сигнала-дубля. Проверить: оффскрин-тест — у корня есть header с нужным title; Press кнопки = диалог закрыт с rejected; grab-тест вайр-лайта пересчитан с шапкой (`test_theme_grab`-контракт не нарушен).
- [x] 4.3 Глубина стека: overlay доп. затемнения родителя при открытии дочернего листа (токен затемнения × глубина, состояние ведёт wiring-владелец листов). Проверить: оффскрин — при двух открытых листах overlay-видимость/alpha родителя выше нуля; при закрытии дочернего — снимается.

## 5. Вёрстка листов настроек (D5, D6)

- [x] 5.1 `XlsxImportRoot.qml:117,197`: невидимые блоки не занимают слотов (переход «скрыт → не создаётся/нулевая высота»); мёртвые зоны ~116/110 pt убраны. Проверить: геометрия-тест оффскрина — сумма видимых рядов = высота содержимого без пустых промежутков > строки.
- [x] 5.2 Широкий модаленый мастер: `CalendarWizardDialog` показывается модальным top-level-окном (`ApplicationModal`+`show()` вместо sheet-`exec/open` с родителем) — без скачка геометрии родителя. Механизм первого запуска (без вложенного цикла) — починка П2, здесь только формат-носитель. Проверить: тест — `windowModality()==ApplicationModal`, родительский `MainWindow` не меняет geometry при открытом мастере.

## 6. Русский язык системных элементов (L1)

- [x] 6.1 `QTranslator` с `qtbase_ru.qm` (путь из `QLibraryInfo`, фиксация языка `ru`) создаётся сразу после `QApplication` до первого окна; `nri_manager.spec`: `datas` папки translations PySide6. Проверить: `tests/test_interface_language.py` — после старта `QMessageBox.question` показывает кнопки «Да»/«Нет»/«Отмена»; `QDialogButtonBox(Ok|Cancel).` кнопки — «ОК»/«Отмена»; guard-тест не завёл второго переводчика.
- [x] 6.2 Все `QFileDialog`-вызовы приложения (экспорт игры `main.py:_on_export_game`, импорт xlsx, картинка, PDF-экспорт, импорт лаунчера) — `DontUseNativeDialog`. Проверить: оффскрин — у созданного диалога опция стоит, заголовок действия/кнопки русские; AST-пин: в `app/` не появилось `QFileDialog`-вызова без опции (список мест в пине).

## 7. Гейт, документация, живой контур

- [x] 7.1 `QT_QPA_PLATFORM=offscreen python -m pytest` зелёные, гейт 100 % не задет (новые Python-строки реестра/языка покрыты тестами выше). Проверить: локальный прогон.
- [x] 7.2 Документация: AGENTS.md (блок computer-use: норма вход→формат — что ждать от живых окон; новые пределы не заведены), `docs/functional-checklist.md` (строки «документ прокручивается», «стандартные диалоги русские», «смена игры — не-модальное окно»), `docs/CHANGELOG.md` (без смены версии), статус П1 в `docs/design-review-roadmap.md`. Проверить: дифф доков сверен с дельтами спеков без расхождений.
- [x] 7.3 Живой ре-аудит (computer-use, throwaway-игра `qa-<date>`, AI-кнопки не нажимать): `window-layout`+`native-focus-repro` по точкам — формат каждого окна контракта (заголовок/крестик/модаленость/дубль-проверка), скролл README/Changelog до конца, «Сменить игру…» не блокирует меню, панель Save As русская; расхождения/починки — дописка в `docs/qa/2026-09-24-*.md`. Проверить: пункты AB1–AB4, A1–A3, E1, CR5, L1, D5, D6 закрыты или зафиксированы как дефекты следующего цикла с причиной.
- [x] 7.4 Финал: дифф сверен с четырьмя spec-дельтами (требование→test), `openspec validate nri-0014-window-contract-and-docs --strict` проходит. Commit `NRI-0014: ...` — только по запросу пользователя.
