# Tasks: nri-0015-main-window-calendar-wizard

Норма формата окон/листов П1 считается влитой. Поведение — сначала пин-тест.

## 1. Главное окно (M1, M4+B6, M5/B4)

- [x] 1.1 `domain/entity_registry.py`: поле `tab_label` для всех типов («Орг.», «Персы», «Вещи», «Локации»); `ThemeTabButton`/`DetailPanelRoot.qml:156-169`: подпись вкладки = `tab_label`, `Accessible.name` = `plural_label`, tooltip = `plural_label`, `minimumWidth` из `implicitWidth` текста + padding (без ручных pt). Проверить: `tests/presentation/test_detail_tabs_labels.py` — при 1028 px ширине ни одна строка вкладки не elided (geometry-ассерт `width >= implicitContentWidth`; живой ре-аудит 5.3 вскрыл поровну делящий вкладки TabBar — пин усилен до `width >= implicitWidth`, добавлена живая ширина колонки 279 px, на делегате `width: implicitWidth`), имена вкладок содержат полные подписи; guard: у каждого типа реестра непустой `tab_label`.
- [x] 1.2 Заглушки пустот: пустая панель деталей — `HintText`-строка «Выберите строку — тут появятся детали» (токен muted, тот же стиль что «Событий ещё нет»); вкладка «Шаблоны» при пустом списке — подсказка про создание/пресет (B6). Проверить: два оффскрин-теста видимости подсказки при пустине и исчезновения при первом элементе; `git diff` не трогает непустые состояния.
- [x] 1.3 `UiPrefs.windows` (`ui_prefs/config.py` — сериализуемый dict роль→[x,y,w,h]) + `presentation/geometry_memory.py` (`restore/attach`, clamp по `QGuiApplication.screens().availableGeometry`); применить: MainWindow, список чар-листов, редактор, Fill (при отсутствии роли — появление по центру активного экрана — B4), окно стола. Проверить: round-trip тест менеджера (сохранил↔прочитал), clamp-тест (роль с координатами за экранами реставрируется внутрь — ассерт пересечения), редактор при первом открытии целиком в экране.

## 2. Мастер календаря (W1= D4, W2=D3, W3=FI-6, W4, W5)

- [x] 2.1 Красная линия старт-контура: e2e `tests/ui/test_e2e_first_run_wizard.py` (offscreen) фиксирует целевое поведение — `Application.start()` завершён, главное окно видимо, мастер открыт поверх, отмена возвращает в окно. Проверить: на текущем коде —падает (текущий путь: `await begin()+exec()`/`main.py:557-564`) — до правки 2.3.
- [x] 2.2 Вёрстка мастера (`calendar_wizard.py`): предпросмотр — единый `ColumnLayout` без промежуточных отступов (числа вплотную под шапкой дней; двойная шапка/мёртвые отступы убраны), левая половина шага центрирована, кнопка «Отмена» и группа навигации — одна строка; числа предпросмотра — токен `color.fg.primary`. Проверить: геометрия-тесты оффскрина (зазор шапка↔первая строка сетки ≤ одной строки; кнопки одного layout-родителя одной строки); цвет — ассерт токена (offscreen-контраст); живьём — пунктом 5.
- [x] 2.3 Радио «Стандартный/Кастомный»: `clicked`→`toggled` (`:217-218`) с защитой рекурсии существующего `blockSignals`-контура `_render`. Проверить: новый тест — `QAccessible` `doAction("Press")`/смена checked приводит к ровно одному `choose_kind` (счётчик), VM сменила kind, «Далее» активна; мышиный клик не удваивает вызовы (существующие тесты зелёные).
- [x] 2.4 Первый запуск без вложенного цикла (`main.py:393,557-564`): `_maybe_show_calendar_wizard` не awaited-`exec()`, а отложенный вызов (`call_soon`) того же открытия мастера, что вход из меню (модаленый top-level из П1, D5-P1). Ошибки применения — путь меню (видимый экран причины). Проверить: e2e из 2.1 зелёные; в логах цикла нет `Cannot enter into task`; тест применения-с-ошибкой (невалидный кастом при записи) показывает отчёт не молча; стартовый мастер остаётся ApplicationModal.

## 3. Единообразие листов (E2, E3/CR1, E4/B5, CR3, CR4)

- [x] 3.1 Метки: общий формат «<Имя>: *» — `EventDialogRoot.qml` (дописать двоеточие «Предыстория»), `EntityCardRoot.qml` (звёздочки обязательных «Название: *», «Характеристики: *»); подписи из существующих источников VM без новых литералов в QML где уже есть модельный текст. Проверить: оффскрин-тест полного набора меток двух листов (список пар «поле→метка» в тесте).
- [x] 3.2 Гейт: сохранение карточки блокируется при пустом обязательном названии, как в событии; тексты причин/тультипов гейта общие. Проверить: тест VM — пустое название → can_save false и кнопка выключена; заполнено → включается; e2e создания карточки не сломался.
- [x] 3.3 AI-кнопка: `EntityCardRoot.qml:118` — `fieldLabel` по целевому полю карточки (ПЕРСОНАЖ не «Событие»); в `EventDialogRoot.qml` проверить каждый `✨` целевым полем; позиционирование — ряд внутри блока поля-мишени. Проверить: оффскрин — имена кнопок = «Сгенерировать: <поле>» по таблице usage-site; grab/geometry-тест: кнопка в границах строки поля; AI не нажимать (реальный запрос) — только имена/позиция в тесте.
- [x] 3.4 `ThemeComboBox`: индикатор раскрытия рисуется внутри поля (contentItem/background), все темы. Проверить: пиксельный grab компонента (индикатор ⊂ rect рамки), существующие острова не поехали (diff по компоненту один).

## 4. Попап даты (P1, P2, P3)

- [x] 4.1 Пустое окно фильтра: страницы обеих сеток открываются на координате текущей календарь-даты; предзаполнение существующих границ не меняется. Проверить: тест попапа без границ — месяц/год = текущая дата; c границами — как было (регрессия существующих сценариев спеки).
- [x] 4.2 Постоянные подписи «Начало окна»/«Конец окна» у обеих сеток до выбора. Проверить: оффскрин — обе подписи видены сразу; после выбора — на месте.
- [x] 4.3 Режим «Все дни»: сплошная accent-заливка всех ячеек убрана (заливка только выбранной границы/hover — токены); контраст обычной ячейки — fg-токены. Проверить: тест delegate-модели (нет состояния «залита» при пустом окне); grab-регрессия не просит exact-пикселей новых цветов; живьём — zoom в пункте 5.

## 5. Гейт, документация, живой контур

- [x] 5.1 `QT_QPA_PLATFORM=offscreen python -m pytest` зелёные, гейт 100 %; новые Python-строки (registry/geometry_memory/wiring) покрыты.
- [x] 5.2 Документация: AGENTS.md (короткие подписи — поле реестра, «одно знание»; память окон в ui.json), `docs/functional-checklist.md` (вкладки читаемы, геометрии помнятся, мастер первого запуска, попап даты), `docs/CHANGELOG.md`; статус П2 в `docs/design-review-roadmap.md`. Проверить: расхождений спека↔доки нет.
- [x] 5.3 Живой ре-аудит (computer-use, throwaway `qa-<date>`): вкладки/детали при 1028 (zoom-читабельность, включая светлую тему), пустые панели, перезапуск на изменённое окно/смену разрешений (clamp), мастер: первый запуск новой игры + «Применить», layout предпросмотра (W1), кнопка W4, радио (реальной мышью где возможно + AX-пресс), попап даты P1-P3; дописка о починках в QA-отчёты серии W/M/P. Проверить: строки W1-W5, M1/M4/M5/B4, E2-E4, CR1/CR3/CR4, B6, P1-P3 закрыты или перенесены с причиной.
- [x] 5.4 Финал: каждая spec-требование↔задача↔тест сверены; `openspec validate nri-0015-main-window-calendar-wizard --strict`; commit `NRI-0015: ...` по запросу пользователя.
