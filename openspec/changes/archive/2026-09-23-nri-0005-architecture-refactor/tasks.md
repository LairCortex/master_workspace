# Tasks: nri-0005-architecture-refactor

Ворота каждой задачи: `QT_QPA_PLATFORM=offscreen python -m pytest` — зелёный полный прогон; поведение сверяется по `docs/functional-checklist.md`. Номера ссылок (`A1`, `B3`, …) — находки из `docs/refactoring-audit.md`; `D#` — решения `design.md`.

## 1. Волна 1: слои и приватное (design D1; находки A1, A2, C1)

- [x] 1.1 Каталог пресетов листа персонажа: перенести `presentation/views/character_sheet/presets/catalog.py` в `domain/character_sheets/preset_catalog.py`, обновить импортёров (в т.ч. `application/services/character_sheet_service.py:47`); прогон зелёный.
- [x] 1.2 `event_service.py:22`: `_TYPE_UNSET` → публичная `TYPE_UNSET`; обновить `wiring.py:15` и ветку `None` конструктора (`event_service.py:45`): явная передача `EventTypeRepository` во всех вызывающих местах (`main.py`, тесты); прогон зелёный.
- [x] 1.3 Переезд `app/application/wiring.py` → `app/presentation/wiring.py` одним коммитом без изменения содержимого; заменить путь импорта в `main.py:51` и `tests/ui/test_e2e_wiring_gaps.py`; прогон зелёный.
- [x] 1.4 `BaseRepository`: публичные свойства `session`/`model`; заменить пять подглядываний (`entity_service.py:92,180`, `event_service.py:45,156`, `character_sheet_service.py:129`, `character_sheet_instance_service.py:70`); прогон зелёный (поломок тестов быть не должно — конструкторы не менялись).
- [x] 1.5 Test-first: в `tests/application/test_calendar_settings_service.py` зафиксировать новый метод `record_names(session, report)` (читает имена по типам записей отчёта); до реализации тест падает, после — зелёный.
- [x] 1.6 Из `calendar_wizard_viewmodel.py` удалить: параметр сессии (`:41`), 6 ORM-импортов (`:65-72`), `_TABLE_MODELS` (`:118-125`), прямой `session.get` (`:549` → вызов `record_names`); обновитьместа конструирования `main.py:413,483` и тесты `test_calendar_wizard_viewmodel.py:96`, `test_calendar_wizard_dialog.py:108`; прогон зелёный.
- [x] 1.7 Ручная сверка: `grep -rn "from app.presentation" app/application app/domain` — пусто (кроме разрешённых мест переехавшего соединителя); `grep "_session\b" app/application/services` — только легальные обращения через публичное свойство.

## 2. Волна 2: единое знание (design D3; находки A3, A5, C3, C4, C7)

- [x] 2.1 В `infrastructure/repositories/coord_mapping.py` добавить `route_insert_dates(obj, fields)`; в `xlsx_import_service.py` удалить `_DATE_SLOTS_BY_FIELD/_INSERT_DATE_PLACEHOLDER/_route_insert_dates` (`:600-622`) и вызвать общую функцию; покрыть кейсом коллизии значений в существующих `tests/application/test_xlsx_import_*`; прогон зелёный.
- [x] 2.2 `presentation/dialog_utils.py`: `_confirm_discard(parent, text)`; заменить четыре копии в `main.py` (`:364-385, 594-606, 683-693, 766-777`); прогон зелёный.
- [x] 2.3 `presentation/qml_island.py`: `IslandDialogMixin` (цикл `QML_IMPORT_PATH/island_context/load_island/release_island`); мигрировать диалогов по одному, каждый — зелёный прогон (17+ диалогов из перечня аудита A5); итог — ни одного ручного `release_island` вне примеси.
- [x] 2.4 Константы: доменный белый список расширений картинок (4 места), общий корень `~/.nri_manager` (подтянуть `ui_prefs/config.py:20` и `doc_viewer_dialog.py:39` к семейству настроец `llm/config.py:9`); прогон зелёный.
- [x] 2.5 Единый синхронизатор связей `sync_related(existing, desired, *, create_missing)` (прикладной слой): свести `entity_service.py:98-123`, `event_service.py:294-314`, `wiring.py:604-614`; семантика каждой траектории закрепляется существующими тестами + новыми кейсами на флаг; прогон зелёный.
- [x] 2.6 Фабрика `dated_repository(model)` вместо четырёх копий репозиториев (character/item/location/organization); обновить точку сборки и тесты `tests/infrastructure/test_repositories.py`; прогон зелёный.
- [x] 2.7 `search_service.py`: типизировать параметры репозиториев, `_type_map` вынести из тела вызова (`:25-31`), использовать реестр типов (после задачи 3.1, если порядок сдвинулся — после реестра); прогон зелёный.

## 3. Волна 3: единый реестр типов сущностей (design D2; находки A4, C5, C6)

- [x] 3.1 `domain/entity_registry.py`: ключ `EntityType`; поля — отображаемое имя, множественная форма, атрибуты связей; тесты реестра: полнота по всем типам, морфология `character`; прогон зелёный.
- [x] 3.2 Миграция потребителей по одному за шаг (прогон после каждого): `entity_service.py:19-31` → `main.py:936-941` → `xlsx_import_service.py:53-59,588-594` → `search_service.py` → `entity_card_dialog.py:67-107` (`_RELATED_CONFIG` становится публичной частью реестра) → `event_dialog.py:36-39` → `timeline_island.py:69-72` → `mention_popup.py:14-17` → `detail_panel_view_model.py:170` → `search_viewmodel.py:20-23` → `world_snapshot_view_model.py:31-40,364` (ручная морфология удаляется) → `llm_service.py:21-40` → `llm_setup_view_model.py:10-42` → `xlsx_schema.py:155-227`.
- [x] 3.3 Тихие развилки `if rel_type is None: return` по пути реестра заменить исчерпывающим `match` по `EntityType`; прогон зелёный, e2e-связи (карточка, ивент, поиск, снимок) сверить по чек-листу.

## 4. Волна 4: крупные узлы с хорошей страховкой (design D6; находки B1, B6)

- [x] 4.1 Рефакторинг под защитой существующих тестов: выделить `presentation/ai_generation_controller.py` из `main.py:952-1183` (весь пакет генерации, закрытие с подтверждением, агрегаты ошибок); `GenerationTarget`-параметр вместо 6-позиционных `_launch`/`request_generation`; `Application` вызывает контроллер; страховка — существующие `tests/ui/test_e2e_llm.py:301,751,799`; прогон зелёный.
- [x] 4.2 `Protocol` `AiCapableDialog` (`get_ai_buttons/get_entity_button/set_save_locked/set_close_guard`); диалоги объявляют соответствие; удалить `hasattr/getattr`-зонды (`main.py:960,988,1172` → контроллер) и задвоенные прокси-геттеры (`event_dialog.py:373-386`, `entity_card_dialog.py:587-606`).
- [x] 4.3 Разрезка xlsx-импорта: `xlsx_analyze.py` (парсинг `:692-746` + слияние `:280-418`), `xlsx_apply.py` (проходы применения `:829-1054`, DB-индекс имён `:433-469` → репозиторный слой), `xlsx_report_text.py` (тексты `:240-245,525-531,869-875`); поведение фиксируют плотные существующие тесты — перенос без изменения утверждений; прогон зелёный.

## 5. Волна 5: транзакции и контракты (design D4/D5; находки A2-остаток, C1-остаток, B3; решение Q14)

- [x] 5.1 Заплатка сценария 6: `main.py:1216` — сырой `asyncio.ensure_future` → общий замок (`run_locked`/`_spawn`); тест-регрессия: параллельные сохранение LLM-настроек и спавн диалога не пересекаются (фикстура с задержкой); прогон зелёный.
- [x] 5.2 Заплатки `event_service`: откат-обёртка `save_event_type` (`:102-123`); ошибка `delete_event_type` (`:146-148`) не глотается — уведомление пользователю (тест на проброс); прогон зелёный.
- [x] 5.3 Заплатка сценария 2: `images/store.py:79-85` — `begin_nested()` (точечный вложенный блок) вместо `rollback()` общей сессии; регрессионные тесты: коллизия контрольной суммы посреди накопленных строк импорта не откатывает их (кейс поверх `test_xlsx_import_apply`, тест `test_image_store`); прогон зелёный.
- [x] 5.4 Заплатки сценария 1: коммит очистки popup-сущностей после delete+flush (`wiring.py:674-678`); прогон зелёный.
- [x] 5.5 Заплатка сценария 4: пост-коммитный сборщик xlsx — собственный `try` с логом (`xlsx_apply`): падение сборщика не превращает успешный импорт в «импорт не удался»; регрессивный тест с падающим gc-колбэком.
- [x] 5.6 Test-first сценария 1 (отмена родительского диалога после создания popup-сущности + случайный коммит другой задачи → сущность не оседает в БД): характеризационный тест на текущем коде (может фиксировать текущее неправильное поведение отдельным `xfail` с задачей на снятие).
- [x] 5.7 `GameSessionUoW(session, lock)`: контекст `.transaction()` (внешний коммит, откат на исключении, нереентерабельность, реестр послезаписных хуков); фикстура `uow(session)` в `tests/conftest.py`; юнит-тесты единицы (успех/ошибка/вложенность/хуки); прогон зелёный.
- [x] 5.8 Перевод траектории «сущность»: `entity_service.update_entity_with_relations` (`:176-193`) — тело в `async with uow.transaction()`, сборщик картинок (`:184-188`) — в реестр хуков (сценарий 3 закрыт: ложного «не удалось сохранить» при записанных данных больше нет); popup-путь соединителя (`:593-628,659-678`) накрывается одной транзакцией; конструкторы репозиториев НЕ менять; снять `xfail` задачи 5.6; прогон зелёный.
- [x] 5.9 Перевод траектории «событие»: `event_service.create_event_with_relations`/`update_event_with_relations` (`:203/:267`) через единицу работы; прогон зелёный (e2e событий — страховка).
- [x] 5.10 Контракты диалогов `@dataclass(frozen=True)` (`presentation/dialog_results.py`): событие создание/редактирование, сущность создание/редактирование; удалить четыре near-дубля `dict.pop` в соединителе (`:263-296`/`:379-427`, `:323-346`/`:529-553`); фабрика `open_entity_card(...)` вместо четырёх повторов обвязки (`:256-304,309-358,365-431,502-566,575-631`); прогон зелёный.
- [x] 5.11 Вторая волна единицы работы: мастер календаря (модель представления получает `uow`, `main.py:413,483`; полный выход сессии из `presentation/`), `main._save_llm_settings`, `ImageStore` (коммит-точки `store.py:195,242` через хуки), листы персонажей, xlsx (дефолтные колбэки `:777-778` — реализация единицы, колбек-параметр удалить); по траектории — зелёный прогон; итоговая проверка: `app/presentation/**` не содержит `commit|rollback|AsyncSession`.

## 6. Волна 6: декомпозиция гигантов (design D4–D6; находки B2, B4, B5)

- [x] 6.1 Из `Application`: `SheetWindowsManager` (жизненный цикл окон листов `main.py:309-867` со своим `deleteLater`-хозяйством); прогон зелёный.
- [x] 6.2 `LlmSettingsRepository` (инфраструктура): ключи `WORLD_PROMPT_KEY/FIELD_PROMPTS_KEY` уходят из `presentation/llm_viewmodel.py:15-16`; прямой CRUD `game_settings` (`main.py:1219-1254`) заменяется вызовами репозитория; `_get_setting`-дубль с календарным сервисом сворачивается; прогон зелёный.
- [x] 6.3 `ExportService` (экспорт `.nri`, `main.py:328-349`) + статус мастера первого запуска — тонкий метод (`:468-505`); прогон зелёный.
- [x] 6.4 Публичные accessors вместо чужого приватного (`main.py:425,546,567,570,577,643,678,741,827,891-926` → `app.wiring.*`; `:749` `fill._instance_id` → публичный getter); прогон зелёный.
- [x] 6.5 `character_sheet_viewmodel` по частям (каждая — отдельный зелёный срез): 6.5.1 `_push_undo` (4 копии `:1280-1283,1331-1333,1345-1349,1664-1668`); 6.5.2 история/выделение/буфер — не-Qt классы с юнит-тестами; 6.5.3 объединение `drag_move*`/`commit_drag*` (`:1173-1252` и `:1407-1494`) в один код с флагом множественности; 6.5.4 `SheetFieldModel` в свой модуль + `Protocol` связи (убрать `getattr(self._vm,...)` `:366`); замер `_refresh_dirty` (время полного прогона теста листа) зафиксирован до/после — деградации нет.
- [x] 6.6 `game_calendar` — только переносы (без изменения логики): три носителя формата (`:1092-1265,1267-1408,1410-1500`) → `infrastructure/calendar_storage.py`; ключи настроек (`:1032-1050`) → слой настроек; соответствующие доменные тесты переезжают физически, утверждения не меняются (проверить по `git diff -U0`, что число assert-ов не уменьшилось); прогон зелёный.
- [x] 6.7 `game_calendar` — развилки: ветвление `_KIND_STANDARD/_KIND_CUSTOM` в носителях → реестр реализаций календаря; ленивый циклический импорт `date_era` устранён явной передажей; глобал `current_calendar` остаётся (зафиксировать решением в коде комментарием со ссылкой на аудит); прогон зелёный.

## 7. Волна 7: хранители границ и документация (design D7/D8)

- [x] 7.1 `tests/test_architecture_layers.py` (AST, механика-прецедент `tests/test_no_qt_date_carriers.py`): запрет `app.application|domain → app.presentation`; запрет `AsyncSession/ORM-моделей/commit|rollback` в `app/presentation/**`; запрет импорта имён с `_` между пакетами; кейсы «нарушение падает с файлом и строкой» + кейсы отсутствия ложных срабатываний; прогон зелёный на текущем дереве.
- [x] 7.2 Пин обхода реестра типов (греп-паттерны словарей тип→модель/репозиторий в прикладном слое, состояние — зелёное после волны 3); кейсы ложных срабатываний; прогон зелёный.
- [x] 7.3 `AGENTS.md`: раздел «Migrations» описывает `infrastructure/db/migrations.py`; упоминание «миграции в init_db() в main.py» убрано; раздел LLM/DI сверен с реальностью (соединитель переехал); `docs/CHANGELOG.md` — запись изменения по формату, версии (`pyproject.toml`, `nri_manager.spec`) не тронуты (`git diff` — пусто).
- [x] 7.4 `AGENTS.md`: новый раздел **«Coding principles»** (design D8) — норматив для всех будущих правок и сессий ИИ; разделы `##` на английском по стилю файла, формулировки сверены с реализацией волн 1–6 (проверить greps-ом: реестр типов единственный, единица работы существует, исключение примеси единственное). Обязательное содержание: (1) слои — `domain`/`application` не импортируют `presentation`/Qt, `presentation` не видит `AsyncSession`/ORM/`commit|rollback`, связывает конкретные классы только точка сборки `main.py`; (2) единое знание — `EntityType`+реестр, правила координат только в `coord_mapping.py`, сквозные константы (корень `~/.nri_manager`, расширения картинок, ключи настроек) в одном месте; (3) транзакции — единственная точка завершения через `GameSessionUoW`, без прямых `commit/rollback` в сервисах и без доступа к `repo._session`, послезаписные действия — хуки, ошибки показываются пользователю; (4) контракты — результаты диалогов замороженные `dataclass`, запрет межмодульного приватного (`obj._x`), композиция над наследованием с единственным исключением `IslandDialogMixin`; (5) рефакторинг — только зелёными срезами, test-first при отсутствии покрытия, поведение/схема/форматы не меняются, документация обновляется тем же изменением; (6) покрытие — см. 7.5; ссылка-источник — `docs/refactoring-audit.md`.
- [x] 7.5 Покрытие 100% как неизмельчаемая норма (design D8): в `AGENTS.md` раздел «Coding principles» включает пункт — шлюз `[tool.coverage.report] fail_under = 100` (line, CI-прогон «line coverage gate») остаётся 100, каждая новая/изменённая строка покрыта тестом, ослабление `fail_under`/`exclude_lines` или новый `# pragma: no cover` — только с обоснованием в предложении изменения. Проверка задачи: `git diff -- pyproject.toml` не трогает шлюз; два существующих `pragma: no cover` сохранены и подписаны причиной; временная правка «новая незакрытая строка в `app/`» роняет `python -m pytest tests/ --cov=app --cov-report=term` (проверить локально и откатить); полный прогон зелёный ровно на 100.
- [x] 7.6 Сквозная приёмка: полный `QT_QPA_PLATFORM=offscreen python -m pytest`; ручной прогон `docs/functional-checklist.md` (сохранение сущности/события, popup-создание с отменой, xlsx-импорт, мастер календаря, LLM-настройки, листы персонажей); `docs/refactoring-audit.md` помечен выполненным по закрытым находкам.

## 8. Закрытие change

- [x] 8.1 Финальная сверка находок аудита и задач (каждая К1/К2 закрыта или помечена явным переносом с причиной); запись в CHANGELOG актуализирована; ready к архивации `openspec archive`.
