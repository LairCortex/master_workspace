# Tasks: close-custom-calendar-map

## 1. Запрет Qt-носителей даты (design D1)

- [x] 1.1 Создать `tests/test_no_qt_date_carriers.py`: AST-скан всех `.py` под `app/` и `tests/` (без `__pycache__`) — падение на `ImportFrom` имён `QDate`/`QCalendarWidget` и на атрибуте/объявлении `setDate`, сообщение содержит файл и строку; проверить, что тест проходит на текущем дереве (упоминания `QDate` в docstring не мешают) и что временная правка «добавить `from PySide6.QtCore import QDate`» в любой файл `app/` роняет его.
- [x] 1.2 Проверить отсутствие ложных срабатываний: `vm.set_dates` не задет, легальный `.date()` у `datetime` не задет — зафиксировать это отдельными кейсами в том же файле; прогон `python -m pytest tests/test_no_qt_date_carriers.py -v` зелёный.

## 2. E2E-приёмка: мастер до конца и жизнь кастомной игры (design D2–D4, spec «E2E-приёмка кастомного календаря»)

- [x] 2.1 Создать `tests/ui/test_e2e_custom_calendar_acceptance.py` с фикстурой кастомной спеки (неделя 8 дней «Перови»—«Осьми», месяцы 3 × {30, 29, 28}, вставной день после месяца 2) по образцу посевов `test_e2e_calendar_wizard_entry.py`.
- [x] 2.2 Реализовать сцену 1: новая игра → клик-проход мастера (выбор «Кастомный» → неделя → месяцы → вставные дни → предпросмотр → «Применить») кликами по `objectName`/`GameCalendarCell`; проверить, что после применения шкала и лента говорят новыми именами без перезапуска.
- [x] 2.3 Продолжить сцену 1: создать событие на вставном дне (клик по `GameCalendarIntercalaryChip` в `ThemeDatePopup`) и событие с эрой «до н.э.» (клетка + эра-чекбокс); проверить подписи шкалы/ленты/детальной/снимка мира (вставной день — имя без номера, эра — суффикс «до н.э.»).
- [x] 2.4 Завершить сцену 1 перезапуском: `Application.shutdown()`/повторный `start()` на той же базе → те же подписи и те же координаты/ключи в БД; прогон `QT_QPA_PLATFORM=offscreen python -m pytest tests/ui/test_e2e_custom_calendar_acceptance.py -v` зелёный.

## 3. E2E-приёмка: правка в середине игры с отчётом о переносе (spec «Правка календаря в середине игры…»)

- [x] 3.1 Реализовать сцену 2 в том же файле: игра с записями на применённом кастоме (`promote_draft`-посев без полного мастер-прогона) → мастер → «Месяцы» укорочение длины месяца, задеющей даты записей → кнопка применения.
- [x] 3.2 Проверить экран отчёта о переносе: среди имён записей — задеваемые; после применения лента и детальная показывают сдвинутые даты; прогон файла зелёный.

## 4. Документация (design D5)

- [x] 4.1 Добавить раздел «Календарь игры» в `docs/README.md`: что настраивается (неделя → месяцы → вставные дни), вставной день ≠ праздник, вход («Настройки → Календарь…», первый запуск новой игры), старые игры = «Стандартный»; вычитка — раздел не противоречит рамкам карты и текстам мастера.
- [x] 4.2 Актуализировать `docs/functional-checklist.md`: строки календаря (мастер вместо «Названий месяцев…», приёмочные формулировки сцен) — сверка с разделом README.

## 5. Закрытие карты и CHANGELOG

- [x] 5.1 Переписать строку C6 в `docs/custom-calendar-roadmap.md` под фактическое содержание (Qt-прокси убраны в C3b; остаток — пин-тест + e2e-приёмка + документация) и закрыть её датой применения; в статусной строке сверху зафиксировать закрытие всей карты (C0–C6).
- [x] 5.2 Записать изменение в `docs/CHANGELOG.md` по прежнему формату («версии не трогаем»); проверить по `git diff`, что версии в `pyproject.toml` и `nri_manager.spec` не тронуты.

## 6. Сквозная приёмка

- [x] 6.1 Полный зелёный прогон: `QT_QPA_PLATFORM=offscreen python -m pytest` — вся копия тестов проходит (новый пин и обе приёмки включены, 100%-гейт покрытия не разрушен).
- [x] 6.2 `openspec validate close-custom-calendar-map --strict` — проходит.
