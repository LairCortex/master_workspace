# Tasks: add-event-timeline-scale-views-w4

## 1. Chart-палитра токенов (ui-theme delta)

- [x] 1.1 Добавить в `app/presentation/theme/tokens.json` обязательные `color.chart.1…8` (пары light/dark, hue-расторы и вариация светлоты, различимы на `color.bg.surface` обеих тем) — проверить тестом генератора: round-trip всех восьми в обеих темах
- [x] 1.2 Тест различимости: для каждой пары chart-токена пиксель-контраст к `color.bg.surface` своей темы выше порога (константа в тесте); тест «chart не читается правилами QSS/CSS chrome» — скан сгенерированных листов
- [x] 1.3 Убедиться, что absence/битый chart-токен гасит тему по общей судьбе «Битые токены» (тест ui-theme расширяется одним кейсом)

## 2. Типы событий: данные (event-types capability)

- [x] 2.1 Domain-сущность `EventType` + поле `Event.event_type: EventType | None`; доменные тесты валидации не требуют new логики — проверить импортом и конструированием
- [x] 2.2 ORM `EventTypeModel` + `events.event_type_id` (nullable FK) в `app/infrastructure/db/models.py`; миграция в `init_db()`: CREATE TABLE, ALTER TABLE ADD COLUMN, conditional seed шести НРИ-дефолтов (guard «только пустая таблица») — тест миграции на старой БД-фикстуре: таблица создана, колонка NULL, сид ровно 6 строк, повторный запуск идемпотентен
- [x] 2.3 `EventTypeRepository` (CRUD + порядок) и методы `EventService`: `get_event_types`, `save_event_type` (валидация `color_index` 1..8), `delete_event_type` (в транзакции отвязка `UPDATE events SET event_type_id=NULL` затем DELETE) — юнит-тесты на in-memory SQLite, включая «удаление занятого типа отвязывает, события целы»
- [x] 2.4 Маппинг событий: репозиторий/сервис подтягивают `event_type` без N+1 (join/`selectinload`) — тест: выборка N событий делает конечное число запросов и отдаёт тип

## 3. Qt-нулевое ядро шкалы: единицы, секции, unit-скобки

- [x] 3.1 `timeline_rows.py`: `ScaleUnit`, расширенные `RowKind`/`Row` (`UNIT`, `SECTION`, поля `unit/unit_count/group_key/token_key`); `build_rows(..., unit, groups)` — DAY-выход побитово прежний (старые юниты W3b/W3c проходят без правок)
- [x] 3.2 MONTH/YEAR-разворот: все единицы окна (включая пустые-заглушки с `unit_count=0`), попадание события пересечением `[start, end|range_end]`, границы окон выравниваются к единицам; якорь открытого конца — конец окна — юниты на матрице (unit × фильтр × пустой день)
- [x] 3.3 Секционирование: `SECTION`-заголовки (алфавит, «Без привязки» последней), внутри секции только тронутые ею единицы, дублирование события в каждую группу; DAY+group: порядок `(has_group, group_name, start, id)` — юниты
- [x] 3.4 Перенос `bracket_lanes` в ядро c unit-режимом (span → `[first_unit, last_unit]`, lane-упаковка прежняя); юниты: скобка через N месяцев, оборот дорожек, открытый конец
- [x] 3.5 `index_at_y`/`normalize_range`/`prev/next_event_index` — прогнать существующие юниты против новой модели (contract равновысоких строк не изменился) и добавить кейс «на MONTH нет EVENT-строк → хелперы не выдают event-цели»

## 4. ViewModel: ступень, группировка, спуск лестницы

- [x] 4.1 `TimelineViewModel`: поля `unit`/`group_by` (дефолт `DAY/None`, не сериализуются), прокидка в `build_rows` + сборка `event→groups` из доменных связей — тест: смена unit меняет `rows`, фильтр и выборку не трогая
- [x] 4.2 Спуск лестницы: `_ensure_day_unit_for(event)` перед внешним выбором (метод пути поиска) и перед jump-командами — тест: выбор id на MONTH ставит unit=DAY, строка в rows, выбор на месте; событие вне фильтра по-прежнему сбрасывает выбор во всех слоях
- [x] 4.3 Смена unit/group_by сохраняет выбранное событие и фильтр (пересборка rows без сброса `_selected_event`) — тест

## 5. Виджет: rendering и жесты крупных ступеней

- [x] 5.1 Делегат: позиции `UNIT` («Март 1245 · 4 события» / приглушённая пустая), `SECTION` (title-вес, текст группы), точка типа слева от строки события (`token_key or color.fg.muted`, `DOT_SIZE=8`, без обводки поверх выделения); равновысокость всех четырёх видов — grab/pixel-тесты
- [x] 5.2 Рейка на MONTH/YEAR: тик против каждой позиции-единицы, раз в год подпись года на MONTH, подпись года на YEAR-ступени; скобки unit-aware из 3.4 — grab/pixel
- [x] 5.3 Липкая метка: подпись верхней позиции по ступени (дата / «Март 1245» / «1245»), follow по единице в рейке и во время drag'а — тесты sync+follow на MONTH
- [x] 5.4 `Ctrl/Cmd + колесо`: шаг лестницы с якорем (приближение — единица из-под липкой, отдаление — первая видимая дата), скролл-шаг без Ctrl не меняется — e2e-тест жеста
- [x] 5.5 Клик по позиции MONTH/YEAR — приближение на ступень с этой единицей как якорем, без selection/id-сигналов; клик в рейке — прыжок по единице — юнит hit-test + e2e
- [x] 5.6 Drag рейки на MONTH/YEAR: подсветка покрываемых единиц, ровно один emit `day_range_applied` с нормализацией к полным датам единиц (1 число/last-day месяца, 1 янв/31 дек года); одно-единичный drag = клик — юниты маппинга + e2e «drag месяцами → чип показывает целые границы»
- [x] 5.7 Шапка: переключатель ступеней (сутки·месяц·год) и группировки (выкл·персонажи·локации·организации·предметы) вписаны в существующую раскладку без вытеснения «+»/чипа/прыжков; переключение сохраняет выбор и фильтр — e2e

## 6. Типы: диалоги и точка входа

- [x] 6.1 `EventTypesDialog` (по образцу `MonthSettingsDialog`): список, переименование, ↑/↓ порядок, добавление, удаление (отвязка подтверждением не нужна по спеке — просто отвязка), выбор цвета из 8 токено-кружков (оф-скин — нумерованные серые образцы), `attach_theme`+live-retheme — тесты round-trip через service
- [x] 6.2 Пункт «Типы событий…» в «+»-меню панели событий, открывает диалог; изменения применяются к игре сразу и видны в шкале без перезахода — e2e
- [x] 6.3 Диалог события: селектор типа (current + «без типа») с цветными точками из текущих токенов, опциональность; сохранение/снятие типа round-trip — тест диалога + e2e «назначил Слух — точка на строке шкалы в hex токена»

## 7. Приёмка и регрессия

- [x] 7.1 Пиксель-приёмка типа: метка строки события с индексом k = `color.chart.k` в обеих темах; live-retheme восьми цветов сменой токенов без потери выбора/скролла — grab/pixel
- [x] 7.2 Инварианты: `test_no_chrome_hex.py` на обновлённом файле `timeline_widget.py` (ни hex, ни palette()); оф-скин-путь делегата на Qt-глобалах — существующий/расширенный кейс
- [x] 7.3 Полная регрессия: `QT_QPA_PLATFORM=offscreen python -m pytest` зелёная (W3/W3b/W3c сценарии суточной ступени не разрушены); обновление затронутых существующих тестов (ROLE-роли, delegate-pixel W3b)
- [x] 7.4 Docs: запись в `docs/CHANGELOG.md` (W4, список миграций «через init_db()»), статус W4 в `docs/design-system-roadmap.md` («влито» по факту merge-коммита, черта QML «после W4» подтверждается) — проверяется ревью
