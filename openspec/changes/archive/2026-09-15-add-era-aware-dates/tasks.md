# Tasks: add-era-aware-dates

## 1. Домен: эра и общий ключ

- [x] 1.1 Создать `app/domain/date_era.py` (`era_key`, сравнение пар `(date, is_bc)`, проверка диапазона 1…9999 обеих эр) и покрыть юнит-тестами порядок через границу эр, зеркальную високосность и отказ нулевого года; `python -m pytest tests/ -k era` зелёные
- [x] 1.2 Добавить `is_bc`-признаки дат в `BaseEntity` и `Rating` и перевести валидацию «конец не раньше начала» на помощник из 1.1; тесты в `tests/domain/test_entities.py`: событие через эру проходит, конец 200 г. до н.э. при начале 100 г. до н.э. отклоняется

## 2. Хранилище: столбцы, переезд, запросы

- [x] 2.1 Добавить ORM-столбцы `start_bc`/`end_bc` (DEFAULT 0) и nullable `start_key`/`end_key` во все шесть таблиц (`events`, `organizations`, `characters`, `items`, `locations`, `ratings`) в `infrastructure/db/models.py`; тест в `tests/infrastructure/test_models.py` проверяет заполнение эры/ключа круговым сохранением
- [x] 2.2 В `init_db()` расширить `_MIGRATIONS` четырьмя столбцами на таблицу, добавить до первого переезда копию `game.db.bak` (если её нет), и идемпотентный SQL-бэкфилл ключей по формуле design D4; тесты: старая БД-фиксттура после двух прогон `init_db` идентична себе по датам, содержит эру «н.э.», заполненные ключи и одну копию `.bak`
- [x] 2.3 Перевести `event_repository.py` (`order_by`, оконный запрос) на ключи и передать ключи из viewmodel-окна; тесты репозитория: смешанная сортировка 500 г. до н.э. → 1 г. н.э. → 2026, бессрочное из до н.э. попадает в окно н.э.
- [x] 2.4 Бэкфилл ключей в `init_db()` закрывает строки, вставленные без ключей (эмуляция old-version row); тест: строка с `start_key IS NULL` после `init_db` получает ключ и корректное место в выборке

## 3. Формат и ввод

- [x] 3.1 Расширить `format_game_date` выводом «N г. до н.э.» для эры до н.э. (наша эра — прежний формат); протестировать оба формата включая пустую эру даты
- [x] 3.2 В `theme_date_popup.py` (`_CustomCalendar`): границы `0001-01-01…9999-12-31`, спин 1…9999, чекбокс «до н.э.» без смены чисел даты, clamp по выбранной дате; попап отдаёт пару (дата, эра); обновить `tests/presentation/test_theme_date_popup.py` (было: 100…9999)
- [x] 3.3 В `timeline_date_popup.py` (попап диапазона) — независимый чекбокс эры у каждого календаря, попап возвращает две пары; тест смешанного окна 500 г. до н.э. … 100 г. н.э.
- [x] 3.4 Перевести мосты дат диалогов (`event_dialog`, `entity_card_dialog`, попап снимка мира) на пары (дата, эра) с умолчанием «сегодня, н.э.»; обновить `tests/presentation/test_dialogs.py`

## 4. QML-мосты и шкала

- [x] 4.1 Добавить viewmodel-свойства `startBc`/`endBc` (событие, карточка сущности, снимок мира), передать их в `ThemeDateField` через готовую строку отображения; QML не вычисляет эру; тесты островов проверяют суффикс «до н.э.» в отображении
- [x] 4.2 Перевести `timeline_rows.py` (фильтр окна, сортировка `(момент, id)`) и логику окна `timeline_viewmodel.py` на общий ключ из 1.1; тесты `timeline_rows` на смешанные эры и окно через границу

## 5. Импорт xlsx

- [x] 5.1 Расширить `parse_cell_date` распознаванием «N г. до н.э.» и `-YYYY-MM-DD` (нативный Excel и голый ISO — по-прежнему н.э.); тест `xlsx_schema` на оба формата и на неразбираемый ввод как проблему строки
- [x] 5.2 Учёт эры в проблемных строках и призраках колонок связей (`xlsx_import_service.py`, минимум/максимум дат — через ключ); тест автосоздания призрака из строки с датой до н.э.
- [x] 5.3 Форматы до н.э. в подсказке диалога (`build_format_text` + описания колонок «Дата начала»/«Дата конца») и демо-строка «Заговор» с обоими текстовыми форматами в `import_template.xlsx` (перегенерирован генератором); тесты хинта и импорта шаблона обновлены

## 6. Документация, спецификации, регресс

- [x] 6.1 Обновить `docs/README.md` (даты 1…9999 двух эр, формат «N г. до н.э.») и `docs/CHANGELOG.md` (миграция через `init_db()`, копия `.bak`)
- [x] 6.2 Прогнать полный набор `QT_QPA_PLATFORM=offscreen python -m pytest` — все зелёные; проверить grep-инвентаризацию, что нигде не осталось прямых сравнений `start_date`/`end_date` вне `date_era` и бэкфилла
