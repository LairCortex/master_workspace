# Tasks: nri-0012-qml-accessibility

## 1. Компоненты (роль/press/focus внутри библиотечных компонентов)

- [x] 1.1 `nri/components/RowItem.qml`: `Accessible.role: Accessible.ListItem`; `Accessible.name` = текст строки; `Accessible.onPressAction` испускает `activateRequested()`. Мышиный путь не меняется (clicked=выбор, doubleClicked=открытие). Проверить: новый `tests/presentation/test_rowitem_accessibility.py` — роль/имя; `actionInterface.doAction("Press")` ровно один раз испускает `activateRequested`; одинарный `QTest.mouseClick` испускает только `selectedRequested`; в off-skin роль/имя те же.
- [x] 1.2 `nri/components/ThemeSwatch.qml`: `Accessible.role: Accessible.RadioButton`; строковое свойство `accessibleName` (default «Цвет палитры №<effectiveColorIndex>») в `Accessible.name`; `Accessible.checked: checked`; `onPressAction` исполняет существующее выбор-поведение (checked=true + `clicked()`). Проверить: тест роли, имени (включая переопределение именем острова), состояния выбора и Press→`clicked()` с `checked=true`.
- [x] 1.3 `nri/components/MentionField.qml`: корень — `Accessible.role: Accessible.EditableText` + `onSetFocusAction` → `forceActiveFocus()` plain-слоя; чип делегата — `Accessible.role: Accessible.Link`, `Accessible.name: modelData.display`, description «Открывает упомянутую сущность», `onPressAction` → `host.activateMention(type,id)`. Проверить: в `test_mention_field_qml.py` — `doAction("SetFocus")` ставит `activeFocus` plain-слою; `doAction("Press")` по чипу = ожидаемый `activateMention`; role/name чипа.

## 2. Острова 9-файлов: аннотации и тесты на остров

- [x] 2.1 `TimelineRowDelegate.qml` + `TimelineRoot.qml`: делегат — `ListItem`, `Accessible.name: row.caption`, description «Открывает событие», `onPressAction` → обработчик двойного клика (`eventDoubleClicked(eventId)`); addButton — `Accessible.name: "Добавить событие"`. Проверить: новый `tests/presentation/test_timeline_accessibility.py` — role/name/description делегата, Press эмитит сигнал открытия события, addButton имеет имя «Добавить событие» по interface; mouse-одинарный/двойной пути не изменились (существующие тесты зелёные).
- [x] 2.2 `DetailPanelRoot.qml`: делегат строки-карточки — `ListItem`, `Accessible.name: rowCard.entityName`, description «Открывает карточку», `onPressAction` → `detailPanelVm.activate(...)` + сигнал двойного клика; картинка — `Button`, `Accessible.name: entityName` (или «Изображение» при его отсутствии), description «Открыть изображение», `onPressAction` → `detailPanelVm.requestImage(...)`. Проверить: новый `tests/presentation/test_detail_panel_accessibility.py` на role/name/description и Press→обрабатываемому сигналу; табы не аннотированы.
- [x] 2.3 `WorldSnapshotRoot.qml`: entity-строка inline-делегата — `ListItem`, `Accessible.name: displayText`, `onPressAction` → `worldSnapshotVm.select(index)`, description «Переходит к сущности»; header-строка — `Button`, `Accessible.name: displayText`, description «Развернуть или свернуть раздел», `onPressAction` → `toggleSection(index)`. Проверить: новый `tests/presentation/test_world_snapshot_accessibility.py` — role/name двух kind'ов, Press header'а эмитит toggle, Press entity — select.
- [x] 2.4 `EntityCardRoot.qml`: usage-site `Accessible.name` по карте design.md (поле имени «Название», даты «Дата начала»/«Дата конца», «Рейтинг», «Ссылка на музыку», mention-поля «Характеристики»/«Предыстория» и `modelData.label` у extra, музыка-открытие «Открыть ссылку на музыку», AI-кнопки «Сгенерировать: <fieldLabel>» — заполнить `fieldLabel: "Событие"` у entity-AI). Проверить: новый `tests/presentation/test_entity_card_accessibility.py` — у перечисленных полей правильная роль и name из интерфейса; кнопки «Сохранить», «Открыть чар-лист» имеют ровно штатное имя из text.
- [x] 2.5 `SheetCanvas.qml`: делегат-поле — роль по `model.type` (текст/многострочный/число/дата → `EditableText`, checkbox → `CheckBox` с `Accessible.checked`, картинка → `Button`, прочие кликабельные → `Button`), `Accessible.name` = label/content по карте design.md; `onPressAction` вызывает существующую ветку одиночного нажатия текущей `mode` (design → `vm.select(fieldId)`; fill → существующий fillPress: открыть инлайн, toggle чекбокса, dropdown, image-файл через существующие обработчики — без дублирования кода); заголовок/разделитель не аннотируются. Инлайн-редактор (штатный `TextField`/`TextArea`) получает `Accessible.name: "Редактирование поля"` у своего корня применения. Проверить: `tests/presentation/test_sheet_canvas_accessibility.py` — роль по типу, Press(design)=select на мок-VM, Press(fill text)=инлайн/`open_inline`, Press(checkbox)=toggle без раскрытия; у non-accessible слоёв `queryAccessibleInterface` None.

## 3. Usage-site имена в остальных островах

- [x] 3.1 `SearchBarRoot.qml`: searchInput — «Поиск по всем сущностям». Проверить: в существующем search-bar острове assert name через interface; остальные контролы не тронуты (diff только строки/поиска).
- [x] 3.2 `LlmSetupRoot.qml`: endpoint→«Endpoint», модель→«Модель», ключ→«Ключ API», описание мира→«Описание мира», репитер fieldPrompt → `modelData.label`. Проверить: accessibility-ассерты (роль+name) в наборе LLM-setup по всем четырём зонам + репитер-пример.
- [x] 3.3 `SheetEditorRoot.qml`: «Ориентация страницы», «Переименование страницы», «Вверх», «Вниз», «Удалить страницу», «Добавить страницу», X/Y/W/H → «Позиция X», «Позиция Y», «Ширина», «Высота», «Кегль», «Текст поля», «Число», «Минимум», «Максимум», «Новая опция», «Поднять опцию», «Опустить опцию», «Значение по умолчанию». Проверить: accessibility-тест редактора листов — у каждого объекта роль и name из интерфейса; текстовые кнопки (Save, Export) не переименованы.
- [x] 3.4 `SheetFillRoot.qml`/`SheetPresetRoot.qml`/`DocViewerRoot.qml`/`XlsxImportRoot.qml`: «Значение поля», «Значение поля (многострочно)», «Значение из списка»; «Имя листа», «Текст лицензии»; «Текст документа»; «Путь к файлу .xlsx», «Требования к формату файла». Проверить: точечные accessibility-ассерты по каждой зоне в соответствующих существующих тестах.
- [x] 3.5 `EventDialogRoot.qml`/`EventTypesRoot.qml`: поля/даты/тип/mention по карте («Название события», «Дата начала», «Дата конца», «Тип события», «Характеристики», «Предыстория»), AI-кнопки через `fieldLabel` («Сгенерировать: Название» и т.п., entity-AI — заполнить `fieldLabel: "Событие"`), swatch диалога — «Цвет типа события»; типы — «Название типа события», свотчи «Цвет палитры №N», стрелки «Поднять тип/Опустить тип». Проверить: `test_event_dialog_accessibility.py` + дописать asserts в `test_event_types_dialog.py`.
- [x] 3.6 `LauncherRoot.qml`, `SheetListRoot.qml`, `ImageViewerRoot.qml`, `RelatedSection.qml` остаются без правок (текстовые контролы + контракт RowItem покрывает их строки). Проверить: `git diff` пуст по этим файлам; их существующие тесты зелёные.

## 4. Guard-тесты контрактов

- [x] 4.1 Guard-тест `tests/test_qml_accessibility_conventions.py`: в `app/presentation/qml/` нет `Accessible.role: Accessible.NoRole`, нет `Accessible.ignored`, нет `Accessible.value`, и штатные текстовые контролы (`ThemeButton` текстовый, `ThemeCheckBox` с text, `ThemeTabButton` с text) не содержат собственного `Accessible.name`. Проверить: тест зелёный и режет умышленное нарушение.
- [x] 4.2 Guard-тест штатного поведения: на выборке живых островов Button/CheckBox/TabButton — `queryAccessibleInterface(...).text(QAccessible.Name)` равно видимому тексту (не пусто). Проверить: зелёный; порченый `Accessible.name=""` на таком контроле уронил бы тест.

## 5. Документация и живой контур

- [x] 5.1 `AGENTS.md`, блок про computer-use: добавить контракт дерева (роль — за компонентом; name — точка применения/содержимое строки; value — только штатные контролы; description — скрытое действие; `NoRole`/`ignored`/name на штатном Button запрещены; press кастомного Item только через `onPressAction`, pin оффскрин через `doAction`), заменить «what tree does NOT expose» на четыре зафиксированных предела (попап упоминаний, value-текст MentionField/инлайн-редактора, нативные попапы дат и выпадаюков, tooltip-мост) с указанием, что value-мост — follow-up NRI-0013. Проверить: блок цитирует карту design.md без расхождений.
- [x] 5.2 `docs/functional-checklist.md`: по строке каждому экрану «интерактивные элементы доступны из дерева accessibility без координат»; `docs/CHANGELOG.md`: запись фичи (версии не двигать — вне этого изменения). Проверить: перечислены все острова и все четыре предела.
- [x] 5.3 Полный гейт: `QT_QPA_PLATFORM=offscreen python -m pytest` зелёные, coverage гейт 100% не задет (QML-правки не Python-строки).
- [x] 5.4 Живой `accessibility-audit` (computer-use, throwaway игра `qa-<date>`, AI-кнопки не_press'ить): снять `get_app_state` по всем экранам, сверить с AGENTS-списком из 4 пределов, записать расхождения в `docs/qa/<date>-accessibility-audit.md`. Проверить: расхождений нет, либо заведены тикеты/пункты на исправление и AGENTS обновлён.

## 6. Финал

- [x] 6.1 Ревью: дифф сверен с обеими spec-дельтами (каждый requirement/scenario имеет задачу и тест), `openspec validate nri-0012-qml-accessibility --strict` проходит. Commit `NRI-0012: add accessibility tree to QML islands` — только по запросу user.
