## 1. Схема файла (реестр листов и колонок)

- [x] 1.1 Создать `app/application/services/xlsx_schema.py`: реестр пяти листов (имя листа, RU-колонки + EN-алиасы, обязательность, допустимые колонки связей по типам, special-колонки `Тип`/`Изображение`/`Рейтинг`) — один источник для парсера, подсказки и шаблона; проверить unit-тестом полноты реестра (все 13 M2M кроме `*_rating` покрыты колонками связей)
- [x] 1.2 Реализовать нормализацию заголовков (RU/EN-алиасы, регистр/пробелы не значимы) и проверку обязательных колонок; покрыть тестами (`name`/`start_date` читаются как `Имя`/`Дата начала`)
- [x] 1.3 Реализовать парсинг значений даты (нативная ячейка / текст `YYYY-MM-DD`), рейтинга (int 1..5) и `;`-списков связей (strip, пропуск пустых сегментов); покрыть тестами на edge-cases (`" а;б; ;в "`, мусорный рейтинг, `"31.12.2025"`)

## 2. План импорта: парсинг и пред-анализ

- [x] 2.1 Реализовать `analyze_file(path) -> ImportPlan` в `xlsx_import_service.py`: чтение workbook (только `.xlsx`), отображение знакомого набора листов (регистр не значим, неизвестные — warning-список), пустой файл/битый файл/нет знакомых листов/кривый знакомый лист — фатальные ошибки; проверить тестами всех фатальных и warning-веток
- [x] 2.2 Реализовать слияние строк с одинаковым (тип, lower(имя)) внутри файла: непустые поля поздней перекрывают, связи копятся; пустое имя/битая дата начала/рейтинг вне диапазона → planned-skip с причиной (лист, номер строки); проверить тестами
- [x] 2.3 Реализовать `name_index` (upsert-кандидаты и разрешение целей связей): GROUP BY lower(name) HAVING COUNT(*)>1 на тип, уникальное совпадение → план обновления (только непустые поля), неоднозначное имя строки → план создания новой, неоднозначная ссылка в колонке связей → planned-skip; проверить тестами на in-memory БД с дубликатами имён

## 3. Применение плана (два прохода)

- [x] 3.1 Реализовать проход 1: создание/обновление сущностей всех типов (включая ghost-цели связей с датами min start/max end из ссылающихся строк и автосоздание отсутствующих `event_types` со свободным color_index); каждый ghost и новый тип — запись в `report.decisions`; проверить тестами (взаимоссылки листов, несколько разных дат у ghost'а)
- [x] 3.2 Реализовать проход 2: применение всех связей напрямую через ORM-отношения (добавление с дедупликацией, без отвязывания) для всех связей кроме `*_rating`; проверить тестом сценария «повторный импорт не отвязывает существующие связи»
- [x] 3.3 Обернуть применение в явную транзакцию с единственным commit по успеху и rollback при исключении (явная зависимость сессии/commit-колбэк от wiring); E2E-проверка: импорт → явный перезапуск приложения → данные в новой сессии
- [x] 3.4 Переиспользовать конвейер `ImageStore` для колонки `Изображение` (путь относительно каталога .xlsx или абсолютный; нечитаемый файл → сущность без изображения + warning в отчёт); проверить тестами обоих исходов

## 4. Диалог и подключение

- [x] 4.1 Переработать `XlsxImportViewModel` в стейт-машину `idle → analyzing → problems → importing → done(report)` со свойствами `analysisIssues[]`, `hasFatal`, `canImport`, `report`; добавить сигналы `analyze_requested(path)`, `confirm_import()`, `download_template()`; покрыть unit-тестами VM
- [x] 4.2 Переработать QML-остров `XlsxImportDialog`/`XlsxImportRoot.qml`: список проблем пред-анализа (лист/строка/причина) с блокировкой кнопки при фатальных, прогресс, панель итогового отчёта (создано/обновлено/связей/пропущено/решения/предупреждения), блок подсказки с колонками из реестра 1.1, кнопка «Скачать шаблон», фильтр файлов только `.xlsx`; characterization-тесты диалога проходят
- [x] 4.3 В `main_window.py` заменить пять QAction импорта одним «Импорт из .xlsx…»; в `wiring.py` — единый `_run_import` без entity_type (analyze/confirm/progress на одном `_session_lock`, итог в диалог, без QMessageBox-отчёта); проверить ручным прогоном/тестом, что прежних пунктов нет

## 5. Шаблон файла

- [x] 5.1 Создать статический `resources/import_template.xlsx`: пять листов с полными RU-шапками (все колонки, включая необязательные и четыре колонки связей) и примерными строками, демонстрирующими `;`-привязки cross-sheet; проверить, что файл проходит `analyze_file` с нулём проблем и импортируется в чистую базу
- [x] 5.2 Реализовать кнопку «Скачать шаблон» (резолв пути dev/bundle как у docs + `getSaveFileName` + копирование) и добавить `resources/import_template.xlsx` в `nri_manager.spec`; проверить unit-тестом копирования и наличием ресурса в spec-бандле

## 6. Тесты и приёмка

- [x] 6.1 Обновить фикстуру `tests/fixtures/import_sample.xlsx` и `tests/application/test_xlsx_import_service.py` под новый формат (пред-анализ, upsert, связи, ghosts, транзакция) — все green в `QT_QPA_PLATFORM=offscreen python -m pytest`
- [x] 6.2 Обновить `tests/ui/test_e2e_import.py`: единый импорт файла пяти листов с привязками → события+сущности на шкале, связи видны в диалоге события, отчёт с пропусками; перезапуск приложения после импорта — данные сохранены. Все тесты `python -m pytest` green
