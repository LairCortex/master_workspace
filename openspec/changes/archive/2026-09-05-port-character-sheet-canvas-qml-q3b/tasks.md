# Tasks: port-character-sheet-canvas-qml-q3b

## 1. Python-сторона острова (до QML)

- [x] 1.1 Модель полей: в `app/presentation/viewmodels/character_sheet_viewmodel.py` — `SheetFieldModel(QAbstractListModel)` (роли id/type/page/x/y/w/h/fontSize/content/imageKey/optionsCount/disabled), сборка по `template`, инкрементальные обновления существующими сигналами VM (add/remove→insert/removeRow, geometry/content/props→dataChanged, pages/orientation/template→reset); в fill-VM — display-роль через доменный `resolve_display` (без второй реализации); юниты: все переходы ролей на обеих VM, «в QML нет копии набора» непроверяемо напрямую — проверяем отсутствие второго источника (тест на идентичность модели `template` после мутаций).
- [x] 1.2 QML-инвайкейблы поверх существующих sync-входов: подтвердить/добавить точечные `@Slot`-обёртки там, где методы VM уже есть (place/move/resize/drag/commit/select*/set_content/apply_number/inline/pages/orientation/z-order/undo/redo для design; select/open_inline/commit/cancel/set_text/set_number/set_dropdown/toggle_checkbox/set_image/clear_image для fill; scroll-канал `set_current_page`/вывод видимой страницы) — контракты существующих property/сигналов/methods не меняются; юниты инвайкейблов.
- [x] 1.3 Шрифт и image-провайдер: `register_sheet_font()` с каталогом `fonts/DejaVuSans.ttf` переезжает из `canvas.py` в модуль qml-острова, вызывается до загрузки островов (шрифтовой тест переезжает без ослабления); идемпотентный `QQuickImageProvider` `image://sheet/<imageId>` поверх ImageStore в `qml/engine.py` (unit: резолв id → байты, отсутствующий id → 0-размер без падения).

## 2. QML-канвас

- [x] 2.1 `app/presentation/qml/SheetCanvas.qml`: лента страниц (Flickable + `Repeater` страниц `page_origin`/размер из домена, выводимые VM), делегаты полей всех 9 типов с переносом/обрезкой как в текущем paint-слое, z-порядок = порядок Repeater; цвета — перенесённые 1:1 константы нетемизируемого слоя; objectName-контракты (`sheetCanvas`, корень страницы/поля по id); проверка: остров грузится на тестовом шаблоне, структура адресна через walk_items.
- [x] 2.2 Зум/прокрутка/fit-width: константы 0.25–4.0/шаг 1.15, Ctrl+колесо с якорем курсора, обычное колесо — скролл, fit по ширине первой страницы при показе и смене ориентации; `visible_page` → `vm.set_current_page`, `scrollToPage(index)`/`visibleCenter(index)` invokables; проверка: координатные тесты ленты/зума из migrated-набора зелёны.
- [x] 2.3 Жесты design: клик-становка инструмента, одиночный/Shift клик, рамка (`select_ids` additive), перенос набора (`begin_gesture/drag_move_selection/commit_drag_selection` с per-field clamp и переносом через зазор — считает VM), ручки ресайза (`resize`), dblclick-инлайн, Del/Backspace/Esc, Shift-override привязки; все цели — существующие sync-входы VM; проверка: migrated-тесты жестов зелёны.
- [x] 2.4 Инлайн-редактирование: QML-редактор внутри поля по `inline_field_id` (тот же шрифт/кегль/обрезка), live→`set_content`, Enter/Ctrl+Enter/Esc→commit/cancel, число по Enter→`apply_number` с откатом reject, единый буфер с панелью; проверка: migrated-тесты инлайнов (включая числовой reject и смену поля без потери коммита) зелёны.
- [x] 2.5 Режимы fill/readonly: клик чекбокса→`toggle_checkbox`, текст/число/многострочное→inline, картинка→файл-мост, read-only — только выбор; дропдаун — сигнал facade-мосту и нативная `QMenu` в координатах поля →`set_dropdown`; проверка: migrated-тесты fill-ввода/дропдауна/read-only зелёны (QMenu — через мок выбора фасада).

## 3. Острова окон

- [x] 3.1 `SheetEditorRoot.qml`: палитра типов (инструменты+pointer), рейка (инлайн-переименование→`rename_page`, add/remove/↑↓ с подтверждением через фасад-мост), комбо ориентации, панель свойств (ветки content/кегль/число-границы/options/чекбокс-дефолт/image-кнопки), строка «Сохранить»/«Экспорт в PDF…» как маркер `defaultButton`; chrome только из `nri.components`; проверка: семантика палитры/рейки/панели из migrated-тестов диалога зелёна.
- [x] 3.2 `SheetFillRoot.qml`: навигационная рейка + канвас(fill) + QML-панель значений (type-ветки → set_text/set_number/set_dropdown/toggle_checkbox/set_image/clear_image); привязка/отвязка персонажа — кнопки фасада (нативный `QInputDialog` остаётся); проверка: migrated-тесты fill-диалога зелёны.
- [x] 3.3 Фасады `editor_dialog.py`/`CharacterSheetEditorDialog` и `fill_dialog.py`/`CharacterSheetFillDialog`: прежние публичные API (`saved`/`binding_changed`, `view_model`, `load*`, `save`, `set_name`, `export_pdf`, `force_close`, `set_read_only`, грязный `closeEvent`), нативное меню «Правка»/`QMenuBar`, `QFileDialog` PDF/изображений, сохранения с `run_locked`+`QMessageBox`, QMenu-мост дропдауна, Enter→`defaultButton` вне инлайна; `main.py` — без правок импортов; проверка: migrated/e2e-тесты окон зелёны, диффы main.py/wiring нет.
- [x] 3.4 Удаление widgets-механики без флага: `canvas.py`, `palette.py`, `page_rail.py`, `properties_panel.py`, `FillPropertiesPanel` удалены; в проекте не осталось `QGraphicsProxyWidget`/`QGraphicsView` чар-листа — grep-инвариант; исключение proxy-полей вычеркнуто из каталога W2 и инвариантного теста; `test_no_chrome_hex` сканирует новые qml с единственным исключением `SheetCanvas.qml` (бумага); проверка: grep чист, тесты инвариантов зелёные.

## 4. Приёмка

- [x] 4.1 Миграция `test_character_sheet_canvas.py`: все проверяемые смыслы (лента/лист-геометрия/зум/жесты/рамка/инлайн/рендеры типов/кириллица шрифта) через QML-адресацию и координатные пробы; шрифтовой пиксель/кириллица-тест на острове; тесты зелёны.
- [x] 4.2 Миграция `test_character_sheet_editor_dialog.py`/`test_character_sheet_fill_dialog.py`/`test_character_sheet_types.py`: смыслы 1:1 (save-ошибки не закрывают окно, PDF-экспорт, привязка, read-only-просмотр мастера, все типы полей); e2e чар-листов на QML-пробы (образец timeline); тесты зелёны.
- [x] 4.3 Инварианты и бандл: новые qml в datas `.spec`, бандл-тестqml проходит; пиксельная приёмка chrome островов (токен = пиксель) + paper константы; полный `python -m pytest` зелёный, coverage-гейт Python-стороны не просаживается.
- [x] 4.4 Закрытие эпика Q: `docs/design-system-roadmap.md` — Q3 реализован (Q3a+Q3b), эпик Q закрыт; `docs/CHANGELOG.md` — запись закрытия эпика без пометки незавершённости.
