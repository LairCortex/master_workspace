# Tasks: Drag-редактирование дат на шкале (W5)

> Статус плана после grill-ревизии 2026-09-01 (вторая сессия). Группы 1–2 уже реализованы и протестированы в `main` (ядро + жест переноса, `TestEventMoveGesture`) — отмечены выполненными здесь, чтобы apply не переделывал готовое. Новые пункты 1.5, 2.5, группа 2b и правки 4/6 — дельта второй grill-сессии: grab-offset, clamp календаря, тяга начала бессрочных, `QMessageBox`.

## 1. Qt-нулевое ядро геометрии жестов (timeline_rows.py)

- [x] 1.1 Написать падающие юниты на `target_day(rows, row_height, y, scroll)` — попадание внутрь блока как у `index_at_y`, экстраполяция за голову/хвост с шагом `EXTRAPOLATION_STEP_PX` (= `ROW_HEIGHT`), пересечения границы месяца/декабрь→январь/29 февраля — и убедиться, что они падают
- [x] 1.2 Реализовать `target_day` + `EXTRAPOLATION_STEP_PX` в `timeline_rows.py`; юниты 1.1 зелёные
- [x] 1.3 Написать падающие юниты на `translate_span(start, end, delta_days)` — сдвиг закрытого диапазона с сохранением длительности, результат никогда не инвертируется (на этом слое clamp не нужен, только сдвиг); реализовать — юниты зелёные
- [x] 1.4 Написать падающие юниты на хелпер hit-зоны засечки (по набору `_serif_bottom` сегментов закрытых многодневок: центр x = `BRACKET_X0 + lane*BRACKET_LANE_STEP`, hit при `|x - center| ≤ SERIF_HIT_PX`; однодневные и открытые не кандидаты) и реализовать его в ядре/виде согласно D8; юниты зелёные
- [x] 1.5 Падающие юниты на clamp цели к календарю (`CALENDAR_MIN = 0100-01-01`, `CALENDAR_MAX = 9999-12-31` — границы `CustomDateEdit`): `target_day` не возвращает дату за границами ни внутри блока, ни экстраполяцией; реализовать

## 2. Жест переноса в TimelineListView

- [x] 2.1 Вооружение `_EventPress` (закрытое событие, суточная ступень, текстовая зона, ЛКМ) и переход в перенос past `DRAG_START_THRESHOLD_PX`; клик без порога выбирает как раньше; e2e: press+release без смещения эмтит обычный `event_selected`, press+drag по строке продолжения бессрочного события не эмтит ничего
- [x] 2.2 Пересчёт превью-состояния `_edit_preview` на каждое move через `target_day` и `translate_span`; e2e: после двух move-событий `view.edit_preview()` содержит целевые даты, данные и модель строк не тронуты (пересмотреть формулу в 2.5)
- [x] 2.3 Отпускание: снять состояние до эмита (паттерн W3c-D6), ровно один `event_dates_moved(event_id, new_start, new_end)`; отпускание вне области списка = commit по экстраполированной цели; e2e-тесты ровно одного эмита и release-за-краем
- [x] 2.4 `Esc` отменяет активный перенос (без эмитов), сброс в `_rebuild()` наравне с рейковыми состояниями (внешняя пересборка убивает жест); e2e: Esc → ни сигналов, ни preview; применение фильтра извне во время drag'а → жест снят
- [x] 2.5 Исправить реализацию под design (grill Q1): перенос = grab-offset — `delta = target_day − день_строки_захвата` (`Row.date` строки `anchor_index`), а не `target − event.start`; превью в момент превышения порога совпадает с исходным диапазоном (нет «телепортации» начала к курсору); править юниты/e2e: захват за середину тела многодневки даёт сдвиг относительно строки захвата (событие 3–10 марта, grab строка 5-го, release против 12-го → 10–17 марта)

## 2b. Тяга начала бессрочного события (view)

- [x] 2b.1 Вооружение `_EventPress(mode=START)` только со start-строки открытого события (строка, чей день = `start_date`), суточная ступень, текстовая зона; строки продолжения/хвост бессрочного не вооружаются (click/dblclick работают); past-порог move: `new_start = target_day` напрямую (без grab-offset), `end=None` в preview; e2e: арминг только на start-строке, preview с `end=None`, тело не тронуто
- [x] 2b.2 Отпускание: ровно один `event_dates_moved(event_id, new_start, None)`; Esc/rebuild отменяют как перенос; календарный clamp 1.5 действует; e2e ровно одного эмита и отмен

## 3. Жест растягивания окончания

- [x] 3.1 Press в hit-зоне нижней засечки закрытого многодневного события вооружает растягивание ДО рейковой ветки; past-порог move считает целевой `end` через `target_day` с clamp `end ≥ start` и календарю (1.5); e2e: тяга на +2 суток даёт preview с новым `end`, тяга выше начала отдаёт `end == start`, press у засечки открытого события уходит в рейку (прыжок), промах мимо hit-зоны — обычный рейковый жест
- [x] 3.2 Отпускание эмитит ровно один `event_dates_moved` с новым `end` (старый `start`); однодневное событие не вооружает растягивание (e2e-немота: удлинение однодневки — долг через диалог); все три ветки зелёные

## 4. Превью-отрисовка в `_RowDelegate`

- [x] 4.1 Призрак на целевой позиции: wash-заливка `token_rgb(color.accent, DRAG_WASH_ALPHA=0.35)` + приглушённый текст `_row_line` (новых токенов нет); если цели нет в модели — призрак на последнем видимом дне, липкая дата показывает истинный целевой день через существующий follow-механизм; тот же призрак работает для `START` (целевая start-строка) и `RESIZE_END`; e2e + grab: пиксель призрака = производная `color.accent` (обе темы), оф-скин — Qt-глобалы; тест hit-зоны засечки пикселем (x = `BRACKET_X0 + lane*BRACKET_LANE_STEP`)
- [x] 4.2 Live-скобка растягивания до целевого дня + приглушённый вид исходной строки (не hover-wash — fixed по grill Q2) во время жеста; смена темы между press и release перекрашивает превью без потери жеста (e2e: ре-тема во время drag'а, пиксель по новой теме)
- [x] 4.3 Прогнать `tests/presentation/test_no_chrome_hex.py` по обновлённому `timeline_widget.py` — ни hex, ни `palette()`

## 5. Подавление побочных жестов на время активного drag'а

- [x] 5.1 В `wheelEvent` во время edit-drag: Ctrl/Cmd-ветка погашена, обычная прокрутка работает и целевой день пересчитывается (e2e: wheel внутри drag'a, release попадает под курсор); переключатели ступени/группировки в шапке игнорируются пока drag активен (e2e: клик по «месяц» во время drag'а не меняет `vm.unit`)
- [x] 5.2 Двойной клик/обычные клики текстовой зоны во время активного жеста не порождают selection/dblclick-сигналов (e2e-немота)

## 6. Commit: панель и wiring

- [x] 6.1 Проброс `event_dates_moved` из списка в `TimelineWidget` (единственный сигнал панели); тест: connect/listener видит id и обе даты (бессрочное — `end=None`)
- [x] 6.2 Wiring-канал под `_spawn`/session-lock: при активном фильтре и выходе новых границ за чип — сначала существующий путь `_on_filter_range` (чип + `filter_changed`), затем `event_service.update_event(eid, start_date, end_date)` + commit, `load_events`, `update_events`, показ деталей через существующий путь (test: расширение фильтра и запись в одном завершении, событие остаётся видимым; бессрочное — запись только `start_date`)
- [x] 6.3 Невыбранное событие выбирается на отпускании (`select_event_by_id` ровно один раз, панель деталей обновлена); выбранное остаётся выбранным (e2e)
- [x] 6.4 Сбой сохранения — `rollback()` + перезагрузка шкалы из БД (строка возвращается на прежние даты) + ровно один модальный `QMessageBox` об ошибке; расширение фильтра до записи не сворачивать (wiring-тест с бросающим сервисом: строка на прежних датах, диалог показан); молчаливость диалог-пути не трогать (долг в CHANGELOG по 7.3)

## 7. Регресс и приёмка

- [x] 7.1 Полный прогон `QT_QPA_PLATFORM=offscreen python -m pytest` зелёный; специфично: все прежние timeline-e2e (W3b/W3c/W4) проходят без правок семантики
- [x] 7.2 Ручная проверка на живом приложении (macOS): перенос через границу месяца (grab-offset без прыжка при захвате), колесо параллельно drag'у, Esc, унос за фильтр (чип расширился), тяга засечки, тяга начала бессрочного, тело бессрочного не двигается; отсутствие нативного Qt text-drag при быстром drag'е
- [x] 7.3 Пометка в `docs/CHANGELOG.md` (W5 влито, эпик W закрыт; долг: молчаливый сбой сохранения в диалог-пути) и строка W5 в `docs/design-system-roadmap.md` (статус + вне-W5 границы: закрытие открытого, однодневное→многодневное, global undo); `openspec validate` change проходит
