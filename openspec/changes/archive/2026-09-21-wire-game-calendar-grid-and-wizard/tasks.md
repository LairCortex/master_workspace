# Tasks: wire-game-calendar-grid-and-wizard

## 1. Сетка игрового календаря

- [x] 1.1 Создать `app/presentation/views/calendar_grid.py`: `GameCalendarGrid` (панель навигации ◀/▶ + комбо месяцев активного календаря + спин года 1…9999, шапка имён недели, сетка `QGridLayout` кнопок-ячеек `GameCalendarCell` с атрибутом `selected`) с API по design D1/D2 и тестами `tests/presentation/test_calendar_grid.py` (пресет 12×7 Monday-first; кастом 13×28 с десятидневной неделей; пустые клетки неполных недель некликабельны; нет «сегодня» и клеток чужих месяцев)
- [x] 1.2 Реализовать строки-плашки вставных дней (`GameCalendarIntercalaryChip`) после месяца-хозяина в порядке списка правил и тест выбора координаты `IntercalaryDay` (включая два правила одного хозяина)
- [x] 1.3 Реализовать координатный контракт сетки: сигнал `day_selected(GameCoord)`, `set_selection(coord|None, is_bc|None)` (вставной день — подсветка плашки, год вне 1…9999 — непредзаполненное состояние без падения), чистый флаг эры `is_bc()/set_era()` (переключение не меняет координату и страницу), режимы `interactive=False`/`show_era=False`; тесты каждого сценария
- [x] 1.4 В `date_utils` добавить пресетную константу русских имён недели `STANDARD_WEEK_NAMES`; тест: сетка пресета подписывает понедельник-первые столбцы, кастомная — своими `week_names`

## 2. Координатные попапы поверх сетки

- [x] 2.1 Перевести `ThemeDatePopup` с `_CustomCalendar`/`QCalendarWidget` на `GameCalendarGrid`: кликанье отдаёт `(GameCoord, is_bc)`, предзаполнение принимает координату/пару через `as_game_coord`/`split_date_era`, `refresh()` сетки при каждом `open_at`, зажатие в `availableGeometry` сохранено; обновить `tests/presentation/test_theme_date_popup.py` на координатный API
- [x] 2.2 Перевести `_DateWindowPopup` на две `GameCalendarGrid`: двухтаповая механика (заведение начала, обратный тап = переначало, живое применение, сброс, независимые эры границ, откат до одной сетки на низком экране) сохранена слово-в-слово с носителем `GameCoord`; обновить `test_timeline_island`/`test_coord_presentation` (assert(isinstance(..., QCalendarWidget)) удалить)
- [x] 2.3 Удалить `popup_prefill_date` и `_display_day_of_intercalary` из `date_utils` и все их использования; проверить grep-ом отсутствие ссылок; прогнать `python -m pytest tests -k "popup or date_utils or coord"` зелёным
- [x] 2.4 Удалить legacy-прокси `date()/setDate(QDate)` из `event_dialog.py`/`entity_card_dialog.py`, перевести их тесты и `tests/ui/helpers.py` (эмуляция `clicked.emit(QDate)` островов) на координатный API; полный `python -m pytest` зелёный

## 3. Тема сетки

- [x] 3.1 В `theme/compiler.py` удалить правила `QCalendarWidget` из листа попапов и добавить правила `GameCalendarGrid`/`GameCalendarCell` (включая `[selected="true"]` и hover)/`GameCalendarIntercalaryChip`/`GameCalendarDayName` из токенов; обновить `test_theme_compile.py` (четыре имени присутствуют, `QCalendarWidget` отсутствует)

## 4. Сервис: черновик, флаг, поднятие, посев

- [x] 4.1 Добавить в `game_calendar` доменный кодек черновика (версия + спека + индекс ступени) поверх `encode_calendar`/`decode_calendar` с тестами кругового прохода и «битой/невалидный черновик = нет черновика»
- [x] 4.2 Расширить `CalendarSettingsService`: `load_draft`/`save_draft`/`discard_draft` (ключ `game_calendar_draft`, повреждённый читается как отсутствие с логом) и константы ключей; тесты на in-memory БД
- [x] 4.3 Реализовать `promote_draft` (одна транзакция: применение C2 + запись `game_calendar` поверх, удаление черновика, опционально флаг `calendar_wizard_seen="1"`; после commit — активация календаря), ошибочный путь откатывает всё; тесты: поднятие чистит черновик, откат при ошибке, перезапись повреждённой строки настройки
- [x] 4.4 Посев в `init_db` при факте свежего создания схемы: `game_calendar` (standard) + `calendar_wizard_seen="0"` по образцу `_seed_default_event_types`; тесты: новая игра посеяна, старая (миграционный путь) посева не получила, повторный `init_db` идемпотентен
- [x] 4.5 Read-доступ флага и черновика для Presentation: константы и методы загрузки в сервисе покрыты, `load_and_apply` поведение не изменил (регрессия существующих тестов C2)

## 5. Вьюмодель мастера

- [x] 5.1 Создать `viewmodels/calendar_wizard_viewmodel.py`: frozen-State (шаг, частичная форма недели/месяцев/вставных, `can_advance`, `problems`, отчёт, строки отчёта) и интенты флоу; валидация каждой ступени через `validate()` на частичной спеке (непройденные ступени — дефолты), русские фразы через `calendar_warnings`; тесты: невалидная ступень блокирует, висячий вставной день блокирует «Далее», пустой предпросмотр держит последний валидный
- [x] 5.2 Реализовать в вьюмодели черновик-семантику: сохранение пройденной ступени (спека с дефолтами + индекс), продолжение с сохранённой ступени с предполнением, отмена не меняется; тесты через in-memory сессию и `discard_draft`
- [x] 5.3 Реализовать интенты применения: `apply` → `apply_to_records(dry_run=True)` → пустой отчёт применяет сразу / непустой открывает экран; «Перенести и применить» → `promote_draft`; «Отменить» → ничего; подписи строк отчёта человекочитаемые (название, иначе «таблица/событие №<id>», поле «начало/конец», даты `format_game_date`); ошибка применения — окно с причиной, состояние остаётся; тесты всех веток

## 6. Диалог мастера

- [x] 6.1 Собрать `views/calendar_wizard.py`: модалка со стеком экранов «выбор → неделя → месяцы → вставные дни → предпросмотр», кнопки «Назад/Далее/Применить/Отменить», панель живого предпросмотра `GameCalendarGrid(interactive=False, show_era=False)` с датой «текущая игровая дата, иначе год 1»; viewmodel-ориентирован (view только показывает State и шлёт интенты); widget-тесты навигации ступеней
- [x] 6.2 Экраны «Неделя» (спин 2…168, поля имён = длине, добавление пустых/отброс хвостов, предполнение 7 текущими именами при первой сборке) и «Месяцы» (предполнение 12 григорианских, «Месяц N»/30, отброс хвостов, строки «имя | длина» со скроллом); тесты каждого сценария spec `calendar-wizard`
- [x] 6.3 Экран «Вставные дни»: список правил с «удалить/вверх/вниз», добавление с предвыбором последнего месяца, порядок как позиции; экраны «Предпросмотр» (сводка + сетка + «Применить») и «Отчёт» (список переносов, «Перенести и применить»/«Отменить»); тесты: порядок правил, отчёт с именами записей, пустой отчёт пропускает экран
- [x] 6.4 Кожа мастера из токенов по каталогу виджетов (кнопки/поля/подписи из общих компонентов, style-facing имена классов комментируются); тест отключения темы не ломает флоу

## 7. Точки входа и распространение

- [x] 7.1 Пункт «Настройки → Календарь…» в `main_window.py` + wiring в `Application._wire_*`: конструирует мастер на текущей сессии с предвыбором текущего ключа; флага не касается; widget-тест входа из меню в обеих играх (пресет/кастом)
- [x] 7.2 Первый вход новой игры в `Application.start()` после обхода и до `MainWindow.show()`: при `calendar_wizard_seen=="0"` — `exec()` мастера с предвыбором «Стандартный»; крест без черновика = применить пресет + флаг `"1"` и выход в окно; крест с черновиком = флаг остаётся `"0"`; тест первого входа через `qtbot`-прецедент лаунчера, включая «брошенный черновик зовёт обратно»
- [x] 7.3 Цепочка распространения после применения (design D11): сигнал вьюмодели → `timeline_vm.load_events()` + пересборка table host'а и детальной панели; тест: после применения кастомного календаря лента и чип показывают новые имена без перезапуска; повторный вход попапа показывает новый календарь

## 8. Зачистка

- [x] 8.1 Обновить `docs/custom-calendar-roadmap.md` (строки C3 и C4 закрыты этим изменением, пометка C3b/C4), записать миграцию поведения в `docs/CHANGELOG.md` (посев новых игр, новые ключи без схемы, удаление QCalendarWidget-контракта попапов)
- [x] 8.2 Проверить итог: `python -m pytest` зелёный, гейт 100%-покрытия (`--cov-fail-under`) выполнен, grep не находит `QCalendarWidget` в `app/`, `popup_prefill_date`, оставшихся `date()/setDate`-прокси диалогов; `openspec validate --strict` проходит на изменении
