## 1. UI-тестовая система (PR1)

- [x] 1.1 TDD DI-шва: написать падающий тест E2E-заготовки — `Application(qapp, http=AppHttpClient(client=httpx.AsyncClient(transport=mock_handler)))` + один LLM-запрос с canned-ответом (красный старт: `TypeError`); затем добавить опциональный параметр `http` в `Application.__init__` и использовать его в `start()` (иначе default-клиент); проверка: новый тест зелёный, полный `python -m pytest` зелёный
- [x] 1.2 Боот-фикстуры в `tests/ui/conftest.py`: `tmp_games_dir` (monkeypatch `get_games_dir` → подкаталог `tmp_path`), `tmp_llm_config` (monkeypatch константы `CONFIG_FILE` → `tmp_path`), `llm_client` (`AppHttpClient` с `MockTransport`-хендлером `POST */chat/completions`), `app` (полный старт: `Application(qapp, http=llm_client)` + `await start(tmp .db)`, teardown `window.close()` + `await shutdown()`), stub'и `QFileDialog.getSaveFileName`/`getOpenFileName`, хелпер async-ожидания на `qtbot.waitUntil`; проверка: тест-заготовка из 1.1 и существующий smoke-тест проходят на новых фикстурах
- [x] 1.3 Переезд `tests/test_application_start.py` → `tests/ui/test_application_boot.py` (без изменения поведения тестов); проверка: `python -m pytest` зелёный, число тестов не уменьшилось
- [x] 1.4 E2E-сценарии (10 шт.) в `tests/ui/`: (1) лаунчер — создание новой игры (заголовок окна, пустой таймлайн); (2) лаунчер — открытие существующей игры с подготовленными данными; (3) CRUD ×4 типа сущностей (параметризация: character/organization/location/item) через контекстное меню таймлайна → карточка → удаление; (4) создание события + редактирование в detail panel; (5) case-insensitive поиск; (6) LLM-визард: сохранение подключения + «Проверить соединение» (canned 200) + AI-генерация поля; (7) импорт .xlsx (новый fixture-файл `tests/fixtures/import_sample.xlsx`); (8) кастомные месяцы → отображение в таймлайне; (9) экспорт `.nri` (assert на архив с БД игры); (10) переключение игры; проверка: `QT_QPA_PLATFORM=offscreen python -m pytest tests/ui` зелёный
- [x] 1.5 Widget-тесты по основным дырам (расширение `tests/presentation/*` + новые файлы): `timeline_widget` (отрисовка, клик, контекстное меню), `event_dialog` (валидация, связанные сущности), `detail_panel` (отображение/редактирование), `entity_card_dialog` (валидация, режим редактирования, music_url), `custom_date_edit` (специальные даты), `mention_text_edit` (вставка, клик по mention), `world_snapshot_widget` («Показать всё»), `ai_assist_button` (статусы), `app_state`; проверка: `--cov-report=term-missing` не показывает этих файлов с missing-строками ниже 95%
- [x] 1.6 Coverage-конфигурация в `pyproject.toml`: `[tool.coverage.report]` с `fail_under = 95`, `show_missing = true`, `exclude_lines = ["if __name__ == .__main__.:"]`; CI (`.github/workflows/build.yml`): два прогона гейт/репорт (строки + ветки с `--cov-fail-under=0`), триггеры без изменений; проверка: локальные прогоны зелёные, гейт падает на intentionally непокрытой строке (проверить и убрать строчку)
- [x] 1.7 Гейт PR1: полный прогон `python -m pytest` зелёный + `QT_QPA_PLATFORM=offscreen python -m pytest` зелёный, `ruff check app/ tests/` чисто, строковое покрытие ≥95%, время прогона <~2 мин; закоммитить `NRI-0005: add two-layer UI test system with E2E boot fixture`

## 2. 100% покрытие и релиз (PR2)

- [x] 2.1 Догнать оставшиеся пропуски по списку `--cov-report=term-missing` поочерёдно по файлам: `application/wiring.py` + `main.py` (остатки signal-веток), `llm_viewmodel`/`llm_service` (except/edge-ветки), `xlsx_import_service` (отчётные edge-строки), `entity_service`/`domain/entities/rating`, `game_manager` (frozen-ветки через monkeypatch `sys.frozen` + `sys.executable`), `migrations` (ad-hoc rebuild-ветки), остаточные partial-ветки views; при встрече недостижимой строки — `# pragma: no cover` с inline-обоснованием (кап: 2 на весь `app/`); проверка: `python -m pytest --cov=app --cov-report=term-missing` не содержит номеров строк в Missing
- [x] 2.2 Поднять `fail_under` до `100` в `pyproject.toml` (гейт CI подхватит автоматически); проверка: полный прогон зелёный на 100% строк
- [x] 2.3 Версия 0.13.0 → **0.13.1** синхронно в трёх местах: `pyproject.toml`, `CFBundleShortVersionString` в `nri_manager.spec`, `docs/CHANGELOG.md` (секция «Инфраструктура/Тесты»: UI-тест-система, гейт 100%, двухпрогонная coverage-механика); проверка: `grep -rn "0.13" pyproject.toml nri_manager.spec docs/CHANGELOG.md` согласован
- [x] 2.4 Гейт PR2: `python -m pytest` — 100% по строкам, полный offscreen-прогон зелёный, `ruff check app/ tests/` чисто, `grep -rc "pragma: no cover" app/ | awk -F: '{s+=$2} END {print s}'` ≤ 2 (каждая с комментарием); закоммитить `NRI-0005: raise line coverage gate to 100 percent and release 0.13.1`

## 3. Финальная проверка

- [x] 3.1 Полный прогон в CI-режиме: `QT_QPA_PLATFORM=offscreen python -m pytest` (оба coverage-прогона) — зелёный, строки = 100%
- [x] 3.2 Ручной смоук: `python -m app.main` — лаунчер, создание игры, CRUD, поиск, диалог LLM (offline: статус not_configured/ready без сети), экспорт
- [x] 3.3 `openspec validate --change ui-tests-full-coverage` — валидно; после мёрджа PR2 — архивирование изменения (`/opsx-archive`)
