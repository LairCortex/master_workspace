## 1. Зависимости и spike ReportLab

- [x] 1.1 Добавить `reportlab` в runtime-зависимости и `pypdf` в dev-зависимости `pyproject.toml`, установить в venv; проверить: `python -c "import reportlab, pypdf"` проходит, версии выводятся
- [x] 1.2 Spike ReportLab AcroForm: прототип-скрипт генерирует PDF с textfield (короткий/многострочный/числовой с Num-флагом), checkbox с начальной отметкой, dropdown с опциями; проверить pypdf-парсингом FT/V/имена/Ff; зафиксировать в design.md (D4), нужны ли pypdf-постпроцессинг и/или runtime-зависимость, и удалить прототип

## 2. Доменная модель (app/domain)

- [x] 2.1 Создать `FieldType` (9 типов), `SheetOrientation`, dataclass'ы `SheetField`/`SheetPage`/`SheetTemplate` (геометрия float-pt, type-specific свойства); unit-тесты на конструирование/сериализацию в dict и обратно
- [x] 2.2 Реализовать чистую функцию масштабирования шаблона под смену ориентации (x,w — coefficient ширины; y,h — высоты; округление до 2 знаков; label/id/значения не меняются) и unit-тесты: A4 landscape→portrait→landscape сохраняет исходную геометрию, id и содержимое полей

## 3. Хранение (инфраструктура + сервис)

- [x] 3.1 Добавить `CharacterSheetModel` (таблица `character_sheets`: id, name UNIQUE, orientation, pages JSON, created_at, updated_at) в `models.py`; тесты: create_all на свежей БД создаёт таблицу, в существующей «старой» БД таблица появляется при `init_db()`, старые данные не повреждены
- [x] 3.2 Реализовать `CharacterSheetRepository` (get_all/get_by_id/create/update/delete, name-уникальность) по образцу базовых репозиториев; async-тесты на in-memory fixtures
- [x] 3.3 Реализовать `CharacterSheetService` (create с проверкой имени, update, delete, генерация стабильного id полей uuid4 при создании); async-тесты: конфликт имени отклонён, сохранение/чтение round-trip, id стабилен между сохранениями
- [x] 3.4 Реализовать JSON-экспорт/импорт проекта (метка `"nri-charsheet"`, version 1, валидация структуры/типов/геометрии с сообщением о причине, суффикс « (копия)» при конфликте имён); тесты: round-trip сохраняет все свойства и id, битый/чужой JSON отклонён с причиной, конфликт → суффикс

## 4. Генерация PDF (app/infrastructure/pdf)

- [x] 4.1 Реализовать `generate_sheet_pdf`: страницы A4 (595.28×841.89 pt) с `setPageSize` по ориентации, переворот координат (y_pdf = h − y − h), статический рендер (заголовок Bold, статический текст, подпись над рамкой, рамка поля, портрет = рамка + «Портрет»); тесты pypdf: число страниц, MediaBox, наличие текста страницы
- [x] 4.2 Реализовать AcroForm-поля (textfield 1-стр/многостр/число/дата с placeholder-подсказкой, checkbox с начальной отметкой, dropdown с опциями; `value`=значение по умолчанию; имя=стабильный id; Num/Multiline-флаги по итогам tasks 1.2); тесты pypdf по каждому типу поля: FT, V, имя, опции, флаги
- [x] 4.3 Реализовать метаданные (title = имя шаблона, имя файла = имя шаблона) и безопасную обрезку контента за границей страницы (export не падает); тесты: title совпадает, шаблон с элементом за краем экспортируется успешно

## 5. Viewmodel редактора

- [x] 5.1 Реализовать `CharacterSheetViewModel`: модель в памяти, dirty-флаг, undo/redo снапшотами (лимит 100, redo сбрасывается новой операцией) и операции полей (add в заданную точку, move, resize, remove, duplicate, copy/paste буфером, на перед/назад); unit-тесты (qapp/pytest-qt) на каждую операцию и на undo/redo-циклы
- [x] 5.2 Операции страниц в viewmodel (добавление, переименование, удаление, выше/ниже) и смена ориентации через функцию из tasks 2.2; unit-тесты: порядок страниц, удаление пустой и с полями, масштабирование применилось ко всем страницам
- [x] 5.3 Snap-к сетке в операциях viewmodel (позиция/размер округляются к шагу при включённом snap, шаг настраиваемый, по умолчанию 20); unit-тесты: snap-вкл/выкл, нестандартный шаг
- [x] 5.4 `save()` и `export_pdf()` корутины viewmodel (вызов сервиса/генератора, смена dirty после сохранения); тесты: сохранение фиксирует модель в БД, export создаёт файл, dirty сбрасывается

## 6. UI редактора (app/presentation)

- [x] 6.1 Канвас: QGraphicsScene-колонка страниц с отступом, PageItem (белый A4, рамка, полупрозрачная сетка при включённом «Сетка»), вид со скроллом, Ctrl+колесо — зум к курсору (25–400%), панорамирование (середина/Space), «вписать в окно»; widget-тесты: зум меняет scale вида, сетка рисуется только при включённом флагe
- [x] 6.2 FieldItem: рамка/статический текст, выбор rubber-band, 4 угловые ручки ресайза (мин. размер), подпись над рамкой, z-value по порядку массива; widget-тесты: drag меняет x/y модели, ручка меняет w/h, Del удаляет, Ctrl+D дублирует, «на перед/назад» меняет z
- [x] 6.3 Палитра из 9 кнопок типов (клик → поле в центре ближайшей к центру вида страницы, выбрано) и панель свойств (label, default value, font size + секция min/max / список опций / начальное состояние чекбокса) с двусторонней синхронизацией с моделью; widget-тесты: добавление каждого типа, изменение свойств отражается в модели и UI
- [x] 6.4 Тулбар редактора (Сохранить/Ctrl+S, Экспорт PDF, Undo/Redo с enabled-состоянием, чекбокс «Сетка» + spin шага, combo ориентации, «+ Страница», «Страницы…», зум −/+/вписать) и key-обработчики (Del, Ctrl+D, Ctrl+Z, Ctrl+Shift+Z, Ctrl+S); widget-тесты: каждый элемент вызывает ожидаемую операцию viewmodel, enabled-state undo/redo корректен
- [x] 6.5 Диалог «Страницы…»: список, inline-переименование, удаление с подтверждением при наличии полей, переместить выше/ниже; widget-тесты: переименование меняет страницу, удаление пустой сразу, с полями — только после подтверждения, порядок меняется
- [x] 6.6 Сборка окна редактора (QDialog: палитра | канвас | свойства, тулбар сверху) и guard закрытия при нечистых изменениях (подтверждение сохранить/отменить); widget-тест: закрытие с dirty показывает запрос, без dirty — закрывается сразу

## 7. Интеграция в приложение

- [x] 7.1 Диалог списка чар-листов (создать с вводом имени/конфликт, открыть, удалить с подтверждением, JSON-экспорт/импорт проекта); E2E-тесты (tests/ui): создание → появление в списке, удаление, импорт JSON с суффиксом
- [x] 7.2 Меню «Чар-листы» в MainWindow (action + сигнал), wiring в `Application` (repo → service → диалог/редактор), закрытие редактора при смене игры; E2E: меню → список → создание → открытие редактора → сохранение → шаблон в списке
- [x] 7.3 Проверить, что шаблоны включаются в экспорт `.nri` и восстанавливаются при импорте; интеграционный тест: игра с шаблоном → экспорт → импорт → шаблон со всеми полями/геометрией/ид присутствовал

## 8. Финальная проверка и релизные артефакты

- [x] 8.1 Прогнать полный `python -m pytest` (offscreen) с coverage; проверить, что linear coverage остаётся 100% (pyproject fail_under)
- [x] 8.2 Bump версии 0.16.0 → 0.17.0 в `pyproject.toml`, `CFBundleShortVersionString` в `nri_manager.spec` и запись в `docs/CHANGELOG.md`; добавить `reportlab` в hiddenimports spec (+ субмодули по итогам 8.4); проверить `ruff check .` без новых замечаний
- [x] 8.3 Локальная `python build_app.py --clean` доходит до конца, из собранного бандла приложение стартует и экспорт PDF из редактора работает (сценарий проверен: GUI-бандл стартует без ошибок; PDF-экспорт выполнен в frozen-окружении бандла за driver-скриптом с теми же зависимостями — all-9-типов шаблон → 2 страницы, 6 AcroForm-полей, кириллица, метаданные; GUI-клик-сквозняк без screen-recording-прав недоступен, сценарий закрыт offscreen E2E-тестами + frozen-проверкой)
