## ADDED Requirements

### Requirement: Accessor активного игрового календаря

Домен SHALL хранить ровно один активный объект игрового календаря на процесс, по умолчанию — встроенный пресет «Стандартный», и SHALL предоставлять явные установку и сброс активного календаря. Чтение, установка и сброс SHALL обращаться к одному общему значению; при отсутствии явной установки активным SHALL оставаться пресет «Стандартный». Accessor SHALL не хранить состояния на отдельную координату. Подключение accessor к запуску и переключению игры выполняется в более позднем куске.

#### Scenario: Дефолт — Стандартный

- **WHEN** активный календарь не устанавливался явно
- **THEN** accessor возвращает пресет «Стандартный», и ключ координаты побитово равен действующей функции `era_key`

#### Scenario: Явная смена и сброс

- **WHEN** подставлен кастомный календарь, затем активный календарь сброшен
- **THEN** между этими действиями accessor отдаёт кастомный календарь, а после сброса — снова пресет «Стандартный»

### Requirement: Хронологический ключ вычисляется через активный календарь

Приложение SHALL вычислять единый хронологический ключ через активный календарь: `era_key(d, is_bc)` SHALL сохранять сигнатуру `(дата, эра)` и быть тонким диспетчером на метод активного календаря, преобразуя календарную дату в координату обычного дня. Реализация пресета «Стандартный» SHALL считать ключ приватной формулой модуля эпох, а не вызовом `era_key` — во избежание взаимной рекурсии диспетчера и его дефолтного календаря.

#### Scenario: Стандартные числа ключа сохранены

- **WHEN** активен пресет «Стандартный»
- **THEN** `era_key` для произвольной выборки дат обеих эр, включая 29 февраля високосного и невисокосного годов и границы 1 и 9999 обеих эр, возвращает числа, побитово равные ключу до этой диспетчеризации

#### Scenario: Порядок следует за активным календарём

- **WHEN** подставлен кастомный календарь с иной длиной месяцев либо вставными днями
- **THEN** `era_key` возвращает ключи активного календаря, а порядок «ключ ↔ дата» совпадает с обходом кастомного года

#### Scenario: Смежные помощники сохранены

- **WHEN** вызваны `cmp_era_dates` и `assert_range`
- **THEN** поведение сохранено: сравнение опирается на новый диспетчер `era_key`, проверка диапазона лет — прежняя

### Requirement: Чистая политика сдвига невалидных координат

Домен SHALL предоставлять независимо от хранилища предикат существования координаты в данном календаре с кодом причины отсутствия и преобразование невалидной координаты в валидную. Причины отсутствия SHALL различаться: день вне длины месяца; месяц вне числа месяцев; номер вставного дня вне списка правил. Преобразование SHALL для переполнения дня давать последний день того же месяца/года/эры; для месяца вне числа месяцев — последний день последнего существующего месяца того же года/эры; для номера вставного дня — крайнее существующее правило списка в порядке спецификации. Преобразование SHALL быть идемпотентным и SHALL не менять эру. Столкновение двух координат на одной дате SHALL допускаться без разъезда.

#### Scenario: Сдвиг переполненного дня

- **WHEN** календарь содержит месяц из 28 дней, а координата указывает 31-е число этого месяца
- **THEN** предикат отвечает «невалидно» с причиной «день вне длины месяца», преобразование даёт 28-е число того же месяца того же года и эры, и ключ сдвинутой координаты определён

#### Scenario: Идемпотентность сдвига

- **WHEN** сдвиг повторно применён к координате, ставшей валидной
- **THEN** координата не меняется, а результат повторного сдвига пуст

#### Scenario: Эра не влияет на сдвиг

- **WHEN** одна и та же координата сдвинута в эрах н.э. и до н.э.
- **THEN** календарная часть сдвинутой координаты одинакова, эра сохранена

### Requirement: Отчёт о переносе невалидных координат

Домен SHALL предоставлять структуру отчёта о сдвиге для будущих экранов мастера: таблица, идентификатор строки, поле (`start`/`end`), старая координата с эрой, новая координата с эрой и код причины. Отчёт SHALL быть пуст, если невалидных координат нет, и SHALL не содержать локализованных подписей и имён сущностей.

#### Scenario: Пустой отчёт на валидных данных

- **WHEN** все проверяемые координаты валидны в активном календаре
- **THEN** отчёт содержит ноль записей и нулевой счётчик переносов

#### Scenario: Форма записи отчёта

- **WHEN** координата начала записи невалидна и сдвинута
- **THEN** отчёт содержит запись с полем `start`, обеими координатами с эрами и кодом причины, без имени сущности
