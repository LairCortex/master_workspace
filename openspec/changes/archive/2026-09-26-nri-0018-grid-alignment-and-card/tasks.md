# Tasks: nri-0018-grid-alignment-and-card

## 1. Квадратная кнопка библиотеки (qml-components Δ, ui-layout-grid Δ)

- [x] 1.1 Красные пины: `ThemeIconButton` — фиксированный квадрат 32×32, роль «кнопка»+имя+Press; ✕ шапки, ✎ музыки, ✨ полей и полной генерации (включая состояние «…»), «+» шкалы, ↑↓−+ рейки редактора/типов — все квадрат 32×32 без размеров в местах применения (`tests/presentation/test_theme_icon_button*.py`, обновление pixel-приёмки `test_theme_grab`)
- [x] 1.2 `ThemeIconButton.qml` + экспорт в `qmldir`; `ThemeAiButton` принимает фиксированный квадрат, оформление/состояния/`aiState` прежние
- [x] 1.3 Пересадить потребителей: `ThemeSheetHeader`✕, `EntityCardRoot`✎, `TimelineRoot`+ (вместо локального `squareSide:30`), `SheetEditorRoot`/`SheetFillRoot`/`EventTypesRoot` глифы; grep-пин: сторонних констант размеров глифов в островах нет
- [x] 1.4 Зелёный прогон группы, покрытие новых строк 100 %

## 2. Оптическое центрирование ThemeCheckBox (qml-components Δ, ui-layout-grid Δ)

- [x] 2.1 Красный геомет-пин: центр индикатора = центр чернил подписи (`FontMetrics.boundingRect("Х")`) ±1 px; полоса чекбокса = полоса ряда кнопок (implicitHeight); в обеих темах
- [x] 2.2 `ThemeCheckBox.qml`: offset индикатора по `min((descent+leading)/2, 1)`, верх/низ padding `space.sm`; места применения (7 островов) без правок
- [x] 2.3 Обновить эталоны пиксельной приёмки; зелёный прогон, покрытие 100 %

## 3. Шапка карточки: русское имя и полная генерация (qml-shell Δ, entity-generation Δ)

- [x] 3.1 Красные пины: заголовок карточки каждого типа «Карточка: <русская метка реестра>» (`display_label`), сырьё ключей в видимых заголовках отсутствует (grep-пин); в шапке порядок «заголовок → ✨ генерация (отступ space.sm) → … → ✕»; в строке «Название» осталась только генерация названия; в диалоге события генерация сущности в слоте шапки; `objectName` кнопок и контракт aiState/гейтов не изменились
- [x] 3.2 `ThemeSheetHeader`: слот действия после текста заголовка (дефолтное свойство, центрирование по строке заголовка)
- [x] 3.3 Локализация заголовка в `entity_card_dialog.py` (в window title попадает русское имя); перенос кнопки полной генерации в слот шапки карточки и события
- [x] 3.4 Обновить e2e/виджет-селекторы родителя кнопок; зелёный прогон, покрытие 100 %

## 4. Полные вкладки и старт главного окна (main-window Δ)

- [x] 4.1 Красные пины: текст вкладки = `plural_label` = имя доступности = подсказка; дефолтный фрейм 1280×800; восстановленный фрейм шире порога вкладок при сохранённом узком (позиция/высота сохранены); порог = ширина панели деталей «все вкладки целиком»
- [x] 4.2 `entity_registry.py`: `tab_label` удалён; viewmodel/QML панелей деталей на полных подписях; пины NRI-0015 про короткие подписи переустановлены
- [x] 4.3 `geometry_memory` для роли main: расширение ширины до провайдера минимума; `main_window`: дефолт 1280×800, минимум правой панели сплиттера из константы панели деталей
- [x] 4.4 Пин follow-up OBS-1 переживает новый старт (строка «Дата:» снимка с целой «Показать всё» на дефолте); зелёный прогон, покрытие 100 %

## 5. Шкала ширин и токены отступов окон (ui-layout-grid Δ)

- [x] 5.1 Красный табличный тест: естественные ширины всех окон/листов кратны 40 и равны целевым (420→440 список/стол, 480 лаунчер, 560 пресет, 640 LLM/xlsx, 720 событие/изображение/документ, 760 floor карточки, 880 мастер, 1120 заполнение, 1280 редактор)
- [x] 5.2 Правки QML `implicitWidth` и python-floor диалогов синхронно (таблица 5.1)
- [x] 5.3 Отступы: `SearchBarRoot` space.xs→space.md, `DocViewerRoot` space.sm→space.md; зелёный прогон, покрытие 100 %

## 6. Навигация календарной сетки: полоса 32 и тематизированный чекбокс (game-calendar-grid Δ, ui-widget-catalog Δ, ui-layout-grid Δ)

- [x] 6.1 Красные пины: контролы строки «◀ месяц | год ▶ ☐ до н.э.» — высота 32 и общий центр; QSS листа попапов содержит тематизированные `::indicator` чекбокса эры из токенов (тест синхронной правки класса)
- [x] 6.2 `calendar_grid.py`: стрелки 28→32, фиксированные высоты строки навигации; правила индикаторов в `compiler.py` (popup-лист), off-skin-проверка нативности

## 7. Мастер календаря: левая граница, колонка по содержимому (calendar-wizard Δ, ui-widget-catalog Δ)

- [x] 7.1 Красные пины: радио «Стандартный/Кастомный» на левой границе заголовка (один вертикальный срез, без `AlignHCenter`); ширина левой колонки = ceiling(40) от natural sizeHint стека и не больше потолка; предпросмотр умещается на минимуме; минимум окна пересчитан; `::indicator` радио тематизированы chrome-листом (обе темы)
- [x] 7.2 `calendar_wizard.py`: снять центрирование шага, колонка по содержимому (stretch → natural width с округлением к 40 и потолком ~520, измерить фактический `sizeHint` для уточнения ступени), новый `setMinimumSize`; `compiler.py`: правила `::indicator` радио/чекбоксов для chrome-области
- [x] 7.3 Обновить пины W4 (`toggled`/строка кнопок без изменений), зелёный прогон, покрытие 100 %

## 8. Документация и живой контур

- [x] 8.1 Артефакты: CHANGELOG, functional-checklist (полосы рядов/вкладки/карточка/мастер), AGENTS.md (ссылки обновить, если контракт дерева затронут — новых пределов не ожидается), строка П5 и статусы в `docs/design-review-roadmap.md`
- [x] 8.2 Финальный таск: живой ре-аудит computer-use (throwaway-игра `qa-2026-09-26`, AI-кнопки не нажимать): строки мастера/попапа/лаунчера (полоса, квадраты), ширины всех окон из таблицы, старт 1280×800 и расширение сохранённого, вкладки полные, карточка/событие — заголовок и ✨ в шапке (проверка самого факта размещения, не генерации), OBS-1 закрыт; дописка в `docs/qa/` (починено/живёт дальше); при новой находке — follow-up, молчаливых расхождений не оставляем
- [x] 8.3 `openspec validate --strict` зелёный; полный `QT_QPA_PLATFORM=offscreen python -m pytest` зелёный с гейтом 100 %
