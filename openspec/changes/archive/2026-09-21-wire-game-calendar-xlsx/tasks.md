# Tasks: wire-game-calendar-xlsx

## 1. Парсер: API и ветвление

- [x] 1.1 В `xlsx_schema` добавить `DateParse` (frozen: `coord: GameCoord | None`, `is_bc`, `problem: DateProblem | None`; инвариант «координата XOR проблема», фабрики `DateParse.ok`/`DateParse.fail`, свойство `ok`; тест на взаимоисключение) и `DateProblem`; менять `parse_cell_date` на `(value, calendar=None) -> DateParse | None` (по умолчанию `current_calendar()`), стандартная ветка = нынешние разборы слово в слово, успех → `.ok(as_game_coord(...), is_bc)`, неразбираемое → `None`, `fail()` в стандартной ветке не возникает. Единовременно мигренировать единственный вызов в сервисе: `_row_to_draft` читает `.coord`/`.problem` вместо распаковки кортежа, `_to_coord` принимает `coord: GameCoord` (мёртвый `as_game_coord(moment)` уходит), `_era_text` рендерит «old»-строку отчёта из координаты — на пресете «Стандартный» вывод побайтово прежний (пин — `test_xlsx_import_date_shift`); проверить `test_xlsx_import_service` зелёным
- [x] 1.2 Стандартная ветка не приобретает ни одной новой формы: позитивные кейсы (11) в `tests/application/test_xlsx_schema.py` переносятся на `DateParse.ok(...)` без изменения значений, 16 None-кейсов остаются нетронутыми как regression-запрет; добавить тест «игра на пресете „Стандартный“ отвергает „3 Зимостой 44“ и имена вставных дней» → `None`

## 2. Парсер: грамматика кастомной ветки

- [x] 2.1 Матрица тестов формы месяца (имена активного календаря; регистр, внешние/внутренние пробелы; хвосты пусто/`г.`/`г. до н.э.` как единственный источник эры; мост родительного падежа только при наличии именительного имени в календаре) и red→green grammatik в `xlsx_schema`
- [x] 2.2 Форма вставного дня: имя из `spec.intercalary` → `IntercalaryDay` по индексу правила; «номер дня при вставном имени» и «имя месяца без номера» — предметные `DateProblem`; green в тех же матрицах
- [x] 2.3 ISO, знаковое ISO и нативная ячейка в кастомной ветке отдают координату того же календаря (сдвиг `shift_invalid` не меняется); green-тесты переноса остаются зелёными
- [x] 2.4 Довести причину пропуска до пользователя: `.problem` из `DateParse` превращается в `RowIssue` с предметной подписью из словаря сервисного слоя (тексты отображения не в чистой функции парсинга); новые тесты причин каждой проблемы (`unknown_name`, `intercalary_with_day`, `month_without_day`, `out_of_range`) + существующие `test_xlsx_import_service`, `test_xlsx_import_date_shift` зелёные

## 3. Генератор шаблона

- [x] 3.1 Перенести `build_template_workbook`/`SAMPLE_ROWS` из `tests/application/test_xlsx_import_template.py` в `app/application/services/xlsx_template.py` (сигнатура с календарём); проверить equality-тестом «генератор ≡ существующий `resources/import_template.xlsx`» на удалённом пока файле
- [x] 3.2 Корректировка примерных дат до валидных координат активного календаря (месяц → `min`, день → `min` от длины), BC-строка «Заговор» игровой текстовой формой в кастомной ветке, дополнительная строка «Событий» со вставным днём при непустом `spec.intercalary`; green: золотой снимок фиксированного кастомного календаря + «стандартная корректировка — тождество»
- [x] 3.3 Зафиксировать золотой снимок «Стандартного» (таблица клеток из выхода генератора до удаления файла) в тестах; проверить снимок зелёным и cleanliness-инвариант: скачанный шаблон любой из двух фиксированных игр импортируется в неё без проблем и без строк переноса

## 4. «Скачать шаблон» и зачистка ресурсов

- [x] 4.1 `save_template_as`: после подтверждения имени собирать workbook в память и писать на диск (дефолтное имя и ветки ошибок — прежние); перевести `tests/presentation/test_xlsx_import_dialog.py` (TestDownloadTemplateFlow) на генерацию — зелёные
- [x] 4.2 Удалить `resources/import_template.xlsx`, datas-запись в `nri_manager.spec` и `tests/test_spec_template_bundle.py`; добавить запрет-тест (спецификация не перечисляет шаблон в datas, файла нет в `resources/`) — зелёные

## 5. Подсказка диалога

- [x] 5.1 `date_formats_hint(calendar)` в слое подсказки: пресет — нынешние строки дословно; кастом — игровая форма с валидным примером активного календаря (`min(3, длина первого месяца)`) и именем вставного дня при наличии; покрыть юнит-тестами
- [x] 5.2 `build_format_text` подставляет календарь-зависимый блок «Даты», диалог перекрывает описания колонок дат; тест текстов: пресет — текущие строки слово-в-слово, кастом — пример с «Зимостой»

## 6. Интеграция и документы

- [x] 6.1 Прогон `QT_QPA_PLATFORM=offscreen python -m pytest` целиком с зелёным результатом и гейтом 100%-покрытия
- [x] 6.2 Строка C5 в `docs/custom-calendar-roadmap.md` помечена закрытой (изменение `wire-game-calendar-xlsx`), запись в `docs/CHANGELOG.md` по образцу C3b/C4
