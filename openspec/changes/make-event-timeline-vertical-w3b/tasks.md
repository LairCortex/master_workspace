# Tasks: make-event-timeline-vertical-w3b

## 1. Чистое ядро строк (без Qt)

- [ ] 1.1 Создать `app/presentation/views/timeline_rows.py`: `RowKind` (EVENT, EMPTY_DAY), `Row` (kind, date, event_id|None, start, end|None, name), `build_rows(events, range_start, range_end)` — каждый день диапазона → EVENT-строки дня (сортировка `(start, id)`) либо одна EMPTY_DAY; без фильтра = min(start)…max(end|start); событие только на позиции start. Проверить: модуль импортируется без QApplication.
- [ ] 1.2 Юниты `tests/presentation/test_timeline_rows.py`: диапазон 3 дня (событие/пусто/событие), многодневка занимает только start и end-день пуст при закрытом конце вне дней начала, детерминизм сортировки, диапазон из одного события, открытый конец расширяет диапазон до конца выборки.
- [ ] 1.3 Jump-хелперы `prev_event_index(rows, from_idx)` / `next_event_index(rows, from_idx)` (края → None). Проверить: юниты прыжка через серию пустых дней и бездействия на краях.

## 2. Список + делегат панели

- [ ] 2.1 В `timeline_widget.py` удалить `TimelineCanvas`; добавить `QListWidget` + itemData-модель строк из 1.1, фиксированная высота строки — константа модуля; делегат рисует: строку события `start — end · имя` (`format_game_date`, открытый конец `— `), пустой день — заглушка той же высоты. Проверить: widget-тесты offscreen — клик по EVENT-row эмитит `event_selected(id)`, dblclick эмитит id, клик по EMPTY_DAY не эмитит ничего.
- [ ] 2.2 Рейка в делегате: тик на сутки; подпись месяца (короткая форма при нехватке места, поворот снизу вверх) один раз при первом дне месяца; скобка start…end (открытый конец — до конца диапазона), пересекающиеся скобки — соседние дорожки рейки; ширина рейки = max(ширина подписей, константа-минимум). Проверить: widget-тесты геометрии (тикет ↔ позиция строки, скобка многодневки покрывает её сутки).
- [ ] 2.3 Цвета делегата из токенов (`token_rgb`, альфы от `accent_rgba`), live-retheme (пересборка палитры паса + `viewport()->update()`, выбор/скролл сохранены), оф-скин — именованные Qt-глобалы, ни hex, ни `palette()`. Проверить: widget-тест смены темы с выбранным событием; grep-инвариант `test_no_chrome_hex.py` зелёный без правок теста.
- [ ] 2.4 Липкая дата: `QLabel`-оверлей поверх viewport (роль `title`, accent-hairline, отступ viewport сверху на высоту оверлея), обновление по `valueChanged`/смене модели через `itemAt` верхнего края; скрыт при пустой модели. Проверить: widget-тест — оверлей показывает дату верхней видимой строки, меняется при переходе дня, скрыт при пустом списке (hint остаётся).
- [ ] 2.5 Публичный API панели: `update_events(events)` (пересборка только при смене версии набора, позиция скролла по выбранному id), идемпотентный `set_selected(id)`, `scroll_to_event(id)`, `jump_prev_event()/jump_next_event()`. Проверить: widget-тесты — выбор извне подсвечивает и доскроллит, повторный `set_selected` не пересобирает.

## 3. Чип-фильтр и jump-навигация

- [ ] 3.1 Убрать из шапки `CustomDateEdit`×2 + «Применить»/«Очистить»; добавить чип («Все даты» / `Дд Мес ГГГГ — Дд Мес ГГГГ`) + второй ряд кнопок `⤒`/`⤓`. Проверить: тест панели — чип/кнопки присутствуют, старых контролов нет, «+»-меню живо.
- [ ] 3.2 Поповер фильтра: top-level `QWidget` со скинингом через app-wide popup-лист W2a, два `_CustomCalendar` (переиспользовать `custom_date_edit.py`); первый клик — старт, второй — финиш → закрыть + `filter_changed(start,end)` live-apply; кнопка «Сбросить» → `filter_changed(None,None)` и чип «Все даты». Fallback низкого экрана — один календарь с назначением тапа. Проверить: widget-тесты live-apply/сброс/текста чипа; фильтр-семантика VM не менялась (существующие юниты зелёные).
- [ ] 3.3 Jump: QShortcut `Alt+Up`/`Alt+Down` (context = панель) + кнопки → ближайший EVENT-row от видимой позиции, `setCurrentRow`+`scrollTo`; на краях — бездействие. Проверить: e2e — прыжок через пустые дни достигает строки события; повтор на краю меняет скролл не более чем в конец/начало.

## 4. ViewModel и wiring

- [ ] 4.1 `timeline_viewmodel.py`: поверх `events` добавить производные видимые строки (вызов `build_rows`; диапазон — из `_current_filter` или min–max); `select_event_by_id`, `selected_event`, сигналы, притенка выбора — без изменений. Проверить: существующие юниты VM зелёные + новый юнит на диапазон/пустые строки.
- [ ] 4.2 `wiring.py`: семантических правок нет; привести точки к новому API панели (обновление строк через `update_events`, search-jump → `set_selected`+`scroll_to_event`). Проверить: E2E создания/выбора/редактирования, search→выбор+скролл, «фильтр исключил выбранное — очищены все слои» зелёные.

## 5. Удаление остатков Gantt

- [ ] 5.1 Удалить `app/presentation/views/track_layout.py`, `tests/presentation/test_track_layout.py` и canvas-тесты в `test_views.py` (замещены тестами 2.x). `compiler.token_rgb` остаётся. Проверить: `rg track_layout` пуст; `ruff check app/ tests/` без ошибок.

## 6. Приёмка

- [ ] 6.1 Переписать `tests/ui/helpers.py` (клик/dblclick по EVENT-row, скролл-aware) и прогнать E2E `test_e2e_events`, `test_e2e_wiring_gaps`, `test_e2e_months` (ожидания — подписи месяцев на рейке), `test_e2e_images`, `test_application_boot`, `test_e2e_crud/import/launcher`. Проверить: все зелёные в offscreen.
- [ ] 6.2 Переписать `tests/ui/test_e2e_timeline_theme.py` на делегат/рейку/липкую дату: grab-пиксели fill-выделения, тикета/скобки, sticky-подписи == токены, обе темы; правка `color.accent` в копиях токенов перекрашивает без правок экранов. Проверить: тест зелёный в offscreen.
- [ ] 6.3 Полный контур: `QT_QPA_PLATFORM=offscreen python -m pytest` зелёный; `python -m pytest --cov` — новый/изменённый код покрыт (гейт подтверждается прогоном CI, локальный 100%-гейт не проходит из-за off-skin-веток W2b — не трогать); ручной smoke: узкая панель (220 px), годовой диапазон, чип+поповер, Alt+↑/↓, live-тема с выбором.

## 7. Документация

- [ ] 7.1 `docs/CHANGELOG.md`: запись W3b (вертикальная шкала дат; UX-фолбэк W3 принят решением; чип-фильтр live-apply; jump-навигация; удаление `track_layout`) по формату существующих секций. Проверить: формат и статусы не противоречат roadmap и git.
- [ ] 7.2 `docs/design-system-roadmap.md`: строка W3b → «влито» только по факту коммита/мерджа; W3c остаётся «не начато»; список «вне карты» сохранён. Проверить: статусы соответствуют git-истории на момент коммита.
