# Tasks: refine-event-timeline-scale-w3c

## 1. Docs-статусы (до реализации)

- [x] 1.1 `docs/design-system-roadmap.md`: строка W3c → «в работе» (change-адрес зафиксирован), commit отдельно от кода. Проверить: остальные строки не тронуты.

## 2. Чистая геометрия дня (timeline_rows, без Qt)

- [x] 2.1 В `app/presentation/views/timeline_rows.py`: `index_at_y(rows, row_height, y)` → индекс первого ряда дня по вертикали (clamp к краям) и `normalize_range(day_a, day_b) -> (min, max)`. Проверить: юниты без QApplication — нормализация к первому ряду дня при нескольких рядах, clamp выше нуля/ниже хвоста, инвертированная пара → (min, max), пустые rows.

## 3. Рейка: клик-прыжок, follow-дата, глухой dblclick

- [x] 3.1 `TimelineListView`: удалить глотание press/dblclick из `eventFilter`; `mousePressEvent` при ЛКМ и `x < rail_width()` армит клик/drag (якорь = первый ряд дня), иначе — базовое поведение списка. Проверить: существующие view-тесты выбора/tooltip проходят; нажатие в рейке не испускает `event_selected`.
- [x] 3.2 Клик-прыжок D4: release до порога drag'а → `scrollToItem(PositionAtTop)` якорного ряда + `setCurrentIndex` с blocked-сигналами; выделение, `_selected_id` и id-сигналы не тронуты. Проверить: тесты — пустой день валиден, день с несколькими событиями → верхним стал первый ряд, selection/детали не изменились.
- [x] 3.3 Follow-дата D5: общий `_refresh_sticky_text()` (sync → follow поверх), флаг follow при hover в рейке, возврат на leave без drag. Проверить: sticky показывает день под курсором в рейке и дату верхнего ряда после выхода; hover-wash rows не изменился.
- [x] 3.4 Двойной клик в рейке consume (D8): ни выбора, ни `event_double_clicked`. Проверить: тест dblclick в рейке против строки события.

## 4. Drag диапазона

- [x] 4.1 State machine порога (`DRAG_START_THRESHOLD_PX`): после арма vertical move за порог (только ЛКМ) → drag-режим `_drag_range` = normalize(якорь, день под курсором), значим только y с clamp к видимому диапазону. Проверить: move до порога не создаёт диапазона; release вне вьюпорта/виджета = последняя видимая граница (юниты 2.1 + view-тест).
- [x] 4.2 Wash-полоса в `_RowDelegate`: `_Palette` + поле drag-wash (accent-derived alpha; вне скина Qt-глобал, const alpha), делегат красит полосу покрываемым рядам. Проверить: grab-тест полосы на частично видимом диапазоне; `tests/presentation/test_no_chrome_hex.py` проходит без правок.
- [x] 4.3 Применение D6/D7: ровно один emit нового сигнала view панелью → `filter_changed(start, end)` (тот же путь `_on_filter_range`), drag-состояние сбрасывается до/после emit; однодневный drag = клик. Проверить: тесты — один emit на release, чип показывает границы, drag поверх активного фильтра заменяет его, промежуточных пересборок модели нет (версия модели менялась один раз).

## 5. Приёмка и инварианты

- [x] 5.1 Сценарные тесты новых формулировок спеки: «Нажатие в рейке не выбирает событие», «Промах мимо привязки», «Follow во время drag'а», «Чип зеркалит drag-диапазон», «Сброс касается обоих способов», «Двойной клик в рейке глушится», «Границы drag'а ограничены видимым». Проверить: каждый сценарий покрыт тестом.
- [x] 5.2 Колесо и jump без изменений: шаг колеса 1 ряд, Alt+Up/Down, ПКМ — существующие тесты проходят; зума нет.
- [x] 5.3 Live-retheme во время активного drag'а: полоса перекрашивается, позиция скролла/выбор не сброшены. Проверить: тест смены темы в drag-фазе.
- [x] 5.4 Полный прогон: `QT_QPA_PLATFORM=offscreen python -m pytest` — зелёный.

## 6. Docs-итог (после мержа)

- [ ] 6.1 `docs/CHANGELOG.md` — срез W3c; `docs/design-system-roadmap.md` — строка W3c → «влито» только фактом коммита/мерджа (как W3b). Проверить: статусы соответствуют git-истории на момент коммита.
