# Живой визуальный ре-аудит: страница «Промпты полей» окна «Настройка LLM…» (2026-09-26)

Точка проверки 2 из 2 — вертикальные пары «подпись над полем» в `app/presentation/qml/LlmSetupRoot.qml`.
Инструменты: `munim-computer-use` (accessibility + zoom 2 px/pt) + попиксельный анализ снимков
`screencapture` (PNG 1280×1016 для окна 640×508 pt, т.е. ровно 2 px/pt Retina) — все замеры ниже
получены детекцией рамок поля `(58,58,68)` / заливающего `(30,30,36)` / фона `(42,42,50)`.

## Условия прогона

- Экземпляров `app.main` перед запуском НЕ было (`pgrep -fl "app.main"` пуст) — ничего чужого не убивал.
- Запущен свой экземпляр PID 83192 (`.venv/bin/python -m app.main`).
- **Проход к окну.** Модальный лаунчер (`launcher.exec()` до создания `MainWindow`) меню «LLM →
  Настройка LLM…» не даёт: живое дерево приложения с лаунчером не содержит ни одного элемента
  `LLM`/`Настройка` (замерено при frontmost), что совпадает с исходниками — `QAction` живёт в
  `_build_menu_bar` MainWindow, которого на том этапе ещё нет. По согласованному фолбэку открыта
 throwaway-игра `qa-2026-09-26` (не пользовательская; пользовательские `R.E.D.W.A.L.L`, `wef`,
  `супер дупер` не тронуты). Через неё: LLM → «Настройка LLM…» (пункт нативного меню-бара; фоновый
  клик координатами по system menu bar не срабатывает — нажатие выполнено через System Events).
- Окно: `position (950, 451)`, `size 640×508 pt` (System Events) — ширина строго контрактные 640 pt.
- Кнопки AI-генерации не нажимались. «Сохранить» не нажималась.

## Замеры и вердикты по пунктам

### П.2 — вертикальная композиция пары (страница «Промпты полей — События», счётчик «3 из 8»)

**PASS.** Заголовок «Промпты полей — События» и счётчик «3 из 8» подтверждены zoom'ом.
Все три пары: подпись сверху, своё поле под ней (детекция по колонке окна):

| пара | подпись (rows ink) | поле (box) | подпись→поле |
|---|---|---|---|
| Название: | y 225–245 px | 260–371 px | 7,0 pt (нет выносных элементов у «Название:») |
| Характеристики: | y 395–419 px | 430–541 px | 5,0 pt |
| Предыстория: | y 565–589 px | 600–711 px | 5,0 pt |

- Подписи СНАД, не слева; левый край текста подписей 16,0–16,5 pt = левая рамка поля 16,0 pt —
  выравнивание по левому краю точное.
- Плейсхолдеры на месте и читаемы на 2 px/pt: «Короткое название события в духе мира»,
  «Опиши ключевые характеристики события», «Напиши предысторию не менее 20 слов».

### П.3 — размеры и ритм

**PASS.**

- Ширина поля: рамка x 32–991 px → **480,0 pt** ровно (потолок `Layout.maximumWidth: 480`);
  при внутренней ширине 640 pt справа остаётся 144 pt пустоты — поле НЕ во всю ширину окна.
- Высота поля: рамка + заливка 260–371 / 430–541 / 600–711 px → **56,0 pt** ровно все три
  (соответствует `Layout.preferredHeight: 56`); нижняя рамка на месте (2 px @2x).
- Зазор подпись→своё поле: чернильный 5,0–7,0 pt (layout- spacing 4 pt + межстрочный воздух
  гарнитуры; для подписей с выносными элементами — ровно 5,0 pt). Группировка плотная.
- Зазор поле→следующая подпись: 11,5 pt; межпарный шаг поле→поле 29 pt. Контраст 5 pt против
  11,5–29 pt: пары читаются отдельными блоками (layout 4 pt против 8 pt + строка подписи).

### П.4 — пятиполевой тип («Промпты полей — Персонажи», счётчик «5 из 8»)

**PASS (с наблюдением).** Заголовок и счётчик подтверждены zoom'ом; в дереве 5 `TextField`
(Название/Характеристики/Предыстория/Личность/Задачи).

- До прокрутки в окне помещались 4 пары (поля 260–371 … 770–881 px); подпись и поле «Задачи»
  были за нижней границью вьюпорта — колонка действительно нуждается в прокрутке.
- Колесо мыши над полем (`scroll` по element id, 6 строк) двигает содержимое: боксы сместились
  вверх на 22 px (11 pt); после прокрутки все боксы 282–393 / 452–563 / 622–733 / 792–903 px
  (**высота каждого 56,0 pt сохранена**), подпись «Задачи:» ink 757–780 px целиком над своим полем,
  низ последнего поля 903 px ≈ 451,5 pt — выше строки кнопок (ink с ~920 px), ничего не обрезано,
  пятая пара полностью достижима.
- Наблюдение (minor): видимого скролл-бара нет — в `x 1230–1277 px` правой полосы ни трека, ни
  ползунка не отрисовано (в дереве `ScrollBar`-узлы есть, но непрокрашены). Критерий точки
  («скролл-бар виден ИЛИ колесо двигает содержимое») выполнен второй веткой; отсутствие
  подсказки прокручиваемости — candidate UX-улучшение (полупрозрачный трек при переполнении).

### П.5 — возврат «Назад» на «События»

**PASS.** Счётчик «3 из 8»; те же три бокса 260–371 / 430–541 / 600–711 px (сброса offset между
страницами нет — у каждой страницы свой ScrollView, состояние «Событий» не уехало); прокрутка не
нужна: низ последнего поля 355,5 pt от верха окна при строке кнопок ~460 pt; видимых скролл-баров нет.

### П.6 — «Закрыть» без записи

**PASS.** AXPress по «Закрыть»: окно исчезло из списка окон немедленно, ни одного диалога
подтверждения не появилось (дерево/список окон чисты — осталось только MainWindow).
`~/.nri_manager/llm_config.json` не изменялся (mtime 17 сен 19:01). Правка подписей не производилась.

### П.7 — Teardown

`pgrep` показал ровно один экземпляр — мой PID 83192; `pkill -if "app.main"` остановил его,
пост-проверка: процессов нет.

## Итог

| пункт | вердикт |
|---|---|
| 2. вертикальные пары + плейсхолдеры (3 из 8) | **PASS** |
| 3. 480 pt / 56 pt / 4–5 pt vs 11,5–29 pt | **PASS** |
| 4. прокрутка 5-полевой страницы, 5-я пара достижима | **PASS** (скролл-бар не отрисован — minor наблюдение) |
| 5. назад на «События»: без прокрутки, всё видно | **PASS** |
| 6. «Закрыть» без подтверждений и записи | **PASS** |

Регрессий нет. Единственная находка — отсутствие видимого индикатора прокрутки на переполненной
странице (minor/UX, вне критериев точки). Побочный процессный факт: к пункту меню нельзя пройти
при незакрытом модальном лаунчере первого запуска — это контракт формата окон (лаунчер модален до
выбора игры), а не дефект данной правки.
