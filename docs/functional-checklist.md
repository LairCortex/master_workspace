# Функциональный чек-лист

Регрессионная сеть на уровне пользовательских функций (компенсирует отсутствие E2E-тестов).
После **каждого** коммита реализации прогоняются затронутые группы; после финала change — полный прогон всех групп.

Статус:

| Статус | Значение |
|---|---|
| ✅ | Группа проверена, всё работает как раньше |
| ❌ | обнаружен регресс |
| ⏳ | не проверялась |

**Baseline (до рефакторинга, v0.13.0):** авто-прогон ✅ — 389/389 тестов, offscreen-запуск (launcher → Application.start → MainWindow → shutdown) без ошибок; ручной smoke 14 групп — закрыт без прогона (решение 2026-08-22): к моменту оформления чек-листа код уже рефакторен (§A–§D), baseline на старом коде невалиден; регрессионная страховка — характеризационные тесты волны 1 (tasks 2.1–2.5)

**Итог nri-0005 (2026-09-23, применение `nri-0005-architecture-refactor`):** ручной клик-прогон headless-сессиями не выполнялся; вместо него — сквозная приёмка полным `QT_QPA_PLATFORM=offscreen python -m pytest` + маппинг затронутых рефакторингом групп на их e2e-аналоги (прогнаны в составе полного прогона): сохранение сущности/события цепочкой через единицу работы — `tests/ui/test_e2e_crud.py`, `test_e2e_events.py`, `test_e2e_dialog_save_failures.py` ✅ auto; popup-создание связанной сущности с отменой родительского диалога — `tests/ui/test_e2e_wiring_gaps.py` (+регрессия задачи 5.6 «отменённая popup-сущность не оседает в БД») ✅ auto; xlsx-импорт (§9) — `tests/ui/test_e2e_import.py`, `tests/application/test_xlsx_import_*` (за известным timing-флаком, см. заметку флага в `docs/refactoring-audit.md`) ✅ auto; мастер календаря (§8) — `tests/presentation/test_calendar_wizard_*`, `tests/ui/test_e2e_calendar_wizard_entry.py`, `test_e2e_custom_calendar_acceptance.py` ✅ auto; LLM-настройки (§11) — `tests/ui/test_e2e_llm.py`, `test_llm_*`, per-game промпты через `LlmSettingsRepository` — `tests/test_application_settings_errors.py` ✅ auto; листы персонажей — `tests/ui/test_e2e_char_sheets.py`, `test_char_sheets_wiring.py`, `tests/presentation/test_sheet_*` ✅ auto. Визуальные ручные пункты (цвет фона рейтинга, цвета снимка мира, 8-шаговый визард в GUI) остаются ⏳ с прежних строк — headless-приёмка их не покрывает.

## 1. Лаунчер игр
> Статус финального прогона (2026-08-22): ✅ авто — `tests/infrastructure/test_game_manager.py` (create/open/delete/import), `tests/presentation/test_views.py` GameLauncherDialog, `tests/test_application_start.py` (смена игры без перезапуска).

- Создать игру (новое имя)
- Открыть игру
- Удалить игру
- Импорт из `.nri` (метаданные отображаются)
- Импорт `.nri` в существующую игру — предупреждение о дубле
- Файл → Сменить игру без перезапуска приложения; формат — **не-модальное окно** с видимым заголовком и крестиком, повторный вход поднимает открытый лаунчер вместо дубля (`tests/ui/test_e2e_launcher.py`, nri-0014 A1–A3)

## 2. Глобальный поиск
> Статус финального прогона (2026-08-22): ✅ авто — `tests/application/test_search_service.py` (имя/характеристики/предыстория, регистр, дедупликация, группы), `tests/presentation/test_views.py` SearchBar (debounce, старт от 2 символов), `tests/test_integration.py` (поиск на реальных данных через сервисы).

- debounce 300 мс
- старт поиска от 2 символов
- регистронезависимость
- поиск по имени
- поиск по характеристикам
- поиск по предыстории
- клик по результату — открывается карточка сущности
- клик по результату-событию — выделение события на таймлайне
- результаты сгруппированы по типам

## 3. Таймлайн
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/application/test_services.py` TimelineViewModel (сортировка, выбор), `tests/presentation/test_views.py` TimelineWidget (фильтр С/По, сброс, сигналы), `tests/test_integration.py::test_timeline_ordering`; ⏳ ручной: сохранение фильтра после перезапуска игры в GUI.

- сортировка событий по дате
- выбор события — панель деталей
- двойной клик — редактирование события
- кнопка `+` — создание события
- контекстное меню — создание самостоятельных сущностей
- фильтр «С / По»
- сброс фильтра «✕»
- фильтр сохраняется после перезагрузки (per-game)

## 4. Событие (создание/редактирование)
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_dialogs.py` EventDialog (валидация, даты, бессрочно), `tests/application/test_event_service.py` (создание/обновление со связями: привязать/создать/отвязать), `tests/test_integration.py` (полный цикл).

- валидация обязательных полей (кнопка «Сохранить» блокируется)
- выбор дат
- «Бессрочно» → `∞`
- связи: привязать существующих сущностей
- связи: создать новую связанную сущность
- связи: отвязать лишние
- предыстория сохраняется
- характеристики сохраняются

## 5. Карточка сущности (4 типа)
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_dialogs.py` EntityCardDialog (поля по типу, image, music, M2M-секции, рейтинг 1–20, spec-driven набор), `tests/application/test_entity_service.py` (sync_related); ⏳ ручной: визуальный цветовой фон рейтинга.

- поля по типу: personality — только персонаж
- поля по типу: image — персонаж / организация / локация
- поля по типу: tasks — 3 типа
- поля по типу: music_url — все типы
- рейтинг 1–20 + цветовой фон
- изображение: загрузка
- изображение: превью
- изображение: удаление
- музыка: ссылка
- музыка: режим редактирования
- M2M-вкладки: привязать существующих
- M2M-вкладки: создать новую связанную
- M2M-вкладки: отвязать лишние

## 6. @Mention
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_mention_text_edit.py` (popup от 2 символов, навигация, Enter/клик, кликабельная ссылка, маркеры в старых текстах, roundtrip HTML).

- `@` + 2 символа → popup с иконками
- навигация стрелками + Enter
- выбор кликом
- ссылка кликабельная → открывает карточку сущности
- обратная совместимость старых текстов с маркерами

## 7. World Snapshot
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/presentation/test_views.py` WorldSnapshotWidget (секции, «Показать всё», signal снапшота), `tests/application/test_services.py` (активные события по дате); ⏳ ручной: цвета по рейтингу, тултипы.

- выбор даты → активные события (диапазон / `∞`)
- 5 секций без дубликатов
- «Показать всё»
- цвет по рейтингу
- тултипы
- двойной клик → карточка
- статистика внизу

## 8. Календарь игры
> Статус финального прогона (2026-09-21, закрытие карты C0–C6): ✅ авто — `tests/domain/test_game_calendar.py` (ядро), `tests/presentation/test_calendar_wizard_viewmodel.py` + `test_calendar_wizard_dialog.py` (экраны и flow мастера), `tests/presentation/test_calendar_grid.py` (сетка дней: плашки вставных дней, эра, имена недели), `tests/presentation/test_theme_date_popup.py` (попапы дат: клик по плашке и чекбокс эры), `tests/ui/test_e2e_calendar_wizard_entry.py` (точки входа и первый запуск), `tests/ui/test_e2e_first_run_wizard.py` (старт-контур nri-0015 W3: окно → мастер поверх → отмена возвращает в окно, без вложенного цикла), `tests/ui/test_e2e_custom_calendar_acceptance.py` (обе приёмочные сцены реальными кликами, см. ниже), `tests/test_calendar_seed_migration.py` (посев новой игры), подписание имён — `tests/presentation/test_custom_months.py`, failure-fallback битого ключа — `tests/test_application_settings_errors.py`. Диалог «Названия месяцев…» удалён, его место занял мастер.

- вход **Настройки → Календарь…** доступен в любой игре, текущий вид предвыбран
- первый запуск новой игры — мастер открывается **после показа главного окна** тем же путём, что вход из меню (модаленый top-level, без вложенного цикла — nri-0015 W3/FI-6, `tests/ui/test_e2e_first_run_wizard.py`); закрыт без применения = «Стандартный», флаг «показан» пишется один раз
- черновик мастера: пройденные ступени сохранены, повторный вход продолжает с сохранённого, брошенный черновик зовёт мастера обратно
- выбор вида «Стандартный» / «Кастомный»
- неделя: длина и имена дней
- месяцы: число, имя и длина каждого (строки «имя | длина»)
- вставные дни: добавление правила с прикреплением после месяца-хозяина, удаление и порядок
- предпросмотр: сводка + живая сетка будущего календаря
- старые игры: отсутствие ключа = «Стандартный», мастер не навязывается; битая настройка = пресет с одним предупреждением
- старые названия месяцев (`custom_months`) мигрируют в настройку календаря
- подписи новым календарём — таймлайн, детали, snapshot, календари выбора, поиск; распространение применения без перезапуска
- сцена «Мастер до конца и жизнь кастомной игры» (e2e, реальные клики): мастер неделя→месяцы→вставные дни→предпросмотр→«Применить»; событие на вставном дне (плашка `GameCalendarIntercalaryChip`) и событие с эрой «до н.э.» (клетка + эра-чекбокс); шкала, лента, детальная и снимок мира подписывают новые имена, вставной день — своим именем без номера, эра — суффиксом «до н.э.»; после перезапуска календарь и обе записи без изменений
- сцена «Правка в середине игры» (e2e): укорочение месяца, задевающее даты записей → экран «Перенос дат» с именами перенесённых записей («Запись / Поле / Было / Станет») → «Перенести и применить» → лента и детальная показывают перенесённые даты

## 9. XLSX-импорт
> Статус финального прогона (2026-08-22): ✅ авто — `tests/application/test_xlsx_import_service.py` (5 типов, заголовки, пропуск с предупреждением, даты текст+native, отчёт), `tests/presentation/test_xlsx_import_dialog.py` (сигнал, прогресс).

- импорт 5 типов сущностей
- заголовки колонок по имени
- строки без обязательных полей пропускаются с предупреждением
- даты `YYYY-MM-DD` (текст)
- даты native Excel
- прогресс импорта
- отчёт «Создано / Пропущено»

## 10. Экспорт/импорт `.nri`
> Статус финального прогона (2026-08-22): ✅ авто — `tests/infrastructure/test_game_manager.py` (экспорт/импорт `.nri`, структура ZIP `game.db` + `meta.json`); меню-экспорт заводится в `tests/test_application_start.py` (start/shutdown smoke).

- экспорт через меню
- структура ZIP: `game.db` + `meta.json`
- импорт через лаунчер

## 11. LLM-AI-ассистент
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/infrastructure/test_llm_config.py` (конфиг 0600), `test_llm_errors.py` + `test_llm_providers.py` (RU-ошибки, ретраи 429/5xx, статусы not_configured/ready), `tests/presentation/test_llm_viewmodel.py` (очередь, блокировка поля), `test_llm_setup_dialog.py`, per-game промпты — `tests/test_application_settings_errors.py`; ⏳ ручной: прохождение визарда 8 шагов в GUI.

- визард 8 шагов
- «Проверить соединение» (1 токен)
- статусы `not_configured` / `ready`
- ✨ на текстовых полях
- очередь генераций
- блокировка поля при генерации
- ошибки: RU-тексты
- ошибки: ретраи 429/5xx
- глобальный конфиг 0600
- per-game промпты

## 12. Изображения
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_image_utils.py` (ресайз ≤1000×1000, base64, превью 280, thumbnail 100).

- ресайз ≤ 1000×1000
- base64 в БД
- превью 280×280
- thumbnail 100×100

## 13. Бессрочность
> Статус финального прогона (2026-08-22): ✅ авто — `tests/domain/test_entities.py` (end_date=None), `tests/infrastructure/test_repositories.py::TestEventRepository::test_get_events_at_date` (бессрочные включаются в диапазон), EventDialog «Бессрочно» — `tests/presentation/test_dialogs.py`, `format_game_date(None, «∞»)`.

- чекбокс скрывает поле даты
- `∞` на таймлайне
- snapshot активен на любую дату после начала
- фильтр включает бессрочные

## 14. Рейтинг
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/presentation/test_dialogs.py` (spinbox 1–20, clamp в populate); ⏳ ручной: цвет серый→красный в списках и snapshot.

- spinbox 1–20
- цвет серый → красный в списках
- цвет серый → красный в snapshot

## 15. Дерево accessibility QML-островов (nri-0012)
> Статус (2026-09-23, change `nri-0012-qml-accessibility`): ✅ авто — оффскрин-тесты по острову (`tests/presentation/test_*_accessibility*.py`, точечные ассерты в `test_search_bar_island`/`test_llm_setup_island`/`test_character_sheet_*_dialog`/`test_event_types_dialog`/`test_thin_dialog_islands`/`test_mention_field_qml`) плюс гарды контракта `tests/test_qml_accessibility_conventions.py` и `tests/test_qml_stock_controls_accessibility.py` (второй пинит оффскрин-половину 4.2; «имя штатной кнопки равно тексту» — за неё живой аудит). Живой прогон — `docs/qa/2026-09-23-accessibility-audit.md`: адресуемость без координат подтверждена, находки — follow-up FI-1…FI-6 (в AGENTS), список четырёх пределов не расширен.

Интерактивные элементы каждого экрана доступны из дерева accessibility с ролью и именем — без координат экрана:

- **Лаунчер (`LauncherRoot`)** — строки игр `ListItem` с именем строки (контракт `RowItem`), текстовые кнопки штатно;
- **Глобальный поиск (`SearchBarRoot`)** — поле «Поиск по всем сущностям» (значение — в value-слоте), строки результатов `ListItem` (контракт строки);
- **Таймлайн (`TimelineRoot`/`TimelineRowDelegate`)** — строка события `ListItem` «Открывает событие», кнопка «Добавить событие»;
- **Панель деталей (`DetailPanelRoot`)** — строка-карточка `ListItem` «Открывает карточку», картинка-кнопка «Открыть изображение», табы штатно;
- **Снимок мира (`WorldSnapshotRoot`)** — строка сущности `ListItem` «Переходит к сущности», заголовок секции `Button` «Развернуть или свернуть раздел»;
- **Dialog события (`EventDialogRoot`)** — «Название события», «Дата начала»/«Дата конца», «Тип события», mention-поля «Характеристики»/«Предыстория», свотч «Цвет типа события», AI-кнопки «Сгенерировать: …» (целиком — «Сгенерировать: Событие»);
- **Типы событий (`EventTypesRoot`)** — «Название типа события», свотчи «Цвет палитры №N», стрелки «Поднять тип»/«Опустить тип», строки типов (контракт `RowItem`);
- **Карточка сущности (`EntityCardRoot`)** — «Название», даты, «Рейтинг», «Ссылка на музыку», mention-поля по меткам, AI-кнопки «Сгенерировать: …»; кнопки «Сохранить»/«Открыть чар-лист» штатно;
- **Чар-лист, дизайн-холст (`SheetCanvas`)** — поля по типу (`EditableText`/`CheckBox`/`Button`), инлайн-редактор «Редактирование поля»;
- **Редактор листов (`SheetEditorRoot`)** — «Ориентация страницы», «Переименование страницы», «Вверх»/«Вниз», «Удалить/Добавить страницу», «Позиция X/Y», «Ширина/Высота», «Кегль», «Текст поля», «Число», «Минимум»/«Максимум», опции;
- **Заполнение листа (`SheetFillRoot`)** — «Значение поля», «Значение поля (многострочно)», «Значение из списка»;
- **Пресеты листов (`SheetPresetRoot`/`SheetListRoot`)** — «Имя листа», «Текст лицензии», строки списка (текст/контракт `RowItem`);
- **Просмотр документа (`DocViewerRoot`)** — «Текст документа»;
- **XLSX-импорт (`XlsxImportRoot`)** — «Путь к файлу .xlsx», «Требования к формату файла»;
- **LLM-настройки (`LlmSetupRoot`)** — «Endpoint», «Модель», «Ключ API», «Описание мира», промпты по `modelData.label`;
- **Просмотр изображения (`ImageViewerRoot`)** — только штатные текстовые кнопки (имя из текста).

Пределы (адресация без координат честно невозможна — см. контракт в `AGENTS.md`): ① строки попапа упоминаний — только клавиатура после фокуса поля; ② набранный текст `MentionField` и инлайн-редактора листа — value-слота у кастомного Item нет (мост — follow-up NRI-0013; живьём plain-слой поля подписывает набранный текст как своё имя); ③ нативные попапы дат и выпадаюков `ThemeComboBox` — отдельные системные окна (триггер именован); ④ текст tooltip-моста — в дерево не переносится; ⑤ (добавлен NRI-0017) cocoa-проекция теряет `ListItem`-роль строк — строки доходят как статический текст с полным именем, адресность и AXPress работают.

## 16. Формат окон контракта и русское оперение (nri-0014)
> Статус (2026-09-24, change `nri-0014-window-contract-and-docs`): ✅ авто — формат/реестр/скролл/язык пинятся оффскрин (файлы указаны по строкам); живой ре-аудит — дописка в `docs/qa/2026-09-24-window-layout-audit.md`. Норма «вход→формат» — в `AGENTS.md` (раздел computer-use); новых пределов координатности не заведено.

- **Документ прокручивается**: «Документация»/«Changelog» — не-модальные окна с настоящей шапкой (`windowTitle`) и крестиком, текст целиком доступен прокруткой (`ScrollView` вокруг поля), повторный пункт поднимает открытое окно; README и Changelog — два разных окна в единственном экземпляре — `tests/ui/test_doc_viewer_scroll.py`, `tests/presentation/test_views.py`, `tests/presentation/test_window_registry.py`
- **«Смена игры» — не-модальное окно**: заголовок виден, меню живёт, закрытие без выбора возвращает в ту же игру; лаунчер первого запуска остаётся модалкой — `tests/ui/test_e2e_launcher.py`
- **«Настройка LLM…»** — тоже не-модальное окно-единственный экземпляр (ключ `llm_setup`) — `tests/ui/test_e2e_llm.py`
- **Листы события/карточки с шапкой**: строка заголовка с текстом окна и кнопкой «Закрыть» (тот же исход, что Esc); второй лист в стеке затемняет нижний (`color.scrim`) — `tests/presentation/test_sheet_header_accessibility.py`, `test_sheet_header_dialogs.py`, `test_sheet_stack_scrim.py`
- **Вёрстка без мёртвых зон**: лист «Импорт из .xlsx…» — скрытые панели не занимают слотов; «Календарь…» — модален как top-level-окно и не двигает главное окно — `tests/presentation/test_xlsx_import_dialog.py`, `tests/ui/test_e2e_calendar_wizard_entry.py`
- **Стандартные диалоги русские**: кнопки `QMessageBox`/`QInputDialog`/`QDialogButtonBox` — «ОК»/«Отмена»/«Да»/«Нет» из комплекта `qtbase_ru.qm` независимо от локали ОС; файловые панели — не-нативные русские с фильтром типов, одинаковы на dev и в `.app` — `tests/test_interface_language.py` (+AST-пин `DontUseNativeDialog` на все вызовы)

## 17. Главное окно, мастер и попап даты (nri-0015)
> Статус (2026-09-25, change `nri-0015-main-window-calendar-wizard`): ✅ авто — все строки закреплены оффскрин-тестами (файлы по строкам); живой ре-аудит — дописка в `docs/qa/2026-09-24-window-layout-audit.md`. Короткие подписи вкладок — поле реестра и память геометрий в `ui.json` зафиксированы в `AGENTS.md`.

- **Вкладки деталей читаемы при дефолтной ширине**: с NRI-0018 вкладка носит полное реестровое имя («Персонажи»), короткие подписи `tab_label` из реестра удалены — текст, имя доступности и tooltip = одно и то же `plural_label`; делегат держит `width: implicitWidth`, старт окна 1280×800 (§20) — `tests/presentation/test_detail_tabs_labels.py` (переустановленные пины: полные подписи, порог «все вкладки целиком», guard отсутствия `tab_label`)
- **Пустые панели объясняют себя**: пустая панель деталей — muted-подсказка «Выберите строку — тут появятся детали», пустая вкладка «Шаблоны» — подсказка про создание/пресет (B6); обе исчезают с первым элементом — `tests/presentation/test_detail_panel_island.py`, `tests/presentation/test_sheet_islands_qml.py`
- **Геометрии именованных окон помнятся**: `UiPrefs.windows` (роль→`[x,y,w,h]`) в `ui.json`, восстановление с clamp в подключённые экраны (окно не может очутиться за краем — M5), редактор/Fill без сохранённой роли открываются по центру экрана (B4) — `tests/presentation/test_geometry_memory.py` (round-trip, clamp, «целиком в экране», сбойные записи выкидываются, тема выживает)
- **Мастер календаря**: первый запуск — мастер поверх показанного окна без вложенного цикла, ошибка применения видимым путём меню (W3/FI-6) — `tests/ui/test_e2e_first_run_wizard.py`; предпросмотр вплотную под шапкой дней, «Отмена» и группа навигации одной строкой, содержимое шага прижато к левой границе текста (центрирование шага снято NRI-0018, §20), числа предпросмотра `color.fg.primary` (W1/W4/W5) — геометрия-тесты `tests/presentation/test_calendar_wizard_dialog.py`; радио «Стандартный/Кастомный» на `toggled` — AX-`doAction("Press")` меняет выбор модели ровно один раз (W2) — там же
- **Листы события/карточки единообразны**: метки обязательных «<Имя>: *» в обоих листах (E2/CR4), «Сохранить» карточки блокируется при пустом названии как в событии (CR3), ✨-кнопка подписана полем-мишенью и стоит в ряду своего поля — карточка персонажа говорит «Сгенерировать: Персонаж», не «Событие» (E3/CR1) — `tests/presentation/test_sheet_parity.py`; индикатор раскрытия `ThemeComboBox` целиком внутри рамки поля в обеих темах (E4/B5) — пиксельный grab `tests/presentation/test_qml_components.py`
- **Попап даты читаем**: пустое окно фильтра открывается страницей текущей даты обеих сеток, не «январь, год 1» (P1); подписи «Начало окна»/«Конец окна» видны до первого выбора (P2); в режиме «Все дни» ни одна обычная ячейка не залита акцентом — попап создаётся без widget-родителя, его красит только popup-sheet (P3) — `tests/presentation/test_theme_date_popup.py`, пиксельная проба `tests/ui/test_theme_grab.py` (живьём повторное открытие подтверждено zoom’ом, оффскрин-артефакт ленивого re-polish ячеек на живом дисплее не воспроизводится)

## 18. «Стол», LLM-окно, логи, версия и темы-мелочи (nri-0016)
> Статус (2026-09-25, change `nri-0016-table-llm-logging`): ✅ авто — все строки закреплены оффскрин-тестами (файлы по строкам); полный гейт `QT_QPA_PLATFORM=offscreen python -m pytest --cov=app` зелёный (3820 passed, 3 skipped, покрытие 100 %). Живой ре-аудит computer-use (таск 7.3) выполнен 2026-09-25, дефекты живой части (LS4, zombie-слушатели островов) починены и подтверждены повторным живым прогоном — `docs/qa/2026-09-25-table-llm-logging-live-audit.md`; синхронизация версии в четыре места — норма `AGENTS.md` (Build & release).

- **Стол закрывается остановкой**: закрытие панели хоста крестиком при работающем столе спрашивает «Остановить стол?» — «Да» останавливает HTTP через существующий `stop_requested` (порт освобождён) и закрывает панель, «Нет» оставляет стол работать и панель открытой; программное закрытие (смена игры, shutdown) подтверждения не спрашивает — `tests/ui/test_table_host_panel_close.py`, `tests/ui/test_table_host_wiring.py` (TB1)
- **Реквизиты — только по факту запуска и актуальны порту**: до старта блоки URL/QR/PIN скрыты, правка порта до старта снимает показ; после старта адреса показывают фактический порт, QR кодирует адрес-хост показанного списка; кнопки панели не реагируют на Enter (без autoDefault); подпись «Порт:» и плейсхолдер пустого списка игроков — `tests/ui/test_table_host_panel_close.py` (показ/сброс/Enter/QR), `tests/presentation/test_table_host_panel.py`, `tests/ui/test_e2e_table_host.py` (TB2, TB5, TB7)
- **PIN шесть цифр и копируется**: сервис даёт `randbelow(1_000_000)` в `:06d`, веб-клиент синхронно требует ровно 6 (`maxlength` и проверка длины — греп-пин), текст PIN выделяется и копируется мышью; каждый старт — новый PIN — `tests/application/test_table_host_service.py`, `tests/application/test_table_host_edges.py`; греп-пин веб-констант — `tests/ui/test_table_host_panel_close.py` (TB6)
- **Посадка — реальные чекбоксы, «Выгнать» с гейтом**: строки посадки — штатные `QCheckBox`+имя (переключение ведёт прежние `seat()/drop_seat()` под тем же гардом загрузки, без рекурсии при перерисовке); «Выгнать» активна только при выбранном игроке и running — «тихого» нажатия больше нет — `tests/ui/test_table_host_panel_close.py`, `tests/presentation/test_table_host_panel.py` (TB3-ремонт, TB4)
- **LLM — окно с парами и футером**: не-модальное окно-единственный экземпляр с настоящим заголовком «Настройка LLM…»; на страницах промптов каждая подпись стоит непосредственно перед своим полем (один Repeater пар); на всех страницах футер «N из M» и постоянная «Закрыть» — закрывает без сохранения и без подтверждения (запись остаётся явным действием) — `tests/presentation/test_llm_setup_pair_layout.py`, `tests/ui/test_e2e_llm.py` (LS1, LS2)
- **Терминология и имена слотов едины**: во всех строках настройки один корень «промпт» (греп-пин нуля «промт» без «п» по `app/`); имя доступности поля ключа — константа «Ключ API», плейсхолдер в имя не просачивается — `tests/test_terminology_prompt_root.py`, `tests/presentation/test_llm_setup_island.py` (LS3, LS4)
- **Лог дома и честный**: «О приложении → Сохранять логи в файл» пишет `~/.nri_manager/nri_manager.log` (не рядом с exe/checkout), `RotatingFileHandler` ~2 МБ × 3 архива UTF-8, выключение освобождает дескриптор, повторное включение дописывает; обработчик не обещает отсекаемый DEBUG (уровень = общий порог); закрытое окно снимает подписку темы (`remove_listener`) и молчит при последующих переключениях — `tests/presentation/test_views.py`, `tests/test_log_home_ast_pin.py` (AST-пин отсутствия `_app_root`), `tests/presentation/test_theme_apply.py` (AB5, AB6, D1)
- **Версия в меню**: неактивная строка «Версия X.Y.Z» в «О приложении», источник — `app.__version__`, тестом пинит равенство pyproject (`tomllib`); в UI нет литералов и чтения из Changelog — `tests/test_version_consistency.py` (AB7)
- **Переключатель темы — одна семантика**: и в лаунчере, и в меню — чекбокс с фиксированной подписью «Светлая тема», состояние — только галкой; текст от темы не зависит, переключение пишет `ui.json` как раньше — `tests/presentation/test_launcher_qml.py`, `tests/presentation/test_views.py` (D2)
- **Экспорт открывается у игр**: панель «Экспорт игры…» стартует в каталоге базы текущей игры (`games/<имя>/` из менеджера сессий, не литерал), имя `‹игра›.nri` предполагается, не-нативная русская панель цела — `tests/ui/test_e2e_export.py` (A4)

## 19. Accessibility-дополнения: живые даты, пресс-действия, описания строк (nri-0017)
> Статус (2026-09-25, change `nri-0017-accessibility-completers`): ✅ авто — все строки закреплены оффскрин-тестами (файлы по строкам), полный гейт зелёный с покрытием 100 %; живой контур computer-use (таск 6.3, throwaway `qa-2026-09-25d`, AI-кнопки не трогались) — дописка в `docs/qa/2026-09-25-accessibility-audit.md`: FI-1/FI-3/FI-4 закрыты живой проекцией, FI-2 переклассифицирован в предел ⑤ (подтверждён), FI-5 частично закрыт (острова чар-листов живьём; doc viewer/xlsx/LLM/стол/типы событий/панель деталей ждут real-mouse ре-аудита), B1–B3 и CR2 закрыты живыми проверками; новая находка OBS-1 (обрезок «Показать всё» при 1028) — отдельный follow-up, молчаливых расхождений нет.

- **Дата открыта прессом (M2/M3=FI-1, spec `qml-components`)**: `ThemeDateField` держит роль `Button`, имя и `Accessible.onPressAction` внутри компонента (хосты не пере-аннотированы), минимальная ширина — наихудшая подпись хост-форматтера (год не режется, elide остаётся запасным), попап открывается и с AXPress, и с мыши; живьём подтверждено на «Дата» снимка и «Дата начала»/«Дата конца» листа события и карточки — `tests/presentation/test_theme_date_field_accessibility.py`, `test_qml_components.py`, `tests/ui/test_theme_grab.py` (пиксельная проба «год цел»), точечные пины хостов в `test_world_snapshot_*`/`test_event_dialog_island`/`test_entity_card_*`
- **Табы чар-листов переключаются активацией (B1, spec `character-sheet-editor`)**: у штатной checkable-кнопки мост доступности не даёт ни одного действия, поэтому `ThemeTabButton` держит `Accessible.onPressAction: control.click()` в компоненте — один Press кликает вкладку как мышь и двигает `currentIndex`/checked-пару; face вкладки штатный, не пере-аннотирован — `tests/presentation/test_sheet_list_accessibility.py`, `test_character_sheet_list_dialog.py`
- **Пресет выбирается одиночным нажатием (B3, spec `character-sheet-editor`)**: активация строки пресета принимает диалог (сначала выбирает строку, затем путь «ОК»: шаблон создаётся, диалог закрывается с accepted-исходом); без нажатия — только подсветка — `tests/presentation/test_character_sheet_preset_dialog.py`
- **Правка редактора и Fill видна в ряду (B2, spec `character-sheet-editor`)**: вместо не проецируемого `QMenuBar` — именованный ряд «Отменить/Повторить/Копировать/Вставить/Дублировать» (редактор) и «Отменить/Повторить» (Fill: редактируемый — виден, read-only — скрыт как прежний `hide()` меню), те же хоткеи `QAction` и тот же вход в историю правок; живые нажатия двигают историю — `tests/presentation/test_character_sheet_editor_dialog.py`, `test_character_sheet_fill_dialog.py`, `test_sheet_window_islands_qml.py`
- **Строки умеют описание (F6, spec `qml-accessibility`)**: у `RowItem` строковый `accessibleDescription` → `Accessible.description`; лаунчер заполняет его «Открывает игру» — сырой `AXDescription` читается живьём; кнопка «✎» музыки именована «Изменить ссылку на музыку» (CR2/F4) — `tests/presentation/test_rowitem_accessibility.py`, `test_launcher_qml.py`, `test_entity_card_accessibility.py`
- **Посадка — штатные чекбоксы с ролью и именем (TB3-усиление, spec `character-sheet-host`)**: строка-виджет несёт `QCheckBox` с именем, состояние переключается из дерева доступности — `tests/ui/test_table_host_seats_accessibility.py`, `tests/ui/test_table_host_panel_close.py`
- **Лимиты без молчаливого роста**: четыре координатных предела тронуты не были; пятый (cocoa-проекция `ListItem` → статический текст) добавлен тем же изменением, открывшим его, с живой минимальной пробукой на зафиксированной сборке — норма в `AGENTS.md`, серия живых прогонов в `docs/qa/2026-09-25-accessibility-audit.md`

## 20. Визуальная сетка: шкала ширин, полосы рядов, карточка и мастер (nri-0018)
> Статус (2026-09-26, change `nri-0018-grid-alignment-and-card`): ✅ авто — все строки закреплены оффскрин-тестами (файлы по строкам), полный гейт `QT_QPA_PLATFORM=offscreen python -m pytest --cov=app` зелёный (3942 passed, 3 skipped, покрытие 100 %); живой ре-аудит computer-use финальным таском (throwaway `qa-2026-09-26`, AI-кнопки не трогались) — `docs/qa/2026-09-26-grid-alignment-live-audit.md`: мастер измерен ровно 1040, ширины замеренных окон ровно ступеней, OBS-1 закрыт на default-старте (остаток на узких сохранёнках остаётся в его follow-up); OBS-2 закрыт в объёме change (округление content-driven ширины к ступени в island-размере, дописка в QA-отчёте), OBS-3 — follow-up; новых пределов координатности не заведено.

- **Ширины окон на шкале 40** (spec `ui-layout-grid`): список чар-листов и панель стола 440, лаунчер 480, пресет 560, LLM/xlsx 640, событие/изображение/документ 720, floor карточки 760, мастер 1040 (пересчитан Д7, пин — формулой ступени), заполнение 1120, редактор 1280; QML `implicitWidth` и python-этаж диалога — две половинки одной ступени; для карточки 760 и списка 440 ступень — floor, а контент выше floor **поднимается к ближайшей ступени округлением общего слайзера** (степень 40 и правило живут в одном модуле `app/presentation/layout_grid.py`, ceil в `fit_dialog_to_island`: карточка 817→840, список 620→640, типы событий 460→480; floor, покрывающий контент, остаётся floor'ом, высоты шкалу не касаются — **OBS-2 закрыт в объёме change решением владельца от 2026-09-26**, дописка в `docs/qa/2026-09-26-grid-alignment-live-audit.md`); отступы строки поиска и просмотрщика документа подтянуты к общему `space.md` — `tests/presentation/test_window_width_scale.py`, `test_island_dialog_sizing.py`
- **Мелкие действия — один квадрат 32×32** (spec `qml-components`): ✕ шапки, ✎ музыки, ✨ всех полей и полных генераций (включая состояние «…»), «+» шкалы, ↑↓−+ редактора и типов событий строятся компонентом `ThemeIconButton`; сторонних размеров глифов в островах нет (греп-скан) — `tests/presentation/test_theme_icon_button.py`, `test_theme_icon_button_usage.py`, пиксельная приёмка `tests/ui/test_theme_grab.py`
- **Полоса высот ряда и оптический центр чекбокса** (specs `qml-components`, `ui-layout-grid`): индикатор `ThemeCheckBox` в центре чернил подписи (±1 raster, метрики шрифта), полоса чекбокса = полоса кнопок ряда; строка навигации календарного попапа «◀ месяц | год ▶ ☐ до н.э.» — все контролы 32 px с общим центром — `tests/presentation/test_theme_checkbox_optical.py`, `test_calendar_nav_band.py`
- **Нативные индикаторы тематизированы, off-skin цел** (spec `ui-widget-catalog`): `::indicator` радио мастера (chrome-лист) и чекбокса эры (popup-лист `GameCalendarEraCheck`) из токенов; без валидной темы переключатели нативные — `tests/presentation/test_theme_compile.py`, `test_calendar_nav_band.py`
- **Вкладки полные, старт 1280×800, сохранённая рамка не режет вкладки** (spec `main-window`): `tab_label` удалён из реестра, подпись = имя = подсказка = `plural_label`; дефолт главной сессии — 1280×800; сохранённая узкая ширина расширяется до порога панели деталей (`min_tabs_width`), позиция/высота не двигаются; OBS-1 закрыт на default-старте минимумом панели из той же константы (живой ре-аудит: «Показать всё» целый с запасом; вёрстка строки на сохранённых узких рамках остаётся в его follow-up) — `tests/presentation/test_detail_tabs_labels.py`, `test_geometry_memory.py`, `test_main_window_default_start.py`
- **Карточка и событие: русский заголовок и ✨ в шапке** (specs `qml-shell`, `entity-generation`): заголовок «Карточка: <русское имя типа>» (сырые ключи греп-пинутся), полная генерация — в слоте `ThemeSheetHeader` сразу за текстом заголовка (objectName/aiState/гейты прежние), в строке «Название» — только генерация названия — `tests/presentation/test_sheet_header_actions.py`, `test_sheet_parity.py`
- **Мастер: левая граница и колонка по содержимому** (spec `calendar-wizard`): радио «Стандартный/Кастомный» на левой границе заголовка без поштучного центрирования; левая колонка = natural sizeHint стека, округлённый к 40 (потолок 520), излишек — предпросмотру, minimum пересобран (ступень 40, измеренная ширина 1040) — `tests/presentation/test_calendar_wizard_dialog.py`, `test_window_width_scale.py`
