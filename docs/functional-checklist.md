# Функциональный чек-лист

Регрессионная сеть на уровне пользовательских функций (компенсирует отсутствие E2E-тестов).
После **каждого** коммита реализации прогоняются затронутые группы; после финала change — полный прогон всех групп.

Статус:

| Статус | Значение |
|---|---|
| ✅ | Группа проверена, всё работает как раньше |
| ❌ | обнаружен регресс |
| ⏳ | не проверялась |

**Baseline (до рефакторинга, v0.13.0):** авто-прогон ✅ — 389/389 тестов, offscreen-запуск (launcher → Application.start → MainWindow → shutdown) без ошибок; ручной smoke 14 групп — закрыт без прогона (решение 2026-08-22): к моменту оформления чек-листа код уже рефакторен (§A–§D), baseline на старом коде невалиден; регрессионная страховка — характеризационные тесты волны 1 (tasks 2.1–2.5)

**Итог nri-0005 (2026-09-23, применение `nri-0005-architecture-refactor`):** ручной клик-прогон headless-сессиями не выполнялся; вместо него — сквозная приёмка полным `QT_QPA_PLATFORM=offscreen python -m pytest` + маппинг затронутых рефакторингом групп на их e2e-аналоги (прогнаны в составе полного прогона): сохранение сущности/события цепочкой через единицу работы — `tests/ui/test_e2e_crud.py`, `test_e2e_events.py`, `test_e2e_dialog_save_failures.py` ✅ auto; popup-создание связанной сущности с отменой родительского диалога — `tests/ui/test_e2e_wiring_gaps.py` (+регрессия задачи 5.6 «отменённая popup-сущность не оседает в БД») ✅ auto; xlsx-импорт (§9) — `tests/ui/test_e2e_import.py`, `tests/application/test_xlsx_import_*` (за известным timing-флаком, см. заметку флага в `docs/refactoring-audit.md`) ✅ auto; мастер календаря (§8) — `tests/presentation/test_calendar_wizard_*`, `tests/ui/test_e2e_calendar_wizard_entry.py`, `test_e2e_custom_calendar_acceptance.py` ✅ auto; LLM-настройки (§11) — `tests/ui/test_e2e_llm.py`, `test_llm_*`, per-game промты через `LlmSettingsRepository` — `tests/test_application_settings_errors.py` ✅ auto; листы персонажей — `tests/ui/test_e2e_char_sheets.py`, `test_char_sheets_wiring.py`, `tests/presentation/test_sheet_*` ✅ auto. Визуальные ручные пункты (цвет фона рейтинга, цвета снимка мира, 8-шаговый визард в GUI) остаются ⏳ с прежних строк — headless-приёмка их не покрывает.

## 1. Лаунчер игр
> Статус финального прогона (2026-08-22): ✅ авто — `tests/infrastructure/test_game_manager.py` (create/open/delete/import), `tests/presentation/test_views.py` GameLauncherDialog, `tests/test_application_start.py` (смена игры без перезапуска).

- Создать игру (новое имя)
- Открыть игру
- Удалить игру
- Импорт из `.nri` (метаданные отображаются)
- Импорт `.nri` в существующую игру — предупреждение о дубле
- Файл → Сменить игру без перезапуска приложения

## 2. Глобальный поиск
> Статус финального прогона (2026-08-22): ✅ авто — `tests/application/test_search_service.py` (имя/характеристики/предыстория, регистр, дедупликация, группы), `tests/presentation/test_views.py` SearchBar (debounce, старт от 2 символов), `tests/test_integration.py` (поиск на реальных данных через сервисы).

- debounce 300 мс
- старт поиска от 2 символов
- регистронезависимость
- поиск по имени
- поиск по характеристикам
- поиск по предыстории
- клик по результату — открывается карточка сущности
- клик по результату-событию — выделение события на таймлайне
- результаты сгруппированы по типам

## 3. Таймлайн
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/application/test_services.py` TimelineViewModel (сортировка, выбор), `tests/presentation/test_views.py` TimelineWidget (фильтр С/По, сброс, сигналы), `tests/test_integration.py::test_timeline_ordering`; ⏳ ручной: сохранение фильтра после перезапуска игры в GUI.

- сортировка событий по дате
- выбор события — панель деталей
- двойной клик — редактирование события
- кнопка `+` — создание события
- контекстное меню — создание самостоятельных сущностей
- фильтр «С / По»
- сброс фильтра «✕»
- фильтр сохраняется после перезагрузки (per-game)

## 4. Событие (создание/редактирование)
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_dialogs.py` EventDialog (валидация, даты, бессрочно), `tests/application/test_event_service.py` (создание/обновление со связями: привязать/создать/отвязать), `tests/test_integration.py` (полный цикл).

- валидация обязательных полей (кнопка «Сохранить» блокируется)
- выбор дат
- «Бессрочно» → `∞`
- связи: привязать существующих сущностей
- связи: создать новую связанную сущность
- связи: отвязать лишние
- предыстория сохраняется
- характеристики сохраняются

## 5. Карточка сущности (4 типа)
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_dialogs.py` EntityCardDialog (поля по типу, image, music, M2M-секции, рейтинг 1–20, spec-driven набор), `tests/application/test_entity_service.py` (sync_related); ⏳ ручной: визуальный цветовой фон рейтинга.

- поля по типу: personality — только персонаж
- поля по типу: image — персонаж / организация / локация
- поля по типу: tasks — 3 типа
- поля по типу: music_url — все типы
- рейтинг 1–20 + цветовой фон
- изображение: загрузка
- изображение: превью
- изображение: удаление
- музыка: ссылка
- музыка: режим редактирования
- M2M-вкладки: привязать существующих
- M2M-вкладки: создать новую связанную
- M2M-вкладки: отвязать лишние

## 6. @Mention
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_mention_text_edit.py` (popup от 2 символов, навигация, Enter/клик, кликабельная ссылка, маркеры в старых текстах, roundtrip HTML).

- `@` + 2 символа → popup с иконками
- навигация стрелками + Enter
- выбор кликом
- ссылка кликабельная → открывает карточку сущности
- обратная совместимость старых текстов с маркерами

## 7. World Snapshot
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/presentation/test_views.py` WorldSnapshotWidget (секции, «Показать всё», signal снапшота), `tests/application/test_services.py` (активные события по дате); ⏳ ручной: цвета по рейтингу, тултипы.

- выбор даты → активные события (диапазон / `∞`)
- 5 секций без дубликатов
- «Показать всё»
- цвет по рейтингу
- тултипы
- двойной клик → карточка
- статистика внизу

## 8. Календарь игры
> Статус финального прогона (2026-09-21, закрытие карты C0–C6): ✅ авто — `tests/domain/test_game_calendar.py` (ядро), `tests/presentation/test_calendar_wizard_viewmodel.py` + `test_calendar_wizard_dialog.py` (экраны и flow мастера), `tests/presentation/test_calendar_grid.py` (сетка дней: плашки вставных дней, эра, имена недели), `tests/presentation/test_theme_date_popup.py` (попапы дат: клик по плашке и чекбокс эры), `tests/ui/test_e2e_calendar_wizard_entry.py` (точки входа и первый запуск), `tests/ui/test_e2e_custom_calendar_acceptance.py` (обе приёмочные сцены реальными кликами, см. ниже), `tests/test_calendar_seed_migration.py` (посев новой игры), подписание имён — `tests/presentation/test_custom_months.py`, failure-fallback битого ключа — `tests/test_application_settings_errors.py`. Диалог «Названия месяцев…» удалён, его место занял мастер.

- вход **Настройки → Календарь…** доступен в любой игре, текущий вид предвыбран
- первый запуск новой игры — мастер модалкой до показа главного окна; закрыт без применения = «Стандартный», флаг «показан» пишется один раз
- черновик мастера: пройденные ступени сохранены, повторный вход продолжает с сохранённого, брошенный черновик зовёт мастера обратно
- выбор вида «Стандартный» / «Кастомный»
- неделя: длина и имена дней
- месяцы: число, имя и длина каждого (строки «имя | длина»)
- вставные дни: добавление правила с прикреплением после месяца-хозяина, удаление и порядок
- предпросмотр: сводка + живая сетка будущего календаря
- старые игры: отсутствие ключа = «Стандартный», мастер не навязывается; битая настройка = пресет с одним предупреждением
- старые названия месяцев (`custom_months`) мигрируют в настройку календаря
- подписи новым календарём — таймлайн, детали, snapshot, календари выбора, поиск; распространение применения без перезапуска
- сцена «Мастер до конца и жизнь кастомной игры» (e2e, реальные клики): мастер неделя→месяцы→вставные дни→предпросмотр→«Применить»; событие на вставном дне (плашка `GameCalendarIntercalaryChip`) и событие с эрой «до н.э.» (клетка + эра-чекбокс); шкала, лента, детальная и снимок мира подписывают новые имена, вставной день — своим именем без номера, эра — суффиксом «до н.э.»; после перезапуска календарь и обе записи без изменений
- сцена «Правка в середине игры» (e2e): укорочение месяца, задевающее даты записей → экран «Перенос дат» с именами перенесённых записей («Запись / Поле / Было / Станет») → «Перенести и применить» → лента и детальная показывают перенесённые даты

## 9. XLSX-импорт
> Статус финального прогона (2026-08-22): ✅ авто — `tests/application/test_xlsx_import_service.py` (5 типов, заголовки, пропуск с предупреждением, даты текст+native, отчёт), `tests/presentation/test_xlsx_import_dialog.py` (сигнал, прогресс).

- импорт 5 типов сущностей
- заголовки колонок по имени
- строки без обязательных полей пропускаются с предупреждением
- даты `YYYY-MM-DD` (текст)
- даты native Excel
- прогресс импорта
- отчёт «Создано / Пропущено»

## 10. Экспорт/импорт `.nri`
> Статус финального прогона (2026-08-22): ✅ авто — `tests/infrastructure/test_game_manager.py` (экспорт/импорт `.nri`, структура ZIP `game.db` + `meta.json`); меню-экспорт заводится в `tests/test_application_start.py` (start/shutdown smoke).

- экспорт через меню
- структура ZIP: `game.db` + `meta.json`
- импорт через лаунчер

## 11. LLM-AI-ассистент
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/infrastructure/test_llm_config.py` (конфиг 0600), `test_llm_errors.py` + `test_llm_providers.py` (RU-ошибки, ретраи 429/5xx, статусы not_configured/ready), `tests/presentation/test_llm_viewmodel.py` (очередь, блокировка поля), `test_llm_setup_dialog.py`, per-game промты — `tests/test_application_settings_errors.py`; ⏳ ручной: прохождение визарда 8 шагов в GUI.

- визард 8 шагов
- «Проверить соединение» (1 токен)
- статусы `not_configured` / `ready`
- ✨ на текстовых полях
- очередь генераций
- блокировка поля при генерации
- ошибки: RU-тексты
- ошибки: ретраи 429/5xx
- глобальный конфиг 0600
- per-game промты

## 12. Изображения
> Статус финального прогона (2026-08-22): ✅ авто — `tests/presentation/test_image_utils.py` (ресайз ≤1000×1000, base64, превью 280, thumbnail 100).

- ресайз ≤ 1000×1000
- base64 в БД
- превью 280×280
- thumbnail 100×100

## 13. Бессрочность
> Статус финального прогона (2026-08-22): ✅ авто — `tests/domain/test_entities.py` (end_date=None), `tests/infrastructure/test_repositories.py::TestEventRepository::test_get_events_at_date` (бессрочные включаются в диапазон), EventDialog «Бессрочно» — `tests/presentation/test_dialogs.py`, `format_game_date(None, «∞»)`.

- чекбокс скрывает поле даты
- `∞` на таймлайне
- snapshot активен на любую дату после начала
- фильтр включает бессрочные

## 14. Рейтинг
> Статус финального прогона (2026-08-22): ⚙️ авто — `tests/presentation/test_dialogs.py` (spinbox 1–20, clamp в populate); ⏳ ручной: цвет серый→красный в списках и snapshot.

- spinbox 1–20
- цвет серый → красный в списках
- цвет серый → красный в snapshot

## 15. Дерево accessibility QML-островов (nri-0012)
> Статус (2026-09-23, change `nri-0012-qml-accessibility`): ✅ авто — оффскрин-тесты по острову (`tests/presentation/test_*_accessibility*.py`, точечные ассерты в `test_search_bar_island`/`test_llm_setup_island`/`test_character_sheet_*_dialog`/`test_event_types_dialog`/`test_thin_dialog_islands`/`test_mention_field_qml`) плюс гарды контракта `tests/test_qml_accessibility_conventions.py` и `tests/test_qml_stock_controls_accessibility.py` (второй пинит оффскрин-половину 4.2; «имя штатной кнопки равно тексту» — за неё живой аудит). Живой прогон — `docs/qa/2026-09-23-accessibility-audit.md`: адресуемость без координат подтверждена, находки — follow-up FI-1…FI-6 (в AGENTS), список четырёх пределов не расширен.

Интерактивные элементы каждого экрана доступны из дерева accessibility с ролью и именем — без координат экрана:

- **Лаунчер (`LauncherRoot`)** — строки игр `ListItem` с именем строки (контракт `RowItem`), текстовые кнопки штатно;
- **Глобальный поиск (`SearchBarRoot`)** — поле «Поиск по всем сущностям» (значение — в value-слоте), строки результатов `ListItem` (контракт строки);
- **Таймлайн (`TimelineRoot`/`TimelineRowDelegate`)** — строка события `ListItem` «Открывает событие», кнопка «Добавить событие»;
- **Панель деталей (`DetailPanelRoot`)** — строка-карточка `ListItem` «Открывает карточку», картинка-кнопка «Открыть изображение», табы штатно;
- **Снимок мира (`WorldSnapshotRoot`)** — строка сущности `ListItem` «Переходит к сущности», заголовок секции `Button` «Развернуть или свернуть раздел»;
- **Dialog события (`EventDialogRoot`)** — «Название события», «Дата начала»/«Дата конца», «Тип события», mention-поля «Характеристики»/«Предыстория», свотч «Цвет типа события», AI-кнопки «Сгенерировать: …» (целиком — «Сгенерировать: Событие»);
- **Типы событий (`EventTypesRoot`)** — «Название типа события», свотчи «Цвет палитры №N», стрелки «Поднять тип»/«Опустить тип», строки типов (контракт `RowItem`);
- **Карточка сущности (`EntityCardRoot`)** — «Название», даты, «Рейтинг», «Ссылка на музыку», mention-поля по меткам, AI-кнопки «Сгенерировать: …»; кнопки «Сохранить»/«Открыть чар-лист» штатно;
- **Чар-лист, дизайн-холст (`SheetCanvas`)** — поля по типу (`EditableText`/`CheckBox`/`Button`), инлайн-редактор «Редактирование поля»;
- **Редактор листов (`SheetEditorRoot`)** — «Ориентация страницы», «Переименование страницы», «Вверх»/«Вниз», «Удалить/Добавить страницу», «Позиция X/Y», «Ширина/Высота», «Кегль», «Текст поля», «Число», «Минимум»/«Максимум», опции;
- **Заполнение листа (`SheetFillRoot`)** — «Значение поля», «Значение поля (многострочно)», «Значение из списка»;
- **Пресеты листов (`SheetPresetRoot`/`SheetListRoot`)** — «Имя листа», «Текст лицензии», строки списка (текст/контракт `RowItem`);
- **Просмотр документа (`DocViewerRoot`)** — «Текст документа»;
- **XLSX-импорт (`XlsxImportRoot`)** — «Путь к файлу .xlsx», «Требования к формату файла»;
- **LLM-настройки (`LlmSetupRoot`)** — «Endpoint», «Модель», «Ключ API», «Описание мира», промты по `modelData.label`;
- **Просмотр изображения (`ImageViewerRoot`)** — только штатные текстовые кнопки (имя из текста).

Пределы (адресация без координат честно невозможна — см. контракт в `AGENTS.md`): ① строки попапа упоминаний — только клавиатура после фокуса поля; ② набранный текст `MentionField` и инлайн-редактора листа — value-слота у кастомного Item нет (мост — follow-up NRI-0013); ③ нативные попапы дат и выпадаюков `ThemeComboBox` — отдельные системные окна; ④ текст tooltip-моста — в дерево не переносится.
