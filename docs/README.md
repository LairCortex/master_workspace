# НРИ Сценарий Менеджер

Десктоп-приложение для создания и управления сценариями настольных ролевых игр (НРИ).

## Требования

- Python 3.11+
- macOS / Linux / Windows

## Установка (для разработки)

```bash
# Клонировать репозиторий
cd master_workspace

# Создать виртуальное окружение
python3.12 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# .venv\Scripts\activate   # Windows

# Установить зависимости
pip install -e ".[dev]"
```

## Запуск приложения

```bash
source .venv/bin/activate
python -m app.main
```

При первом запуске откроется **диалог выбора игры** — создайте новую игру или выберите существующую. Каждая игра хранится в отдельном файле `games/<имя>.db`.

## Сборка (исполняемый файл)

Для создания standalone-приложения без необходимости установки Python.

> **Важно:** PyInstaller не поддерживает кросс-компиляцию — сборку нужно запускать на каждой целевой ОС. Папка `games/` создаётся рядом с исполняемым файлом при первом запуске.

### Быстрая сборка (Python уже установлен)

```bash
pip install -e ".[build]"
python build_app.py --clean
```

### Сборка на Windows с нуля

Если на компьютере ещё не установлен Python:

1. **Скачайте Python 3.11+** с [python.org/downloads](https://www.python.org/downloads/)
2. Запустите установщик. **Обязательно** поставьте галочку **"Add Python to PATH"** на первом экране
3. Нажмите **"Install Now"**
4. Откройте **PowerShell** (Win+X → Terminal) или **cmd** (Win+R → `cmd`)
5. Проверьте установку:

```powershell
python --version
# Должно вывести: Python 3.11.x или выше
```

6. Перейдите в папку проекта и соберите:

```powershell
cd C:\путь\к\master_workspace

# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
.venv\Scripts\activate

# Установить проект и зависимости для сборки
pip install -e ".[build]"

# Собрать приложение
python build_app.py --clean
```

7. Результат: `dist\nri_manager\nri_manager.exe`
8. Папку `dist\nri_manager\` можно скопировать на любой Windows-компьютер и запустить `nri_manager.exe` — Python не требуется

### Сборка на macOS с нуля

1. Установите Python через Homebrew:

```bash
# Установить Homebrew (если ещё нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Python
brew install python@3.12
```

2. Соберите:

```bash
cd ~/путь/к/master_workspace
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[build]"
python build_app.py --clean
```

3. Результат: `dist/НРИ Сценарий Менеджер.app`

### Сборка на Linux (Ubuntu/Debian) с нуля

1. Установите Python и системные зависимости:

```bash
sudo apt update
sudo apt install python3.12 python3.12-venv python3-pip \
    libgl1-mesa-glx libegl1-mesa libxkbcommon0 libdbus-1-3
```

2. Соберите:

```bash
cd ~/путь/к/master_workspace
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[build]"
python build_app.py --clean
```

3. Результат: `dist/nri_manager/nri_manager`

### Результаты сборки

| Платформа | Результат | Путь |
|-----------|-----------|-------------------------------|
| **macOS** | `.app` bundle | `dist/НРИ Сценарий Менеджер.app` |
| **Windows** | `.exe` + папка | `dist\nri_manager\nri_manager.exe` |
| **Linux** | бинарник + папка | `dist/nri_manager/nri_manager` |

### Автоматическая сборка (CI/CD)

Проект настроен на автоматическую сборку через **GitHub Actions**:

- **Push в main** — запускает тесты и сборку на macOS, Windows, Linux. Артефакты доступны для скачивания 14 дней
- **Тег `v*`** (например `v0.7.0`) — тесты, сборка, автоматический **GitHub Release** с архивами для всех трёх ОС

Для создания релиза:

```bash
git tag v0.7.0
git push --tags
```

Скачать собранное приложение: вкладка **Releases** на странице репозитория GitHub.

## Запуск тестов

```bash
source .venv/bin/activate
python -m pytest -v
```

## Использование

### Выбор игры (лаунчер)

При запуске приложения открывается диалог выбора игры:

- **Новая игра** — введите название, создастся новый файл БД
- **Открыть** — откройте выбранную из списка игру
- **Удалить** — удалите игру (с подтверждением)
- **Импорт** — загрузить игру из `.nri`-архива (с подтверждением метаданных)

Переключение между играми без перезапуска: **Файл → Сменить игру**.

### Главное окно

Приложение состоит из трех зон:

1. **Поиск** (сверху) — глобальный live-поиск по всем сущностям (события, организации, персонажи, предметы, локации). Начните вводить от 2 символов — результаты появятся автоматически с задержкой 300мс. Поиск регистронезависимый, работает по имени, характеристикам и предыстории. Клик по результату открывает карточку сущности или выделяет событие на таймлайне.

2. **Таймлайн** (слева) — хронологический список событий, отсортированный по дате начала. Даты отображаются в формате `dd.MM.YYYY`. События визуально разделены, выбранное событие подсвечивается. Двойной клик по событию открывает диалог редактирования. Клик по событию загружает его содержимое в центральную панель.

3. **Детали события** (центр) — четыре вкладки с составляющими выбранного события:
   - Организации
   - Персонажи
   - Предметы
   - Локации

   Каждая сущность в списке показывает сводку: рейтинг, характеристики, предысторию, личность, задачи и количество связей. Если у сущности есть изображение — показывается thumbnail 100×100. Фон элемента окрашен в зависимости от рейтинга (серый → красный).

   Двойной клик по любой сущности открывает её карточку для просмотра и редактирования.

### Экспорт и импорт игр

#### Экспорт
1. Откройте игру
2. **Файл → Экспорт игры…**
3. Выберите путь для сохранения `.nri`-файла

Архив `.nri` содержит полную копию базы данных и метаданные (имя игры, версия, дата экспорта). ZIP-компрессия уменьшает размер файла.

#### Импорт
1. На экране выбора игры нажмите **«Импорт»**
2. Выберите `.nri`-файл
3. Подтвердите импорт (отображаются имя игры, версия, дата экспорта)

Если игра с таким именем уже существует — будет показано предупреждение.

### Обзор мира (World Snapshot)

Третья панель главного окна — визуальное представление мира на выбранную дату:

1. Выберите дату и нажмите **«Показать»**
2. Отображаются все события, в чей диапазон дат (дата начала — дата конца) попадает выбранная дата
3. Дерево содержит 5 сворачиваемых секций:
   - **Активные события** — список найденных событий (стартует свёрнутой)
   - **Локации** — все локации из этих событий
   - **Организации** — все организации
   - **Персонажи** — все персонажи
   - **Предметы** — все предметы
4. Каждая сущность появляется ровно один раз (без дубликатов)
5. Фон узлов окрашен по рейтингу (серый → красный)
6. При наведении — тултип с описанием и рейтингом
7. Двойной клик — открывает карточку сущности
8. Внизу — статистика: количество событий, персонажей, организаций, локаций, предметов
9. Кнопка **«Показать всё»** отображает все события и связанные сущности без учёта даты

### Кастомные названия месяцев

Для фэнтези-сеттингов можно заменить стандартные названия месяцев (Январь–Декабрь) на пользовательские:

1. Откройте **Настройки → Названия месяцев…**
2. Введите свои названия для нужных месяцев. Пустые поля сохраняют стандартное название.
3. Нажмите **«Сохранить»** — все даты в приложении обновятся
4. Кнопка **«Сбросить»** возвращает стандартные названия

Кастомные названия отображаются:
- На таймлайне (даты событий)
- В панели деталей
- В обзоре мира
- В календаре при выборе дат (навигация и выпадающий список месяцев)
- В результатах поиска

Настройки хранятся в БД каждой игры — у каждой игры может быть свой календарь.

### Меню

| Меню | Пункт | Действие |
|---|---|---|
| **Файл** | Сменить игру | Открывает лаунчер для переключения на другую игру |
| **Файл** | Экспорт игры… | Сохраняет текущую игру как `.nri`-архив |
| **Настройки** | Названия месяцев… | Настройка кастомных названий месяцев |
| **Настройки** | Импорт событий из .xlsx… | Импорт событий из Excel-файла ([формат](#формат-xlsx-для-импорта)) |
| **Настройки** | Импорт персонажей из .xlsx… | Импорт персонажей из Excel-файла ([формат](#формат-xlsx-для-импорта)) |
| **Настройки** | Импорт локаций из .xlsx… | Импорт локаций из Excel-файла ([формат](#формат-xlsx-для-импорта)) |
| **Настройки** | Импорт организаций из .xlsx… | Импорт организаций из Excel-файла ([формат](#формат-xlsx-для-импорта)) |
| **Настройки** | Импорт предметов из .xlsx… | Импорт предметов из Excel-файла ([формат](#формат-xlsx-для-импорта)) |
| **LLM** | Настройка LLM… | Открывает визард настройки AI-ассистента |
| **О приложении** | Документация | Показывает README приложения |
| **О приложении** | Changelog | Показывает историю изменений |

### Формат .xlsx для импорта

Первая строка файла — **заголовки столбцов**. Порядок столбцов не важен, они определяются по имени. Строки без обязательных полей пропускаются с предупреждением.

Дата может быть задана в формате `YYYY-MM-DD` (текст) или как нативная дата Excel.

#### События

| Столбец | Обязательный | Описание |
|---|---|---|
| `name` | да | Название события |
| `start_date` | да | Дата начала |
| `end_date` | нет | Дата конца |
| `characteristics` | нет | Описание / характеристики |
| `backstory` | нет | Предыстория |

#### Персонажи

| Столбец | Обязательный | Описание |
|---|---|---|
| `name` | да | Имя персонажа |
| `start_date` | да | Дата начала |
| `end_date` | нет | Дата конца |
| `characteristics` | нет | Описание / характеристики |
| `backstory` | нет | Предыстория |
| `personality` | нет | Личность |
| `tasks` | нет | Задачи |
| `music_url` | нет | Ссылка на музыкальную тему |
| `image` | нет | Путь к изображению (относительный к .xlsx или абсолютный). PNG, JPG, BMP, GIF, WebP. Альтернативное имя: `изображение` |

#### Локации

| Столбец | Обязательный | Описание |
|---|---|---|
| `name` | да | Название локации |
| `start_date` | да | Дата начала |
| `end_date` | нет | Дата конца |
| `characteristics` | нет | Описание / характеристики |
| `backstory` | нет | Предыстория |
| `tasks` | нет | Задачи |
| `music_url` | нет | Ссылка на музыкальную тему |
| `image` | нет | Путь к изображению. Альтернативное имя: `изображение` |

#### Организации

| Столбец | Обязательный | Описание |
|---|---|---|
| `name` | да | Название организации |
| `start_date` | да | Дата начала |
| `end_date` | нет | Дата конца |
| `characteristics` | нет | Описание / характеристики |
| `backstory` | нет | Предыстория |
| `tasks` | нет | Задачи |
| `music_url` | нет | Ссылка на музыкальную тему |
| `image` | нет | Путь к изображению. Альтернативное имя: `изображение` |

#### Предметы

| Столбец | Обязательный | Описание |
|---|---|---|
| `name` | да | Название предмета |
| `start_date` | да | Дата начала |
| `end_date` | нет | Дата конца |
| `characteristics` | нет | Описание / характеристики |
| `backstory` | нет | Предыстория |
| `music_url` | нет | Ссылка на музыкальную тему |

### Создание события

1. Нажмите кнопку **"+"** в верхней части таймлайна
2. Заполните обязательные поля (отмечены `*`):
   - **Название** — имя события
   - **Дата начала** — когда событие начинается
   - **Дата конца** — когда событие заканчивается
   - **Характеристики** и/или **Предыстория** — описание события
3. Опционально: добавьте связанные сущности через вкладки внизу диалога
4. Нажмите **"Сохранить"** — кнопка активна только когда все обязательные поля заполнены

### Редактирование события

1. Дважды кликните на событие в таймлайне
2. Откроется диалог с предзаполненными полями и вкладками связанных сущностей
3. Можно изменить название, даты, описание, а также добавить/удалить связанные организации, персонажей, предметы и локации
4. Нажмите **"Сохранить"** — таймлайн и детали обновятся автоматически

### Редактирование сущности

1. Выберите событие на таймлайне
2. Дважды кликните на сущность в правой панели (на любой вкладке)
3. Откроется карточка с полями, специфичными для типа сущности:
   - **Персонаж**: имя, рейтинг, даты, описание, личность, задачи, изображение
   - **Организация**: имя, рейтинг, даты, описание, задачи, изображение
   - **Локация**: имя, рейтинг, даты, описание, задачи, изображение (карта)
   - **Предмет**: имя, рейтинг, даты, описание
4. Измените нужные поля и нажмите **"Сохранить"**

### @Mention — ссылки на сущности в тексте

Во всех многострочных текстовых полях (характеристики, предыстория, личность, задачи) можно вставлять гиперссылки на другие сущности:

1. Введите `@` и минимум 2 символа имени
2. Появится выпадающий список найденных сущностей с иконками типов
3. Продолжайте вводить символы для сужения поиска или удаляйте Backspace
4. Выберите сущность кликом или стрелками + Enter
5. Ссылка вставится в текст как кликабельное имя (синий цвет)
6. Клик по ссылке откроет карточку сущности для редактирования

Поиск работает по именам всех типов: события, организации, персонажи, предметы, локации. В поле «Название» ссылки не поддерживаются (только в многострочных полях).

### Система рейтинга

Все сущности имеют рейтинг от 1 до 20:

- **1** — наименее важная (серый фон)
- **20** — наиболее важная (красный фон)
- Рейтинг задаётся при создании/редактировании карточки через `QSpinBox`
- В списке сущностей фон элемента окрашивается в соответствии с рейтингом

### Изображения

Для персонажей, организаций и локаций доступна загрузка изображений:

- В карточке сущности слева отображается превью (до 280×280)
- **"Выбрать файл"** — выбрать изображение с диска (PNG, JPG, BMP, GIF, WebP)
- Изображение автоматически ресайзится до 1000×1000 и сохраняется в БД как base64
- **"Убрать"** — удалить изображение
- В списке сущностей изображение показывается как thumbnail 100×100

### Управление связями между сущностями

Все M2M связи из схемы БД доступны для управления в карточках:

| Карточка | Связанные сущности |
|---|---|
| **Организация** | Персонажи, Предметы, Локации |
| **Персонаж** | Предметы, Локации, Организации |
| **Предмет** | Локации, Персонажи, Организации |
| **Локация** | Персонажи, Организации, Предметы |

Доступные действия:
- **"Привязать существующего"** — выбрать из списка всех сущностей в БД (мультивыбор)
- **"Создать нового"** — откроется карточка для создания новой сущности, после сохранения она автоматически привяжется
- **"Отвязать"** — удалить связь (сама сущность не удаляется)

### Обязательные поля

Для всех сущностей обязательны:
- **Описание** (характеристики и/или предыстория)
- **Дата начала**

**Дата конца** — необязательна. Чекбокс **«Бессрочно»** скрывает поле даты — событие/сущность считается бессрочным. На таймлайне отображается как `∞`. В обзоре мира бессрочные события активны на любую дату после начала.

Даты поддерживают диапазон от 100 до 9999 года — подходит для фэнтези-сеттингов.

## Структура проекта

```
app/
├── main.py                          # Точка входа, DI, qasync, мульти-игра
├── domain/
│   ├── entities/                    # Доменные dataclass-сущности
│   │   ├── description.py
│   │   ├── event.py
│   │   ├── organization.py
│   │   ├── character.py
│   │   ├── item.py
│   │   ├── location.py
│   │   └── rating.py
│   └── enums/
│       └── entity_type.py
├── infrastructure/
│   ├── db/
│   │   ├── database.py              # Async engine, session factory
│   │   ├── game_manager.py          # Управление играми (list/create/delete)
│   │   └── models.py                # SQLAlchemy ORM (7 таблиц + 13 M2M)
│   ├── repositories/
│   │   ├── base_repository.py       # Generic CRUD
│   │   ├── event_repository.py
│   │   ├── organization_repository.py
│   │   ├── character_repository.py
│   │   ├── item_repository.py
│   │   ├── location_repository.py
│   │   └── rating_repository.py
│   └── llm/
│       ├── base_provider.py         # ABC для LLM-провайдеров
│       ├── local_provider.py        # Локальный GGUF (llama-cpp-python)
│       └── model_manager.py         # Скачивание/удаление моделей
├── application/
│   └── services/
│       ├── event_service.py
│       ├── search_service.py
│       ├── entity_service.py
│       └── llm_service.py           # Сборка промтов, очередь генерации
└── presentation/
    ├── state/
    │   └── app_state.py             # Shared state с Qt-сигналами
    ├── utils/
    │   ├── image_utils.py           # Загрузка, ресайз, base64 encode/decode
    │   └── date_utils.py            # Форматирование дат с кастомными месяцами
    ├── viewmodels/
    │   ├── timeline_viewmodel.py
    │   ├── detail_viewmodel.py
    │   ├── search_viewmodel.py
    │   ├── event_dialog_viewmodel.py
    │   ├── entity_viewmodel.py
    │   └── llm_viewmodel.py         # Статус модели, промты, генерация
    └── views/
        ├── main_window.py           # Главное окно + меню
        ├── timeline_widget.py
        ├── detail_panel.py          # Сводка + thumbnails + рейтинг-цвет
        ├── search_bar.py
        ├── event_dialog.py
        ├── mention_text_edit.py     # QTextEdit с @mention автодополнением
        ├── custom_date_edit.py     # QDateEdit с кастомными названиями месяцев
        ├── month_settings_dialog.py # Диалог настройки названий месяцев
        ├── entity_card_dialog.py    # Карточка + превью + рейтинг
        ├── game_launcher_dialog.py  # Выбор игры + импорт
        ├── world_snapshot_widget.py # Обзор мира на дату
        ├── llm_setup_dialog.py     # Визард настройки LLM (8 шагов)
        └── ai_assist_button.py     # Кнопка AI на текстовых полях

.github/workflows/build.yml         # CI/CD: тесты + сборка на 3 ОС
build_app.py                         # Скрипт сборки (PyInstaller)
nri_manager.spec                     # PyInstaller spec-файл
```

### AI-ассистент (LLM)

Приложение поддерживает локальную языковую модель для AI-генерации текстовых полей.

#### Зависимости

AI-ассистент требует дополнительных пакетов (не входят в базовую установку):

```bash
pip install -e ".[llm]"
```

Если пакеты не установлены — кнопки AI будут неактивны, при попытке генерации появится сообщение об ошибке.

#### Настройка (визард из 8 шагов)

1. Откройте **LLM → Настройка LLM…**
2. **Шаг 1 — Модель:** скачайте Qwen 2.5 14B (GGUF, Q4_K_M, ~8.5 ГБ) с HuggingFace. Прогрессбар показывает ход скачивания. Для ускорения скачивания можно задать `HF_TOKEN` в переменных окружения
3. **Шаг 2 — Описание мира:** опишите ваш сеттинг, эпоху, стиль и ключевые особенности мира. Это главный контекст для всех генераций — чем подробнее описание, тем точнее результат
4. **Шаги 3–7 — Промты полей:** для каждого типа сущности (события, организации, персонажи, предметы, локации) можно задать инструкции AI для каждого текстового поля. Если оставить пустым — AI будет ориентироваться только на название поля и описание мира
5. **Шаг 8 — Информация:** предупреждения о потреблении памяти и очереди генерации
6. Нажмите **«Сохранить и закрыть»**

#### Использование

- На каждом текстовом поле (название, характеристики, предыстория, личность, задачи) есть кнопка **✨**
- Если модель настроена: по нажатию AI сгенерирует текст на основе описания мира, промта поля и текущего содержимого
- Если модель не настроена: кнопка неактивна, по нажатию появится сообщение с инструкцией
- Во время генерации поле блокируется, показывается прогресс, окно нельзя закрыть
- Несколько генераций ставятся в очередь и обрабатываются последовательно
- При первом нажатии кнопки AI модель автоматически загружается в оперативную память (~30 секунд)

#### Сборка промта

Финальный промт собирается из двух частей:

**Системный промт (system):**
```
Ты — автор контента для настольной ролевой игры. Мир: {описание мира}
Правила:
- Отвечай ТОЛЬКО готовым текстом для поля, без вопросов, пояснений и рассуждений.
- Не задавай уточняющих вопросов. Додумай сам на основе контекста мира.
- Не добавляй заголовков, маркеров, нумерации — только текст.
- Пиши в стиле и тоне описанного мира.
```

**Пользовательский промт (user):**
```
Создаю: {тип сущности}, поле «{название поля}».
{промт поля, если задан}
Текущий текст пользователя: {содержимое поля, если есть}
```

#### Важно

- Модель использует **~10 ГБ оперативной памяти** пока загружена
- Первая генерация после запуска приложения занимает ~30 секунд на загрузку модели
- Модель хранится в `~/.nri_manager/models/`
- Настройки промтов сохраняются в БД каждой игры (per-game)
- Архитектура поддерживает подключение облачных моделей в будущем

## Стек технологий

| Компонент | Технология |
|---|---|
| UI | PySide6 (Qt 6) |
| ORM | SQLAlchemy 2.0 (async) |
| БД | SQLite (aiosqlite) — отдельный файл на игру |
| Async | asyncio + qasync |
| Сборка | PyInstaller |
| CI/CD | GitHub Actions (macOS, Windows, Linux) |
| Бэкап | ZIP-архив `.nri` (game.db + meta.json) |
| Тесты | pytest, pytest-qt, pytest-mock, pytest-asyncio |
| LLM | llama-cpp-python (GGUF), huggingface-hub |
| Архитектура | MVVM |
